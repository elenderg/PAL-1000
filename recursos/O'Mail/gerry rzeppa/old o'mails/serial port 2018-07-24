This sends and transmits on the serial port now. Took me a while to figure out how to get readfile not to block.

Anyways, "receive from a serial port into a buffer" will read all the bytes available on the serial port. It reads them in 1024 chunks.
