\ copyright © 2006, 2017 a sociedade osmosiana - o ambiente de trabalho

Rotina para que se adicione um botão para a caixa de diálogo usando uma string:
  Se a string estiver em branco,
     esvazie o botão;
     Retorne.
  Se a caixa de diálogo for inexistente, retorne.
  Crie o botão.
  Acrescente o botão para os botões desta caixa de diálogo.
  Atribua a string ao nome deste botão.

Rotina para que se adicione uma cor e um rótulo para um menu:
  Se o menu for inexistente, retorne.
  Crie um comando.
  Acrescente o comando para os comandos deste menu.
  Atribua a largura do botão da paleta à largura deste comando.
  Atribua o rótulo ao rótulo deste comando.
  Atribua a cor à cor deste comando.

Rotina para que se adicione um nome e um rótulo para um menu:
  Se o menu for inexistente, retorne.
  Crie um comando.
  Acrescente o comando para os comandos deste menu.
  Atribua a altura do botão do painel à largura deste comando.
  Atribua o nome ao nome deste comando.
  Atribua o rótulo ao rótulo deste comando.
  Atribua a cor do ambiente de trabalho à cor deste comando. \
  Atribua "à esquerda" ao alinhamento deste comando.

Rotina para que se adicione um nome e um rótulo para um submenu:
  Adicione o nome e "" e o rótulo para o submenu.

Rotina para que se adicione um nome e um atalho de tecla e um rótulo para um submenu:
  Se o submenu for inexistente, retorne.
  Crie um comando.
  Acrescente o comando para os comandos deste submenu.
  Atribua a largura do submenu à largura deste comando.
  Atribua o nome ao nome deste comando.
  Atribua o atalho de tecla ao atalho de tecla deste comando.
  Atribua o rótulo ao rótulo deste comando.
  Atribua a cor do ambiente de trabalho à cor deste comando. \ aqui muda a cor dos submenus - eram brancos
  Atribua "à esquerda" ao alinhamento deste comando.

Rotina para que se adicione a paleta de cores para um submenu usando um rótulo:
  Se o submenu for inexistente, retorne.
  Adicione a cor transparente e o rótulo para o submenu.
  Atribua a largura da paleta de cores dividido por 3 ao largura deste último comando destes comandos deste submenu.
  Atribua "limpar" ao nome deste último comando destes comandos deste submenu.
  Atribua "centralizado" ao alinhamento deste último comando destes comandos deste submenu.
  Adicione a cor branca e o rótulo para o submenu. \ botão que fica depois do botão limpar
  Atribua a largura da paleta de cores dividido por 3 ao largura deste último comando destes comandos deste submenu.
  Adicione a cor preta e o rótulo para o submenu. \ botão que fica depois do botão branco
  Atribua a largura da paleta de cores dividido por 3 ao largura deste último comando destes comandos deste submenu.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor cinza.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor vermelha.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor laranja.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor amarela.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor verde limão.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor verde.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor verde menta.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor azul piscina.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor azul celeste.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor azul.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor púrpura.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor magenta.
  Adicione a paleta de cores para o submenu usando o rótulo e a cor rosa.

Rotina para que se adicione a paleta de cores para um submenu usando um rótulo e uma cor:
  Se o submenu for inexistente, retorne.
  Preserve a cor.
  Atribua 875 à luminosidade desta cor. \ Pretendo mudar pra 800, pra não ficar tão claro como está agora
  Itere. [o código a seguir]
    Adicione a cor e o rótulo para o submenu.
    \Atribua 24 pixels ao raio desta caixa arredondada deste último comando destes comandos deste submenu. \ muda o raio dos botões
    Subtraia 125 desde a luminosidade desta cor. \ aqui seria algo como 80
    Se a luminosidade desta cor for menor do que 125, retorne. \ e aqui teria que ser algo ENTRE 240-241
  Reitere. [Execute novamente o código]

Rotina para que se determine se o usuário clicou em um comando:
  Se a localização deste mouse estiver neste comando, diga sim.
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se determine se o mouse está em cima de um comando:
  Atribua a localização deste mouse para uma localização.
  Se a localização estiver neste comando, diga sim.
  \Se a localização deste mouse estiver na caixa arredondada deste comando, diga sim.
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se ajuste a caixa de diálogo:
  Se a caixa de diálogo for inexistente, retorne.
  Ajuste a caixa de diálogo (botões).
  Ajuste a caixa de diálogo (caixa arredondada).
  Ajuste a caixa de diálogo (texto de aviso).
  Ajuste a caixa de diálogo (caixa de texto).

Rotina para que se ajuste a caixa de diálogo (botões):
  Atribua a caixa de exibição deste painel superior direito e 0 pixels para uma caixa arredondada. \ muda o raio da borda dos botões sim e não
  Atribua a coordenada X2 desta caixa arredondada menos a largura do botão do painel à coordenada X1 desta caixa arredondada.
  Itere. [o código a seguir]
    Obtenha um botão desde os botões desta caixa de diálogo (regressivamente).
    Se o botão for inexistente, retorne.
    Atribua a caixa arredondada à caixa arredondada deste botão.
    Mova a caixa arredondada para a esquerda a largura do botão do painel.
  Reitere. [Execute novamente o código]

Rotina para que se ajuste a caixa de diálogo (texto de aviso):
  Atribua a coordenada X1 desta caixa arredondada desta caixa de diálogo mais 12 pixels à coordenada X1 desta caixa de alerta desta caixa de diálogo. \era 1/8 polegada. Pode também ser 6-35/100 milímetros ou 6350 micrômetros
  Atribua a coordenada Y1 desta caixa arredondada desta caixa de diálogo mais 4 pixels à coordenada Y1 desta caixa de alerta desta caixa de diálogo. \ era 4 px
  Obtenha uma largura usando o texto de aviso desta caixa de diálogo e a fonte padrão.
  Atribua a coordenada X1 desta caixa de alerta desta caixa de diálogo mais a largura mais 8 pixels à coordenada X2 desta caixa de alerta desta caixa de diálogo.
  Atribua a coordenada Y2 desta caixa arredondada desta caixa de diálogo menos 4 pixels à coordenada Y2 desta caixa de alerta desta caixa de diálogo.

Rotina para que se ajuste a caixa de diálogo (caixa arredondada):
  Atribua a caixa de exibição deste painel superior direito e 0 pixels [raio dda borda caixa de diálogo] ao caixa arredondada desta caixa de diálogo.
  Se os botões desta caixa de diálogo estiverem vazios, retorne.
  Atribua a coordenada X1 deste primeiro botão desta caixa de diálogo à coordenada X2 desta caixa arredondada desta caixa de diálogo.

Rotina para que se ajuste a caixa de diálogo (caixa de texto):
  Se a caixa de texto desta caixa de diálogo for inexistente, retorne.
  Ative o detector de rolagem horizontal desta caixa de texto desta caixa de diálogo.
  Selecione cada byte na caixa de texto desta caixa de diálogo.
  Atribua a cor da canetinha desta caixa de diálogo à cor da canetinha desta caixa de texto desta caixa de diálogo.
  Atribua a coordenada X2 desta caixa de alerta desta caixa de diálogo ao coordenada X1 duma caixa.
  Atribua a coordenada Y1 desta caixa de alerta desta caixa de diálogo à coordenada Y1 desta caixa.
  Atribua a coordenada X2 desta caixa arredondada desta caixa de diálogo menos 12 pixels à coordenada X2 desta caixa.
  Atribua a coordenada Y2 desta caixa de alerta desta caixa de diálogo à coordenada Y2 desta caixa.
  Mude a caixa de texto desta caixa de diálogo usando a caixa.

Rotina para que se ajuste um menu usando uma localização e uma largura:
  Se o menu for inexistente, retorne.
  Preserve a localização.
  Atribua a coordenada X desta localização mais a largura para uma coordenada X2.
  Itere. [o código a seguir]
    Obtenha um comando desde os comandos deste menu.
    Se o comando for inexistente, pare.
    Se a coordenada X desta localização mais a largura deste comando for maior do que a coordenada X2, 
      Atribua a coordenada X desta original localização à coordenada X desta localização; 
      Adicione a altura do botão do painel para a coordenada Y desta localização.
    Atribua a localização e a localização à caixa arredondada deste comando.
    Adicione a largura deste comando e a altura do botão do painel para o canto inferior direito desta caixa arredondada deste comando.
    Adicione a largura deste comando para a coordenada X desta localização.
  Reitere. [Execute novamente o código]

Rotina para que se ajuste umas abas:
  Se a contagem destas abas for 0, retorne.
  Atribua a largura desta caixa deste alternador dividido pela contagem destas abas para uma largura.
  Arredonde a largura para a quantidade de twips/pixel .
  Itere. [o código a seguir]
    Obtenha uma aba desde as abas.
    Se a aba for inexistente, retorne.
    Atribua a caixa deste alternador à caixa desta aba.
    Adicione 24 pixels para a coordenada Y1 desta aba.
    Se a aba não for a primeira aba destas abas, atribua a coordenada X2 desta precedente aba desta aba à coordenada X1 desta aba.
    Se a aba não for a última aba destas abas, atribua a coordenada X1 desta aba mais a largura à coordenada X2 desta aba.
  Reitere. [Execute novamente o código]

Um alternative é uma string.

O texto do campo é uma string.

Rotina para que se pergunte uma questão:
  Pergunte a questão com "".

Rotina para que se pergunte uma questão com as alternativas sendo uma string e uma segunda string:
  Pergunte a questão com as alternativas sendo a string e a segunda string e "".

Rotina para que se pergunte uma questão com as alternativas sendo uma string e uma segunda string e uma terceira string:
  Crie a caixa de diálogo.
  Atribua "botão" à categoria desta caixa de diálogo.
  Atribua a questão ao texto de aviso desta caixa de diálogo.
  Adicione um botão para a caixa de diálogo usando a string.
  Adicione um segundo botão para a caixa de diálogo usando a segunda string.
  Adicione um terceiro botão para a caixa de diálogo usando a terceira string.
  Execute a caixa de diálogo.
  Destrua a caixa de diálogo.

Rotina para que se pergunte uma questão com um default número:
  Converta o default número para uma string.
  Pergunte a questão com a string.

Rotina para que se pergunte uma questão com um default proporção:
  Converta a default proporção para uma string.
  Pergunte a questão com a string.

Rotina para que se pergunte uma questão com um default string:
  Crie a caixa de diálogo.
  Atribua "input" à categoria desta caixa de diálogo.
  Atribua a questão ao texto de aviso desta caixa de diálogo.
  Adicione um botão para a caixa de diálogo usando "Entendido".
  Adicione um segundo botão para a caixa de diálogo usando "Cancelar".
  Crie a caixa de texto desta caixa de diálogo.
  Atribua o default ao caixa de texto desta caixa de diálogo.
  Execute a caixa de diálogo.
  Destrua a caixa de diálogo.

Uma caixa de exibição é uma caixa.

O painel superior direito é uma estrutura com
  Uma altura,
  Uma caixa,
  Uma caixa de exibição e
  Um modo do programa.

Um botão é uma lista com
  Uma caixa arredondada e
  Um nome.

A largura do botão do painel é 96 pixels. \ era 64. São os botões de diálogo, tais como "Sim", "Não", "Cancelar", etc.

Rotina para que se feche a caixa de diálogo:
  Se a caixa de diálogo for inexistente, retorne.
  Se a caixa de texto desta caixa de diálogo for inexistente, atribua "cancelar" ao texto do campo.
  Se a caixa de texto desta caixa de diálogo não for inexistente, atribua "" ao texto do campo.
  Renuncie ao controle.

O altura do botão do painel é uma altura igual a 32 pixels.

Um comando é uma lista com
  Uma caixa arredondada,
  Uma largura,
  Uma cor,
  Uma fonte,
  Um alinhamento,
  Um nome,
  Um atalho de tecla, e
  Um rótulo.

Rotina para que se crie um botão:
  Aloque memória para o botão.

Rotina para que se crie um comando: \ aqui cria os menus
  Aloque memória para o comando.
  Atribua a cor transparente à cor deste comando. \
  Atribua a fonte padrão à fonte deste comando.
  Atribua 0 pixels ao raio desta caixa arredondada deste comando. \ muda o raio dos botões
  Atribua "à esquerda" ao alinhamento deste comando.

Rotina para que se crie a caixa de diálogo:
  Aloque memória para a caixa de diálogo.
  \Atribua a cor preta à cor da canetinha desta caixa de diálogo.

Rotina para que se crie um menu:
  Aloque memória para o menu.
  Atribua a letra 'A' maiúscula para um byte.
  Itere. [o código a seguir]
    Se o byte for maior do que o Z maiúsculo, pare.
    Atribua o byte para uma string.
    Adicione a string e a string ao menu.
    Atribua "centralizado" ao alinhamento deste último comando destes comandos deste menu.
    Adicione 1 para o byte.
  Reitere. [Execute novamente o código]
  Adicione "*" e "*" para o menu. \ seletor de cores
  Atribua "centralizado" ao alinhamento deste último comando destes comandos deste menu. \ seletor de cores
  Atribua o canto superior esquerdo desta caixa de exibição deste painel superior direito para uma localização. \ seletor de cores
  \ ajuste o menu usando a localização e 26 vezes a largura do botão da paleta. \ no special menu
  Ajuste o menu usando a localização e 27 vezes a largura do botão da paleta. \ special menu

\ O número identificador do menu global é um número.
\ Uma ordem numérica é um número
\ Uma configuração de menu é um número.

\Rotina para que se crie um menu usando um número identificador de menu:
  \Processe "user32.dll" "CreateMenu" retornando um número.
  \ Se o número for 0, apresente uma mensagem de erro contendo "Falha ao criar o menu", Retorne.
  \Atribua o número ao número identificador de menu.

\Rotina para que se insira um menu usando um número identificador e uma string e uma ordem numérica e uma configuração de menu:
  \ Anexe o caractere NULL após a string.
  \Processe "user32.dll" "AppendMenu" com \ pode-se utilizar as funções InsertMenu (deprecated) ou InsertMenuItem, para maiores opções de personalização
  \  O número identificador,
  \  A configuração de menu, [0x00000000L = MF_STRING MF_ENABLED]
  \  A ordem numérica
  \  e A string.

\Rotina para que se anexe o menu global para a janela do programa:
\Processe "user32.dll" "SetMenu" com
\  A janela principal do programa,
\  o Número identificador do menu global.

Rotina para que se crie um submenu:
  Aloque memória para o submenu.

Rotina para que se crie uma aba:
  Aloque memória para a aba.

O menu atual é um menu.

O processo atual é um process.

A aba atual é uma aba.

Rotina para que se determine se um evento é ctrl-tab:
  Se o evento for inexistente, diga não.
  Se a categoria deste evento não for "pressionamento de tecla", diga não.
  Se a tecla deste evento não for a tecla Tab, diga não.
  Se o detector de tecla Ctrl deste evento não estiver ativo, diga não.
[Se as condições acima não puderem ser satisfeitas, então:] Diga sim.

Rotina para que se determine se uma listagem deveria ser somente leitura:
  Se a listagem for inexistente, diga não.
  Se a categoria desta listagem não for "arquivo", diga não.
  Itere. [o código a seguir]
    Obtenha uma aba desde a abas.
    Se a aba for inexistente, diga não.
    Se a listagem desta aba for inexistente, reitere.
    Se a listagem desta aba for a listagem, reitere.
    Se a categoria desta listagem desta aba não for "arquivo", reitere.
    Se o endereço completo desta listagem desta aba é o endereço completo desta listagem, diga sim.
  Reitere. [Execute novamente o código]

Rotina para que se determine se uma localização está na painel superior direito:
  Se a localização estiver na caixa deste painel superior direito, diga sim.
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se determine se uma localização está em um botão:
  Se o botão for inexistente, diga não.
  Se a localização estiver na caixa arredondada deste botão, diga sim.
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se determine se uma localização está em um comando:
  Se o comando for inexistente, diga não.
  Se a localização estiver na caixa arredondada deste comando, diga sim.
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se determine se uma localização está em um menu:
  Se o menu for inexistente, diga não.
  Encontre um comando usando o menu e a localização.
  Se o comando for inexistente, diga não.
[Se as condições acima não puderem ser satisfeitas, então:] Diga sim.

Rotina para que se determine se uma localização está no pad:
  Se a localização estiver na caixa deste pad, diga sim.
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se determine se uma localização está no painel de informações:
  Se o modo do programa deste painel superior direito não for "normal", diga não.
  Se a localização estiver na caixa arredondada deste painel de informações, diga sim.
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se determine se uma localização está no alternador:
  Encontre uma aba usando a localização.
  Se a aba for inexistente, diga não.
[Se as condições acima não puderem ser satisfeitas, então:] Diga sim.

Rotina para que se determine se uma localização está em uma aba:
  Se a aba for inexistente, diga não.
  Se a localização estiver na caixa desta aba, diga sim.
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se determine se uma aba está disponível:
  Se a aba for inexistente, diga não.
  Se a listagem desta aba for inexistente, diga não.
  Se a categoria desta listagem desta aba não for "listagem de unidades", diga não.
[Se as condições acima não puderem ser satisfeitas, então:] Diga sim.

Rotina para que se determine se o usuário decidiu-se;
Rotina para que se determine se o usuário está decidido:
  Pergunte "Confirma operação?" com as alternativas sendo "Sim" e "Não".
  Se o texto do campo for "Sim", diga sim.
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se determine se o usuário está pronto pra fechar uma listagem:
  Se a listagem for inexistente, diga sim.
  Se a listagem não foi alterado, diga sim.
  Pergunte "Salvar?" com as alternativas sendo "Sim" e "Não" e "Cancelar".
  Se o texto do campo for "Cancelar", diga não.
  Se o texto do campo for "Não", diga sim.
  Salve a listagem.
  Se o erro do fluxo de entrada/saída não estiver em branco,
     mostre erro o erro do fluxo de entrada/saída;
     diga não.
[Se as condições acima não puderem ser satisfeitas, então:] Diga sim.

A cor do ambiente de trabalho é uma cor.

Rotina para que se destrua uma aba:
  Se a aba for inexistente, retorne.
  Destrua o root listagem desta listagem desta aba.
  Desaloque a aba.

O caixa de diálogo é uma lista com
  Uma categoria [escolha, erro, input],
  Uma caixa arredondada,
  Uma caixa de alerta,
  Um texto de aviso,
  Uma cor da canetinha,
  Uma caixa de texto,
  Uns botões.

Rotina para que se pinte o painel superior direito: \
  Exponha tudo.
  Pinte a caixa deste painel superior direito com a cor preta [borda] e a cor do ambiente de trabalho [preenchimento]. \era branca
  Pinte a linha de cima desta caixa deste painel superior direito com a cor cinza escuro.
  Pinte a linha reta do lado esquerdo desta caixa deste painel superior direito com a cor cinza escuro.
  Pinte a linha reta do lado direito desta caixa deste painel superior direito com a cor branca.
  Pinte a linha de baixo desta caixa deste painel superior direito com a cor preta. \era branca
  \Pinte a caixa deste painel superior direito com a cor do ambiente de trabalho e a cor do ambiente de trabalho e 1/8 polegada (arredondando o topo).
  Se o modo do programa deste painel superior direito for "caixa de diálogo",
     pinte a caixa de diálogo;
     Retorne.
  Pinte o menu.
  Pinte o painel de informações.

Rotina para que se pinte uma caixa com uma cor da borda e uma cor de preenchimento e um raio (arredondando a base):
  Atribua a caixa e o raio para uma caixa arredondada.
  Pinte a caixa arredondada com a cor da borda e a cor de preenchimento. \
  Preserve a caixa.
  Atribua a altura desta caixa dividido por 2 para uma altura.
  Atribua a coordenada Y1 desta caixa mais a altura à coordenada Y2 desta caixa.
  Pinte a caixa com a cor da borda e a cor transparente. \
  Adicione a quantidade de twips/pixel para a coordenada Y2 desta caixa.
  Pinte a caixa com a cor transparente e a cor de preenchimento. \

Rotina para que se pinte uma caixa com uma cor da borda e uma cor de preenchimento e um raio (arredondando o topo):
  Atribua a caixa e o raio para uma caixa arredondada.
  Pinte a caixa arredondada com a cor da borda e a cor de preenchimento. \
  Preserve a caixa.
  Atribua a altura desta caixa dividido por 2 para uma altura.
  Atribua a coordenada Y2 desta caixa menos a altura à coordenada Y1 desta caixa.
  Pinte a caixa com a cor da borda e a cor transparente. \ TRANSPARENTE?
  Subtraia a quantidade de twips/pixel desde a coordenada Y1 desta caixa.
  Pinte a caixa com a cor transparente e a cor de preenchimento. \está igual ao SAL

Rotina para que se pinte um botão: \específico para os botões sim/não, cancelar, etc. Não vale para os menus.
  Se o botão for inexistente, retorne.
  Exponha tudo.
  Pinte a caixa arredondada deste botão com a cor branca [borda] e a cor de realce [preenchimento].
  Cubra externamente a caixa arredondada deste botão.
  Pinte o nome deste botão na caixa arredondada deste botão usando a cor branca [cor da fonte do botão realçado] e a fonte padrão e "centralizado".

Rotina para que se pinte uns botões:
  Obtenha um botão desde o botões.
  Se o botão for inexistente, retorne.
  Pinte o botão.
  Reitere. [Execute novamente o código]

Rotina para que se pinte um comando:
  \Escreva " - pinte um comando" para "c:\0\log.txt".
  Se o comando for inexistente, retorne.
  Exponha tudo.
  Atribua a cor deste comando para uma cor.
  Se a cor deste comando for a cor transparente, atribua a cor do ambiente de trabalho à cor. \ aqui muda a cor dos botões do menu
  Pinte a caixa arredondada deste comando com a cor branca e a cor. \ aqui muda a cor dos botões do menu e também do seletor de cores...
  Pinte a linha de cima desta caixa arredondada deste comando com a cor preta.
  Pinte a linha reta do lado esquerdo desta caixa arredondada deste comando com a cor preta.
  Pinte a linha reta do lado direito desta caixa arredondada deste comando com a cor preta.
  Pinte a linha de baixo desta caixa arredondada deste comando com a cor preta.
  Atribua a caixa arredondada deste comando para uma caixa.
  Ajuste a caixa usando 12 pixels [coordenada X1 ] e 0 pixels [coordenada Y1] e -12 pixels [coordenada X2] e 0 pixels [coordenada Y2]. \ ajusta a posição das letras nos botões
  Cubra externamente a caixa arredondada deste comando. \ não pode cobrir internamente a caixa
  Pinte o nome deste comando na caixa com a cor preta e a fonte deste comando e o alinhamento deste comando. \ muda a cor da letra do menu
  Pinte o atalho de tecla deste comando sob o lado direito de a caixa com a cor preta e a fonte deste comando. \ muda a cor da fonte do menu
  Se o usuário clicou no comando, realce o comando.
  \Se o usuário não clicou no comando, desafunde o comando.


  \Rotina para que se execute um evento usando uma caixa (exibir cursor):
  \Atribua a cor branca para uma cor.
  \Se a localização deste evento estiver na caixa, atribua a cor verde à cor.
  \Pinte e preencha a caixa com a cor.
  \Atualize a tela.

Rotina para que se ressalte um comando:
  \Atribua a caixa arredondada deste comando para uma caixa.
  \Cubra externamente a caixa arredondada deste comando.
  Pinte a linha de cima desta caixa arredondada deste comando com a cor branca.
  Pinte a linha reta do lado esquerdo desta caixa arredondada deste comando com a cor branca.
  Pinte a linha reta do lado direito desta caixa arredondada deste comando com a cor preta.
  Pinte a linha de baixo desta caixa arredondada deste comando com a cor preta.


Rotina para que se afunde um comando:
  Atribua a caixa arredondada deste comando para uma caixa.
  Cubra externamente a caixa arredondada deste comando.
  Pinte e preencha a caixa arredondada deste comando com a cor branca.
  Pinte o nome deste comando na caixa com a cor verde e a fonte deste comando e o alinhamento deste comando.
  Pinte a linha de cima desta caixa arredondada deste comando com a cor cinza.
  Pinte a linha reta do lado esquerdo desta caixa arredondada deste comando com a cor cinza.
  Pinte a linha reta do lado direito desta caixa arredondada deste comando com a cor branca.
  Pinte a linha de baixo desta caixa arredondada deste comando com a cor branca.
  Aguarde por um evento.
  Desafunde o comando.

Rotina para que se desafunde um comando:
  Atribua a caixa arredondada deste comando para uma caixa.
  Cubra externamente a caixa arredondada deste comando.
  Pinte o nome deste comando na caixa com a cor preta e a fonte deste comando e o alinhamento deste comando.
  \Pinte e preencha a caixa arredondada deste comando com a cor do ambiente de trabalho.
  Pinte a linha de cima desta caixa arredondada deste comando com a cor preta.
  Pinte a linha reta do lado esquerdo desta caixa arredondada deste comando com a cor preta.
  Pinte a linha reta do lado direito desta caixa arredondada deste comando com a cor preta.
  Pinte a linha de baixo desta caixa arredondada deste comando com a cor preta.


Rotina para que se desressalte um comando:
  Atribua a caixa arredondada deste comando para uma caixa.
  Cubra externamente a caixa arredondada deste comando.
  Pinte a linha de cima desta caixa arredondada deste comando com a cor do ambiente de trabalho.
  Pinte a linha reta do lado esquerdo desta caixa arredondada deste comando com a cor do ambiente de trabalho.
  Pinte a linha reta do lado direito desta caixa arredondada deste comando com a cor do ambiente de trabalho.
  Pinte a linha de baixo desta caixa arredondada deste comando com a cor do ambiente de trabalho.

Rotina para que se realce um comando:
  \Desressalce os outros comandos.
  \Atribua a caixa arredondada deste comando para uma caixa.
  Atribua a cor de realce à cor deste comando.
  Cubra externamente a caixa arredondada deste comando.
  Atribua a caixa arredondada deste comando para uma caixa.
  Pinte e preencha a caixa com a cor de realce.
  Pinte o nome deste comando na caixa arredondada deste comando com a cor branca e a fonte deste comando e o alinhamento deste comando. \
  Aguarde por um evento.
  Desrealce o comando.

Rotina para que se desrealce um comando:
  Atribua a caixa arredondada deste comando para uma caixa.
  Cubra externamente a caixa arredondada deste comando.
  Atribua a cor do ambiente de trabalho à cor deste comando.


Rotina para que se decida se o mouse está em um comando:
  Atribua -1 pixel para um número.
  Atribua -1 pixel para um segundo número.
  Itere. [o código a seguir]
    Adicione o número e o segundo número à localização atual.
    Atribua a localização desta seta do mouse para uma localização.
  \Se a localização deste mouse estiver sobre a caixa arredondada deste comando, diga não.
    Se a localização desta seta do mouse estiver na caixa arredondada deste comando, diga sim.
    Se a localização desta seta do mouse estiver debaixo de a caixa arredondada deste comando, diga não.
    Se a localização desta seta do mouse estiver no lado esquerdo ou no lado direito de a caixa arredondada deste comando, diga não.
  \Se a localização deste mouse estiver lado direito de_ a caixa arredondada deste comando, diga não.
  \Se a localização deste mouse estiver dentre 1/96 a caixa arredondada deste comando, diga sim.
    Se a localização estiver neste comando, diga sim.
  Reitere. [Execute novamente o código]
[Se as condições acima não puderem ser satisfeitas, então:] Diga não.

Rotina para que se pinte uns comandos:
  Obtenha um comando desde o comandos.
  Se o comando for inexistente, retorne.
  Pinte o comando.
  Reitere. [Execute novamente o código]

Rotina para que se pinte o ambiente de trabalho:
  Pinte o painel superior direito.
  Pinte o alternador.
  Pinte o pad.
  Pinte o menu atual.

Rotina para que se pinte a caixa de diálogo:
  Se a caixa de diálogo for inexistente, retorne.
  Exponha tudo.
  Pinte a caixa arredondada desta caixa de diálogo com a cor preta [borda] e a cor branca [preenchimento].
  Pinte a linha de cima desta caixa arredondada desta caixa de diálogo com a cor cinza muito escuro.
  Pinte a linha reta do lado esquerdo desta caixa arredondada desta caixa de diálogo com a cor cinza muito escuro.
  Pinte a linha reta do lado direito desta caixa arredondada desta caixa de diálogo com a cor branca.
  Pinte a linha de baixo desta caixa arredondada desta caixa de diálogo com a cor branca.
  Cubra externamente a caixa arredondada desta caixa de diálogo.
  Pinte o texto de aviso desta caixa de diálogo na caixa de alerta desta caixa de diálogo com a cor da canetinha desta caixa de diálogo e a fonte padrão e "à esquerda".
  Pinte a caixa de texto desta caixa de diálogo (editing).
  Pinte os botões desta caixa de diálogo.

Rotina para que se pinte um menu:
  Se o menu for inexistente, retorne.
  Pinte os comandos deste menu.

Rotina para que se pinte o pad:
  Cubra somente fora de a caixa deste pad.
  Pinte a caixa deste pad com a cor preta [borda] e a cor branca [preenchimento]. \aqui muda a cor do editor de texto
  Se o modo do programa desta aba atual for "explorador de arquivos",
     pinte a listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     pinte o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     pinte o documento desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     pinte a página desta listagem desta aba atual;
     Retorne.

Rotina para que se pinte o painel de informações:
  Exponha tudo.
  \Pinte a caixa arredondada deste painel de informações com a cor do ambiente de trabalho e a cor do ambiente de trabalho.
  Pinte a caixa arredondada deste painel de informações com a cor preta [borda] e a cor branca [preenchimento].
  Pinte a linha de cima desta caixa arredondada deste painel de informações  com a cor cinza escuro.
  Pinte a linha reta do lado esquerdo desta caixa arredondada deste painel de informações  com a cor cinza escuro.
  Pinte a linha reta do lado direito desta caixa arredondada deste painel de informações  com a cor branca.
  Pinte a linha de baixo desta caixa arredondada deste painel de informações  com a cor branca.
  Atribua a caixa arredondada deste painel de informações para uma caixa.
  Ajuste a caixa usando 12 pixels e 0 pixels e -12 pixels e 0 pixels.
  Cubra externamente a caixa.
  Pinte a string deste painel de informações na caixa com a cor preta e a fonte padrão e "à direita".

Rotina para que se pinte o alternador: \ os alternadores são as "bandeirinhas" de baixo
  Cubra somente fora de a caixa deste alternador.
  Pinte a caixa deste alternador com a cor do ambiente de trabalho [borda] e a cor do ambiente de trabalho [preenchimento]. \ aqui muda a cor das bandeirinhas
  Atribua a caixa deste alternador para uma caixa.
  Subtraia 32 pixels desde a coordenada Y2 desta caixa.
  Pinte a caixa com a cor do ambiente de trabalho [borda] e a cor do ambiente de trabalho [preenchimento]. \ aqui é a cor da área retangular acima das abas
  \Pinte a linha de baixo desta caixa com a cor cinza.
  Pinte as abas.

Rotina para que se pinte uma aba:
  Se a aba for inexistente, retorne.
  Cubra somente fora de a caixa deste aba.
  Pinte a caixa deste aba com a cor do ambiente de trabalho [borda] e a cor do ambiente de trabalho [preenchimento] . \e 1/16 polegada (arredondando a base). \ aqui muda a cor das abas
  Pinte a caixa deste aba atual com a cor do ambiente de trabalho [borda] e a cor de realce [preenchimento] . \e 1/16 polegada (arredondando a base). \ e aqui da aba em uso
  Pinte a linha reta do lado direito desta caixa desta aba atual com a cor branca.
  Pinte a linha reta do lado esquerdo desta caixa desta aba atual com a cor cinza.
  Pinte a linha de cima desta caixa desta aba atual com a cor branca.
  Pinte a linha de baixo desta caixa desta aba atual com a cor cinza.
  Se o precedente aba desta aba não for inexistente, pinte o linha reta do lado esquerdo desta caixa desta aba com a cor cinza.
  Se a aba não for a aba atual, pinte o linha de cima desta caixa desta aba com a cor branca.
  Se a aba for a aba atual, pinte o nome desta aba no centro de a caixa desta aba com a cor branca e a fonte padrão.
  Se a aba não for a aba atual, pinte o nome desta aba no centro de a caixa desta aba com a cor preta e a fonte padrão.

Rotina para que se pinte umas abas:
  Obtenha uma aba desde as abas.
  Se a aba for inexistente, retorne.
  Pinte a aba.
  Reitere. [Execute novamente o código]

Rotina para que se preencha uns abas:
  Obtenha uma aba desde as abas.
  Se a aba for inexistente, retorne.
  Atribua "explorador de arquivos" ao modo do programa deste aba.
  Crie a listagem desta aba.
  Abra a listagem desta aba.
  Reitere. [Execute novamente o código]

Rotina para que se finalize:
  Destrua as abas.
  Finalize o compilador.
  Finalize o caderno.
  \Finalize o editor.
  \Finalize o explorador de arquivos.
  Finalize o ambiente de trabalho.

Rotina para que se finalize o painel superior direito:
  \Finalize o painel de informações.
  Destrua o menu.

Rotina para que se finalize o ambiente de trabalho:
  \Finalize o alternador.
  \Finalize o pad.
  Finalize o painel superior direito.

\Rotina para que se finalize o pad:

\Rotina para que se finalize o painel de informações:

\Rotina para que se finalize o alternador:

Rotina para que se encontre um botão usando uma tecla:
  Esvazie o botão.
  Se a caixa de diálogo for inexistente, retorne.
  Itere. [o código a seguir]
    Obtenha o botão desde os botões desta caixa de diálogo.
    Se o botão for inexistente, retorne.
    Se o nome deste botão estiver em branco, reitere.
    Atribua o conteúdo deste caractere inicial deste nome deste botão para um byte.
    Uppercase o byte.
    Se o byte for a tecla, retorne.
  Reitere. [Execute novamente o código]

Rotina para que se encontre um botão usando uma localização:
  Esvazie o botão.
  Se a caixa de diálogo for inexistente, retorne.
  Itere. [o código a seguir]
    Obtenha o botão desde os botões desta caixa de diálogo.
    Se o botão for inexistente, retorne.
    Se a localização estiver no botão, retorne.
  Reitere. [Execute novamente o código]

Rotina para que se encontre um comando usando um menu e uma localização:
  Esvazie o comando.
  Se o menu for inexistente, retorne.
  Itere. [o código a seguir]
    Obtenha o comando desde os comandos deste menu.
    Se o comando for inexistente, retorne.
    Se a localização estiver no comando, retorne.
  Reitere. [Execute novamente o código]

Rotina para que se encontre um endereço da pasta para ser compilada:
  Limpe o endereço da pasta.
  Se a listagem desta aba atual for inexistente, retorne.
  Se o source desta listagem desta aba atual for inexistente, retorne.
  Atribua o endereço da pasta desta listagem desta aba atual ao endereço da pasta.

Rotina para que se encontre um aba disponível começando com uma aba:
  Preserve a aba.
  Itere. [o código a seguir]
    Se a aba for inexistente, pare.
    Se a aba estiver disponível, pare.
    Atribua a próxima aba desta aba à aba.
  Reitere. [Execute novamente o código]
  Atribua a aba ao aba disponível.

O modo de localização de texto é uma string.
 \ A localização de texto possui os seguintes modos: 
 \ localização de texto comum, 
 \ verificação ortográfica comum, 
 \ verificação ortográfica - em todas as páginas do documento, 
 \ verificação ortográfica - apenas na página atual do documento

Rotina para que se encontre uma aba usando uma localização:
  Esvazie a aba.
  Itere. [o código a seguir]
    Obtenha a aba desde as abas.
    Se a aba for inexistente, retorne.
    Se a localização estiver no aba, retorne.
  Reitere. [Execute novamente o código]

Rotina para que se format uma string para fit em uma caixa usando uma fonte:
  Atribua a largura desta caixa menos 180 para uma largura.
  Itere. [o código a seguir]
    Se a quantidade de caracteres desta string for menor do que 2, retorne.
    Obtenha uma segunda largura usando a string e o contexto de dispositivo de memória e a fonte.
    Se a segunda largura for menor do que a largura, retorne.
    Remova o último caractere desde a string.
    Remova o último caractere desde a string.
    Remova o último caractere desde a string.
    Remova o último caractere desde a string.
    Acrescente "..." para a string.
  Reitere. [Execute novamente o código]

Uma aba disponível é uma aba.

Rotina para que se mude alinhamento usando um alinhamento:
  Se o modo do programa desta aba atual for "editor de documentos",
     mude alinhamento usando a página desta listagem desta aba atual e o alinhamento;
     Retorne.
  Avise.

Rotina para que se mude a cor da borda e a cor do preenchimento usando uma cor:
  \ special menu
  Se o modo do programa desta aba atual for "editor de documentos",
     mude a cor da borda e a cor do preenchimento usando a página desta listagem desta aba atual e a cor;
     Retorne.
  Avise.

Rotina para que se mude a cor da borda usando uma cor:
  Se o modo do programa desta aba atual for "editor de documentos",
     mude a cor da borda usando a página desta listagem desta aba atual e a cor;
     Retorne.
  Avise.

Rotina para que se envie para frente:
  Se o modo do programa desta aba atual for "editor de documentos",
     envie para frente usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute capitalize:
  Se o modo do programa desta aba atual for "editor de texto",
     execute capitalize usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     execute capitalize usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute verificação ortográfica:
  Se o modo do programa desta aba atual for "editor de texto",
     execute verificação ortográfica usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute verificação ortográfica (só na página atual):
  Se o modo do programa desta aba atual for "editor de documentos",
     execute verificação ortográfica usando a página desta listagem desta aba atual (somente na página atual);
     Retorne.
  Avise.

Rotina para que se execute verificação ortográfica (em todo o documento):
  Se o modo do programa desta aba atual for "editor de documentos",
     execute verificação ortográfica usando a página desta listagem desta aba atual (entire documento);
     Retorne.
  Avise.

Rotina para que se execute encerramento:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute encerramento (caderno);
     Retorne.
  Execute encerramento (demais cláusulas).

Rotina para que se execute encerramento (demais cláusulas):
  Se a listagem desta aba atual é raiz,
     avise o usuário;
     desvencilhe-se de os elementos na listagem desta aba atual; mostre o ambiente de trabalho com painel de informações; Retorne.
  Se o usuário não estiver pronto pra fechar a listagem desta aba atual, retorne.
  Mostre no painel de informações "Fechando...".
  Feche a listagem desta aba atual.
  Alterne a aba atual para o parent listagem desta listagem desta aba atual.
  Mostre o ambiente de trabalho com painel de informações.
  Atualize o cursor.

Rotina para que se execute encerramento (caderno):
  Execute encerramento sem drawing (caderno).
  Mostre o ambiente de trabalho com painel de informações.
  Atualize o cursor.

Rotina para que se encerre todos os itens:
  Mostre no painel de informações "Fechando...".
  Obtenha uma aba desde as abas (regressivamente).
  Se a aba for inexistente, pare.
  Alterne para a aba.
  Se a listagem desta aba foi alterada, mostre o ambiente de trabalho.
  Se o usuário não estiver pronto pra fechar a listagem desta aba, pare.
  Recolha a listagem desta aba para a raiz.
  Desvencilhe-se de os elementos na listagem desta aba.
  Alterne a aba para a listagem desta aba.
  Reitere. [Execute novamente o código]
  Mostre o ambiente de trabalho com painel de informações.

Rotina para que se execute encerramento sem drawing (caderno):
  Feche a página desta listagem desta aba atual.
  Esvazie a página desta listagem desta aba atual.
  Alterne a aba atual para a listagem desta aba atual.

Rotina para que se execute um comando:
  Se o comando for inexistente, retorne.
  \ offer it dispatchers
  Se o rótulo deste comando for "a",
     execute o comando (a);
     Retorne.
  Se o rótulo deste comando for "alinhamento...",
     execute o comando (alinhamento...);
     Retorne.
  Se o rótulo deste comando for "b",
     execute o comando (b);
     Retorne.
  Se o rótulo deste comando for "borda...",
     execute o comando (borda...);
     Retorne.
  Se o rótulo deste comando for "c",
     execute o comando (c);
     Retorne.
  Se o rótulo deste comando for "verificação ortográfica...",
     execute o comando (verificação ortográfica...);
     Retorne.
  Se o rótulo deste comando for "color...",
     execute o comando (color...);
     Retorne.
  Se o rótulo deste comando for "d",
     execute o comando (d);
     Retorne.
  Se o rótulo deste comando for "e",
     execute o comando (e);
     Retorne.
  Se o rótulo deste comando for "f",
     execute o comando (f);
     Retorne.
  Se o rótulo deste comando for "Preencher...",
     execute o comando (Preencher...);
     Retorne.
  Se o rótulo deste comando for "fonte...",
     execute o comando (fonte...);
     Retorne.
  Se o rótulo deste comando for "altura da fonte...",
     execute o comando (altura da fonte...);
     Retorne.
  Se o rótulo deste comando for "g",
     execute o comando (g);
     Retorne.
  Se o rótulo deste comando for "malha...",
     execute o comando (malha...);
     Retorne.
  Se o rótulo deste comando for "h",
     execute o comando (h);
     Retorne.
  Se o rótulo deste comando for "i",
     execute o comando (i);
     Retorne.
  Se o rótulo deste comando for "j",
     execute o comando (j);
     Retorne.
  Se o rótulo deste comando for "k",
     execute o comando (k);
     Retorne.
  Se o rótulo deste comando for "l",
     execute o comando (l);
     Retorne.
  Se o rótulo deste comando for "label...",
     execute o comando (label...);
     Retorne.
  Se o rótulo deste comando for "m",
     execute o comando (m);
     Retorne.
  Se o rótulo deste comando for "n",
     execute o comando (n);
     Retorne.
  Se o rótulo deste comando for "o",
     execute o comando (o);
     Retorne.
  Se o rótulo deste comando for "p",
     execute o comando (p);
     Retorne.
  Se o rótulo deste comando for "canetinha...",
     execute o comando (canetinha...);
     Retorne.
  Se o rótulo deste comando for "q",
     execute o comando (q);
     Retorne.
  Se o rótulo deste comando for "r",
     execute o comando (r);
     Retorne.
  Se o rótulo deste comando for "s",
     execute o comando (s);
     Retorne.
  Se o rótulo deste comando for "classifique ...",
     execute o comando (classifique ...);
     Retorne.
  Se o rótulo deste comando for "t",
     execute o comando (t);
     Retorne.
  Se o rótulo deste comando for "u",
     execute o comando (u);
     Retorne.
  Se o rótulo deste comando for "v",
     execute o comando (v);
     Retorne.
  Se o rótulo deste comando for "w",
     execute o comando (w);
     Retorne.
  Se o rótulo deste comando for "x",
     execute o comando (x);
     Retorne.
  Se o rótulo deste comando for "y",
     execute o comando (y);
     Retorne.
  Se o rótulo deste comando for "z",
     execute o comando (z);
     Retorne.
  Se o rótulo deste comando for "*",
     execute o comando (*);
     Retorne. \ special menu
  Se o rótulo deste comando for "F4", 
    efetue a saída; 
    Retorne. \ special menu
  \ does it dispatchers
  Mostre o ambiente de trabalho.
  Se o rótulo deste comando for "Enviar pra frente",
     envie para frente;
     Retorne.
  Se o rótulo deste comando for "Centralizar",
     mude alinhamento usando "centralizado";
     Retorne.
  Se o rótulo deste comando for "alinhar à esquerda",
     mude alinhamento usando "à esquerda";
     Retorne.
  Se o rótulo deste comando for "alinhar à direita",
     mude alinhamento usando "à direita";
     Retorne.
  Se o rótulo deste comando for "mude a cor da borda",
     mude a cor da borda usando a cor deste comando;
     Retorne.
  Se o rótulo deste comando for "mude a cor da borda e a cor do preenchimento",
     mude a cor da borda e a cor do preenchimento usando a cor deste comando;
     Retorne. \ special menu
  Se o rótulo deste comando for "preencha a forma geométrica",
     preencha a forma geométrica usando a cor deste comando;
     Retorne.
  Se o rótulo deste comando for "mude o tamanho usando 96 pixels",
     mude o tamanho usando 96 pixels;
     Retorne.
  Se o rótulo deste comando for "mude o tamanho usando 48 pixels",
     mude o tamanho usando 48 pixels;
     Retorne.
  Se o rótulo deste comando for "mude o tamanho usando 32 pixels",
     mude o tamanho usando 32 pixels;
     Retorne.
  Se o rótulo deste comando for "mude o tamanho usando 24 pixels",
     mude o tamanho usando 24 pixels;
     Retorne.
  Se o rótulo deste comando for "mude o tamanho usando 1/5 polegada",
     mude o tamanho usando 1/5 polegada;
     Retorne.
  Se o rótulo deste comando for "mude o tamanho usando 16 pixels",
     mude o tamanho usando 16 pixels;
     Retorne.
  Se o rótulo deste comando for "mude o tamanho usando 12 pixels",
     mude o tamanho usando 12 pixels;
     Retorne.
  Se o rótulo deste comando for "mude o tamanho de outros itens",
     mude o tamanho de outros itens;
     Retorne.
  Se o rótulo deste comando for "mude nome da fonte",
     mude nome da fonte;
     Retorne.
  Se o rótulo deste comando for "modifique o valor do raio",
     modifique o valor do raio;
     Retorne.
  Se o rótulo deste comando for "modifique a cor",
     modifique a cor usando a cor deste comando;
     Retorne.
  Se o rótulo deste comando for "execute capitalize",
     execute capitalize;
     Retorne.
  Se o rótulo deste comando for "execute verificação ortográfica",
     execute verificação ortográfica;
     Retorne.
  Se o rótulo deste comando for "execute verificação ortográfica (em todo o documento)",
     execute verificação ortográfica (em todo o documento);
     Retorne.
  Se o rótulo deste comando for "execute verificação ortográfica (só na página atual)",
     execute verificação ortográfica (só na página atual);
     Retorne.
  Se o rótulo deste comando for "execute encerramento",
     execute encerramento;
     Retorne.
  Se o rótulo deste comando for "encerre todos os itens",
     encerre todos os itens;
     Retorne.
  Se o rótulo deste comando for "execute comment",
     execute comment;
     Retorne.
  Se o rótulo deste comando for "execute compile",
     execute compile;
     Retorne.
  Se o rótulo deste comando for "efetue cópias",
     efetue cópias;
     Retorne.
  Se o rótulo deste comando for "execute cut",
     execute cut;
     Retorne.
  Se o rótulo deste comando for "execute deciframento",
     execute deciframento;
     Retorne.
  Se o rótulo deste comando for "execute duplicate",
     execute duplicate;
     Retorne.
  Se o rótulo deste comando for "execute enlarge",
     execute enlarge;
     Retorne.
  Se o rótulo deste comando for "execute encrypt",
     execute encrypt;
     Retorne.
  Se o rótulo deste comando for "execute extract globals",
     execute extract globals;
     Retorne.
  Se o rótulo deste comando for "transcreva cabeçalhos de rotina",
     transcreva cabeçalhos de rotina;
     Retorne.
  Se o rótulo deste comando for "execute extract tipos",
     execute extract tipos;
     Retorne.
  Se o rótulo deste comando for "execute extract cifra binária literal",
     execute extract cifra binária literal;
     Retorne.
  Se o rótulo deste comando for "encontrar termo",
     tente encontrar o texto;
     Retorne.
  Se o rótulo deste comando for "encontrar próximo termo",
     tente encontrar novamente o texto;
     Retorne.
  Se o rótulo deste comando for "execute flip",
     execute flip;
     Retorne.
  Se o rótulo deste comando for "execute obtenha tamanho",
     execute obtenha tamanho;
     Retorne.
  Se o rótulo deste comando for "execute grayscale",
     execute grayscale usando o detector de ignição;
     Retorne.
  Se o rótulo deste comando for "agrupe as formas geométricas",
     agrupe as formas geométricas;
     Retorne.
  Se o rótulo deste comando for "desative exibição de malha",
     execute malha usando o sinal de desligamento;
     Retorne.
  Se o rótulo deste comando for "ative exibição de malha",
     execute malha usando o detector de ignição;
     Retorne.
  Se o rótulo deste comando for "ajuste o recuo interno",
     ajuste o recuo interno;
     Retorne.
  Se o rótulo deste comando for "execute label usando title",
     execute label usando "title";
     Retorne.
  Se o rótulo deste comando for "execute label usando número de página",
     execute label usando "page número";
     Retorne.
  Se o rótulo deste comando for "execute label usando número de página (par)",
     execute label usando "page número (par)";
     Retorne.
  Se o rótulo deste comando for "execute label usando número de página (ímpar)",
     execute label usando "page número (ímpar)";
     Retorne.
  Se o rótulo deste comando for "crie o arquivo (extensão .lst)",
     crie o arquivo (extensão .lst);
     Retorne.
  Se o rótulo deste comando for "execute lowercase",
     execute lowercase;
     Retorne.
  Se o rótulo deste comando for "minimize a janela",
     minimize a janela;
     Retorne.
  Se o rótulo deste comando for "execute mirror",
     execute mirror;
     Retorne.
  Se o rótulo deste comando for "crie a nova pasta",
     crie a nova pasta;
     Retorne.
  Se o rótulo deste comando for "crie o novo documento",
     crie o novo documento;
     Retorne.
  Se o rótulo deste comando for "execute new elipse",
     crie a nova forma geométrica usando "elipse";
     Retorne.
  Se o rótulo deste comando for "crie a nova página",
     crie a nova página;
     Retorne.
  Se o rótulo deste comando for "execute new imagem",
     crie a nova forma geométrica usando "imagem";
     Retorne.
  Se o rótulo deste comando for "execute new polígono",
     crie a nova forma geométrica usando "polígono";
     Retorne.
  Se o rótulo deste comando for "execute new retângulo",
     crie a nova forma geométrica usando "retângulo";
     Retorne.
  Se o rótulo deste comando for "execute new caixa de texto",
     crie a nova forma geométrica usando "caixa-de-texto";
     Retorne.
  Se o rótulo deste comando for "execute new caixa de texto arquivo",
     execute new caixa de texto arquivo;
     Retorne.
  Se o rótulo deste comando for "abra o item",
     abra o item;
     Retorne.
  Se o rótulo deste comando for "abra o visualizador hexadecimal",
     abra o visualizador hexadecimal;
     Retorne.
  Se o rótulo deste comando for "abra o editor de texto",
     abra o editor de texto;
     Retorne.
  Se o rótulo deste comando for "ajuste o recuo externo",
     ajuste o recuo externo;
     Retorne.
  Se o rótulo deste comando for "cole o item",
     cole o item;
     Retorne.
  Se o rótulo deste comando for "efetue a impressão",
     efetue a impressão;
     Retorne.
  Se o rótulo deste comando for "efetue a saída",
     efetue a saída;
     Retorne.
  Se o rótulo deste comando for "refaça a ação",
     refaça a ação;
     Retorne.
  Se o rótulo deste comando for "execute reduza",
     execute reduza;
     Retorne.
  Se o rótulo deste comando for "renomeie o arquivo",
     renomeie o arquivo;
     Retorne.
  Se o rótulo deste comando for "renumere as páginas",
     renumere as páginas;
     Retorne.
  Se o rótulo deste comando for "reverta a ação",
     reverta a ação;
     Retorne.
  Se o rótulo deste comando for "efetue a rotação da página",
     efetue a rotação da página;
     Retorne.
  Se o rótulo deste comando for "efetue a rotação da forma",
     efetue a rotação da forma;
     Retorne.
  Se o rótulo deste comando for "coordene a execução dos programas",
     coordene a execução dos programas;
     Retorne.
  Se o rótulo deste comando for "salve as alterações",
     salve as alterações;
     Retorne.
  Se o rótulo deste comando for "salve tudo",
     salve tudo;
     Retorne.
  Se o rótulo deste comando for "salve arquivo como pdf",
     salve arquivo como pdf;
     Retorne.
  Se o rótulo deste comando for "selecione todos os itens",
     selecione todos os itens;
     Retorne.
  Se o rótulo deste comando for "envie o item para trás",
     envie o item para trás;
     Retorne.
  Se o rótulo deste comando for "execute measure",
     execute measure;
     Retorne.
  Se o rótulo deste comando for "suavize os polígonos",
     suavize os polígonos;
     Retorne.
  Se o rótulo deste comando for "execute yank usando 1/8 polegada",
     execute yank usando 1/8 polegada;
     Retorne.
  Se o rótulo deste comando for "execute yank usando 8 px",
     execute yank usando 8 px;
     Retorne.
  Se o rótulo deste comando for "execute yank usando 1/16 polegada",
     execute yank usando 1/16 polegada;
     Retorne.
  Se o rótulo deste comando for "execute yank usando 4 px",
     execute yank usando 4 px;
     Retorne.
  Se o rótulo deste comando for "execute yank usando 1/32 polegada",
     execute yank usando 1/32 polegada;
     Retorne.
  Se o rótulo deste comando for "execute yank usando 1/48 polegada",
     execute yank usando 1/48 polegada;
     Retorne.
  Se o rótulo deste comando for "execute yank usando a quantidade de twips/pixel ",
     execute yank usando a quantidade de twips/pixel ;
     Retorne.
  Se o rótulo deste comando for "classifique as definições",
     classifique as definições;
     Retorne.
  Se o rótulo deste comando for "organize as linhas selecionadas no texto",
     organize as linhas selecionadas no texto;
     Retorne.
  Se o rótulo deste comando for "execute classifique elementos por date",
     execute classifique elementos usando "data";
     Retorne.
  Se o rótulo deste comando for "execute classifique elementos por extensão",
     execute classifique elementos usando "extensão";
     Retorne.
  Se o rótulo deste comando for "execute classifique elementos por categoria",
     execute classifique elementos usando "categoria";
     Retorne.
  Se o rótulo deste comando for "execute classifique elementos por nome",
     execute classifique elementos usando "nome";
     Retorne.
  Se o rótulo deste comando for "interrompa a execução do programa",
     interrompa a execução do programa;
     Retorne.
  Se o rótulo deste comando for "efetue remoção de comentários",
     efetue remoção de comentários;
     Retorne.
  Se o rótulo deste comando for "desfaça a ação",
     desfaça a ação;
     Retorne.
  Se o rótulo deste comando for "execute ungrayscale",
     execute grayscale usando o sinal de desligamento;
     Retorne. \antes era usando "no"
  Se o rótulo deste comando for "separe as formas agrupadas",
     separe as formas agrupadas;
     Retorne.
  Se o rótulo deste comando for "execute desmarque",
     execute desmarque;
     Retorne.
  Se o rótulo deste comando for "mude o texto selecionado para maiúsculas",
     mude o texto selecionado para maiúsculas;
     Retorne.
  Se o rótulo deste comando for "exiba a versão do programa no painel de informações",
     exiba a versão do programa no painel de informações;
     Retorne.
  \ not properly dispatched
  Produza o relatório de informações usando "Erro interno nº 763 - este comando ainda não foi implementado".

Rotina para que se execute um comando (*):
  \ special menu
  Crie um submenu.
  Adicione a paleta de cores para o submenu usando "mude a cor da borda e a cor do preenchimento".
  Execute o comando usando o submenu e a largura da paleta de cores.

Rotina para que se execute um comando (a):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Achar uma palavra" e "F" e "encontrar termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Achar uma palavra" e "F" e "encontrar termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Achar novamente" e "N" e "encontrar próximo termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Achar novamente" e "N" e "encontrar próximo termo" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Abrir" e "A" e "abra o item" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Abrir" e "A" e "abra o item" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Abrir como dados brutos" e "abra o visualizador hexadecimal" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Abrir como texto" e "abra o editor de texto" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Agrupar os itens selecionados" e "G" e "agrupe as formas geométricas" para o submenu.
  \Adicione "Ajuda" e "F1" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Alinhar à Direita" e "mude alinhamento usando à direita" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Alinhar à Esquerda" e "mude alinhamento usando à esquerda" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Centralizar" e "mude alinhamento usando centralizado" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Alterar Cor da Canetinha..." e "canetinha..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Alterar Cor do Pincel..." e "canetinha..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Alterar Cor do Texto..." e "canetinha..." para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Alterar Tipo da Fonte..." e "mude nome da fonte" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Alterar Tamanho da Fonte..." e "altura da fonte..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Ampliar Página" e "+" e "execute enlarge" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Aumentar Zoom" e "+" e "execute enlarge" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (alinhamento...):
  Crie um submenu.
  Adicione "Alinhar à Direita" e "mude alinhamento usando à direita" para o submenu.
  Adicione "Alinhar à Esquerda" e "mude alinhamento usando à esquerda" para o submenu.
  Adicione "Centralizar" e "mude alinhamento usando centralizado" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (b):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Borda..." e "borda..." para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (borda...):
  Crie um submenu.
  Adicione a paleta de cores para o submenu usando "mude a cor da borda".
  Execute o comando usando o submenu e a largura da paleta de cores.

Rotina para que se execute um comando (c):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Cor da Canetinha..." e "canetinha..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Cor do Pincel..." e "canetinha..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Cor do Texto..." e "canetinha..." para o submenu.
  Adicione "Centralizar" e "mude alinhamento usando centro" para o submenu.
  Adicione "Colar" e "V" e "cole o item" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Classificar..." e "classifique ..." para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Classificar.." e "classifique ..." para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Comentar" e "\" e "execute comment" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Compilar" e "M" e "execute compile" para o submenu.
  \Adicione "Contatar Autor" e "F3" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Cor..." e "color..." para o submenu.
  Adicione "Copiar" e "C" e "efetue cópias" para o submenu.
  Adicione "Cortar" e "X" e "execute cut" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Criar arquivo lst" e "L" e "crie o arquivo (extensão .lst)" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Criptografar..." e "execute encrypt" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (verificação ortográfica...):
  Crie um submenu.
  Adicione "Apenas essa página" e "execute verificação ortográfica (só na página atual)" para o submenu.
  Adicione "O documentoo todo" e "execute verificação ortográfica (em todo o documento)" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (color...):
  Crie um submenu.
  Adicione "Canetinha..." e "canetinha..." para o submenu.
  Adicione "Borda..." e "borda..." para o submenu.
  Adicione "Preencher..." e "Preencher..." para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (d):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Definir item como..." e "Label..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Desagrupar" e "U" e "separe as formas agrupadas" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Descriptografar..." e "execute deciframento" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Desfazer" e "Z" e "desfaça a ação" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Desfazer" e "Z" e "desfaça a ação" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Desfazer Preto e Branco" e "execute ungrayscale" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Deixar texto em LETRAS MAIÚSCULAS" e "mude o texto selecionado para maiúsculas" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Deixar texto em LETRAS MAIÚSCULAS" e "mude o texto selecionado para maiúsculas" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Deixar texto em letras minúsculas" e "execute lowercase" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Deixar texto em letras minúsculas" e "execute lowercase" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Deixar 1ª letra do texto em Maiúscula" e "execute capitalize" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Deixar 1ª letra do texto Maisúscula" e "execute capitalize" para o submenu.
  Adicione "Documentação" e "F1" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Diminuir Zoom" e "R" e "execute reduza" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Duplicar" e "D" e "execute duplicate" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Duplicar" e "D" e "execute duplicate" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Duplicar" e "D" e "execute duplicate" para o submenu.

  Execute o comando usando o submenu.

Rotina para que se execute um comando (e):
  Crie um submenu.
  Adicione "Encerrar o programa" e "F" e "efetue a saída" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Encontrar uma palavra" e "E" e "encontrar termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Encontrar uma palavra" e "E" e "encontrar termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Encontrar novamente" e "N" e "encontrar próximo termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Encontrar novamente" e "N" e "encontrar próximo termo" para o submenu.
  \\Adicione "Enviar dúvidas por email" e "F3" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Enviar pra frente" e "Enviar pra frente" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Esconder Linhas de Malha" e "desative exibição de malha" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Espelhar" e "execute mirror" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Executar" e "R" e "coordene a execução dos programas" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Executar" e "R" e "coordene a execução dos programas" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Exibir Linhas de Malha" e "ative exibição de malha" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Extrair Tipos" e "execute extract tipos" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Extrair Globais" e "execute extract globals" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Extrair Nomes de Tarefas" e "transcreva cabeçalhos de rotina" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Extrair código hexadecimal do arquivo" e "execute extract cifra binária literal" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (f):
  Crie um submenu.
  Adicione "Fechar este item" e "W" e "execute encerramento" para o submenu.
  Adicione "Fechar todos os itens" e "encerre todos os itens" para o submenu.
  Adicione "Fechar o programa" e "F" e "efetue a saída" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Fonte..." e "fonte..." para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Fonte..." e "fonte..." para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (fonte...):
  Crie um submenu.
  Adicione "Cor..." e "canetinha..." para o submenu.
  Adicione "Tamanho..." e "altura da fonte..." para o submenu.
  Adicione "Tipo..." e "mude nome da fonte" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (Preencher...):
  Crie um submenu.
  Adicione a paleta de cores para o submenu usando "preencha a forma geométrica".
  Execute o comando usando o submenu e a largura da paleta de cores.

Rotina para que se execute um comando (altura da fonte...):
  Crie um submenu.
  Adicione "Gigante" e "mude o tamanho usando 96 pixels" para o submenu.
  Adicione "Maior" e "mude o tamanho usando 48 pixels" para o submenu.
  Adicione "Grande" e "mude o tamanho usando 32 pixels" para o submenu.
  Adicione "Normal" e "mude o tamanho usando 24 pixels" para o submenu.
  Adicione "Pequena" e "mude o tamanho usando 1/5 polegada" para o submenu.
  Adicione "Menor" e "mude o tamanho usando 16 pixels" para o submenu.
  Adicione "Minúsculo" e "mude o tamanho usando 12 pixels" para o submenu.
  Adicione "Outro tamanho y/x..." e "mude o tamanho de outros itens" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (g):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Girar Página" e "efetue a rotação da página" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Girar Forma" e "J" e "efetue a rotação da forma" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Grade..." e "malha..." para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Gravar alterações" e "S" e "salve as alterações" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Gravar alterações" e "S" e "salve as alterações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Gravar alterações" e "S" e "salve as alterações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (malha...):
  Crie um submenu.
  Adicione "Esconder" e "desative exibição de malha" para o submenu.
  Adicione "Mostrar" e "ative exibição de malha" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (h):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (i):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Imprimir..." e "P" e "efetue a impressão" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Imprimir..." e "P" e "efetue a impressão" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Imprimir..." e "P" e "efetue a impressão" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (j):
  Crie um submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (k):
  Crie um submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (l):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Localizar um texto" e "F" e "encontrar termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Localizar um texto" e "F" e "encontrar termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Localizar novamente" e "N" e "encontrar próximo termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Localizar novamente" e "N" e "encontrar próximo termo" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (label...):
  Crie um submenu.
  Adicione "Número da Página" e "execute label usando número de página" para o submenu.
  Adicione "Número da Página (Par)" e "execute label usando número de página (par)" para o submenu.
  Adicione "Número da Página (Ímpar)" e "execute label usando número de página (ímpar)" para o submenu.
  Adicione "Título" e "execute label usando title" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (m):
  Crie um submenu.
  Adicione "Mandar email p/ tirar dúvidas" e "F3" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Mandar pra frente" e "Enviar pra frente" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Mandar pra trás" e "envie o item para trás" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Medir..." e "execute measure" para o submenu.
  Adicione "Minimizar" e "minimize a janela" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Mostar Grade" e "ative exibição de malha" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Mudar Cor da Canetinha..." e "canetinha..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Mudar Cor do Pincel..." e "canetinha..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Mudar Cor do Texto..." e "canetinha..." para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Mudar o Tamanho da Letra..." e "altura da fonte..." para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Mudar o Tipo da Letra..." e "mude nome da fonte" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Mudar o Tamanho da Letra..." e "altura da fonte..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Mudar o Tipo da Letra..." e "mude nome da fonte" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (n):
  Crie um submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nova Pasta..." e "crie a nova pasta" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Novo Documento..." e "crie o novo documento" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Nova Elipse" e "execute new elipse" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nova Página" e "crie a nova página" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Nova Figura..." e "execute new imagem" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Novo Polígono" e "execute new polígono" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Novo Retângulo" e "execute new retângulo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Novo Texto" e "execute new caixa de texto" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Novo Arquivo de Texto..." e "execute new caixa de texto arquivo" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (o):
  Crie um submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Obter Tamanho" e "execute obtenha tamanho" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Ocultar Grade" e "desative exibição de malha" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Ordenar/Organizar..." e "classifique ..." para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Ordenar/Organizar..." e "classifique ..." para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (p):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Parar" e "interrompa a execução do programa" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Parar" e "interrompa a execução do programa" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Preencher..." e "Preencher..." para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Preto e Branco" e "execute grayscale" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Procurar" e "F" e "encontrar termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Procurar" e "F" e "encontrar termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Procurar de Novo" e "N" e "encontrar próximo termo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Procurar de Novo" e "N" e "encontrar próximo termo" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (canetinha...):
  Crie um submenu.
  Adicione a paleta de cores para o submenu usando "modifique a cor".
  Execute o comando usando o submenu e a largura da paleta de cores.

Rotina para que se execute um comando (q):
  Crie um submenu.
  Adicione "Quero sair do programa" e "F" e "efetue a saída" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (r):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Raio..." e "modifique o valor do raio" para o submenu.
  Adicione "Recortar" e "X" e "execute cut" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Recuar texto" e "I" e "ajuste o recuo interno" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Refazer" e "Y" e "refaça a ação" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Refazer" e "Y" e "refaça a ação" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Reduzir Zoom" e "R" e "execute reduza" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Remover Comentário(s)" e "U" e "efetue remoção de comentários" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Remover Espaços Extras" e "O" e "ajuste o recuo externo" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Remover Etiqueta" e "execute desmarque" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Renomear..." e "F2" e "renomeie o arquivo" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Renumerar Páginas..." e "renumere as páginas" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Reverter" e "reverta a ação" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Reverter" e "reverta a ação" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (s):
  Crie um submenu.
  Adicione "Sair do programa" e "F" e "efetue a saída" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Salvar" e "S" e "salve as alterações" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Salvar" e "S" e "salve as alterações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Salvar" e "S" e "salve as alterações" para o submenu.
  Adicione "Salvar Tudo" e "salve tudo" para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Salvar como PDF..." e "salve arquivo como pdf" para o submenu.
  Adicione "Selecionar Tudo" e "T" e "selecione todos os itens" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Suavizar" e "M" e "suavize os polígonos" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (classifique ...):
  Crie um submenu.
  \Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Todas como definições" e "classifique as definições" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Somente como linhas selecionadas" e "organize as linhas selecionadas no texto" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Por tipo" e "execute classifique elementos por categoria" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Por Nome" e "execute classifique elementos por nome" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Por extensão" e "execute classifique elementos por extensão" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (t):
  Crie um submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Trazer pra frente" e "Enviar pra frente" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (u):
  Crie um submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (v):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Verificar Ortografia" e "execute verificação ortográfica" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Verificar Ortografia..." e "verificação ortográfica..." para o submenu.
  Adicione "Versão" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Virar" e "execute flip" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (w):
  Crie um submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (x):
  Crie um submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (y):
  Crie um submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Yank 1/8" e "execute yank usando 1/8 polegada" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Yank 1/12" e "execute yank usando 8 px" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Yank 1/16" e "execute yank usando 1/16 polegada" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Yank 1/24" e "execute yank usando 4 px" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Yank 1/32" e "execute yank usando 1/32 polegada" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Yank 1/48" e "execute yank usando 1/48 polegada" para o submenu.
  Se o modo do programa desta aba atual for "editor de documentos", adicione "Yank None" e "execute yank usando a quantidade de twips/pixel " para o submenu.
  Se o modo do programa desta aba atual for "sumário", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "editor de texto", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Se o modo do programa desta aba atual for "explorador de arquivos", adicione "Nada por aqui" e "exiba a versão do programa no painel de informações" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando (z):
  Crie um submenu.
  Adicione "Zarpar daqui" e "F" e "efetue a saída" para o submenu.
  Execute o comando usando o submenu.

Rotina para que se execute um comando usando um submenu:
  Execute o comando usando o submenu e a largura do submenu.

Rotina para que se execute um comando usando um submenu e uma largura:
  Atribua a coordenada X1 deste comando e a coordenada Y2 desta caixa de exibição deste painel superior direito para uma localização.
  Ajuste o submenu usando a localização mais 1 e a largura.
  Desmanche todos os eventos.
  Esvazie o comando selecionado.
  Atribua o submenu ao menu atual.
  Mostre o ambiente de trabalho.
  Execute alguns eventos usando o submenu.
  Esvazie o menu atual.
  Execute o comando selecionado.
  Destrua o submenu.
  Mostre o ambiente de trabalho.
  Atualize o cursor.

Rotina para que se execute comment:
  Se o modo do programa desta aba atual for "editor de texto",
     execute comment usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute compile:
  Se o modo do programa desta aba atual não for "editor de texto",
     avise o usuário;
     Retorne.
  Salve tudo.
  Se o erro do fluxo de entrada/saída não estiver em branco, retorne.
  Encontre um endereço da pasta para ser compilada.
  Se o endereço da pasta estiver em branco,
     mostre erro "Não tem nada por aqui.";
     Retorne.
  Compile o endereço da pasta.
  Se o detector de erros deste compilador estiver ativo,
     exiba as etapas e erros de compilação;
     Retorne.
  Mostre no painel de informações a string deste temporizador deste compilador seguido de " milissegundos".

Rotina para que se exiba as etapas e erros de compilação:
  Se o endereço do arquivo atual deste compilador estiver em branco,
     mostre erro a mensagem de erro deste compilador;
     Retorne.
  Itere. [o código a seguir]
    Obtenha uma aba desde as abas.
    Se a aba for inexistente,
     mostre erro a mensagem de erro deste compilador;
     Retorne.
    Se o endereço do arquivo atual deste compilador não for o endereço completo desta listagem desta aba, reitere.
    Se o source desta listagem desta aba for inexistente,
     mostre erro a mensagem de erro deste compilador;
     Retorne.
  Selecione o número da linha de erro deste compilador usando a caixa de texto deste source desta listagem desta aba.
  Role a caixa de texto deste source desta listagem desta aba para o cursor de texto e centralize-o.
  Alterne para a aba.
  Mostre o ambiente de trabalho.
  Mostre erro a mensagem de erro deste compilador.

Rotina para que se efetue cópias:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     efetue cópias usando a listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     efetue cópias usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     efetue cópias usando o documento desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     efetue cópias usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute cut:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute cut usando a listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     execute cut usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     execute cut usando o documento desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     execute cut usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute deciframento:
  Se o modo do programa desta aba atual for "editor de texto",
     execute deciframento usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute a caixa de diálogo:
  Se a caixa de diálogo for inexistente, retorne.
  Desmanche todos os eventos.
  Ajuste a caixa de diálogo.
  Alterne o painel superior direito para "caixa de diálogo".
  Execute alguns eventos usando a caixa de diálogo.
  Alterne o painel superior direito para "normal" sem drawing.
  Se o texto do campo estiver em branco, mostre o ambiente de trabalho.
  Se a caixa de texto desta caixa de diálogo for inexistente, mostre o ambiente de trabalho.
  Desmanche todos os eventos.
  Atualize o cursor.

Rotina para que se execute duplicate:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute duplicate usando a listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     execute duplicate usando o documento desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     execute duplicate usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute encrypt:
  Se o modo do programa desta aba atual for "editor de texto",
     execute encrypt usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute enlarge:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute enlarge usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute um evento:
  Se o evento for inexistente, retorne.
  Se a categoria deste evento for "pressionamento de tecla",
     execute o evento (pressionamento de tecla);
     Retorne.
  Se a categoria deste evento for "clique do botão esquerdo",
     execute o evento (clique);
     Retorne.
  Se a categoria deste evento for "clique duplo",
     execute o evento (clique duplo);
     Retorne.
  Se a categoria deste evento for "atualização de tela",
     execute o evento (atualização de tela);
     Retorne.
  Se a categoria deste evento for "clique do botão direito",
     execute o evento (clique direito);
     Retorne.
  Se a categoria deste evento for "movimentação de cursor",
     execute o evento (exibir cursor do mouse);
     Retorne.

Rotina para que se execute um evento (pressionamento de tecla - ctrl-tab):
  Se o detector de tecla Shift deste evento estiver ativo, alterne para o predecessor de_ a aba atual.
  Se o detector de tecla Shift deste evento não estiver ativo, alterne para o successor de_ a aba atual.
  Mostre o ambiente de trabalho com painel de informações.

Rotina para que se execute um evento (pressionamento de tecla - atalho de tecla):
  Se a tecla deste evento for a tecla A/a,
     execute comment;
     Retorne.
  Se a tecla deste evento for a tecla B/b,
     selecione todos os itens;
     Retorne.
  Se a tecla deste evento for a tecla C/c,
     efetue cópias;
     Retorne.
  Se a tecla deste evento for a tecla D/d,
     execute duplicate;
     Retorne.
  Se a tecla deste evento for a tecla E/e,
     tente encontrar o texto;
     Retorne.
  Se a tecla deste evento for a tecla G/g,
     agrupe as formas geométricas;
     Retorne.
  Se a tecla deste evento for a tecla I/i,
     ajuste o recuo interno;
     Retorne.
  Se a tecla deste evento for a tecla J/j,
     efetue a rotação da forma;
     Retorne.
  Se a tecla deste evento for a tecla L/l,
     crie o arquivo (extensão .lst);
     Retorne.
  Se a tecla deste evento for a tecla M/m e o modo do programa desta aba atual for "editor de texto",
     execute compile;
     Retorne.
  Se a tecla deste evento for a tecla M/m e o modo do programa desta aba atual for "editor de documentos",
     suavize os polígonos;
     Retorne.
  Se a tecla deste evento for a tecla N/n,
     tente encontrar novamente o texto;
     Retorne.
  Se a tecla deste evento for a tecla O/o e o modo do programa desta aba atual for "editor de texto",
     ajuste o recuo externo;
     Retorne.
  Se a tecla deste evento for a tecla O/o,
     abra o item;
     Retorne.
  Se a tecla deste evento for a tecla P/p,
     efetue a impressão;
     Retorne.
  Se a tecla deste evento for a tecla F/f,
     efetue a saída;
     Retorne.
  Se a tecla deste evento for a tecla R/r e o modo do programa desta aba atual for "editor de texto",
     coordene a execução dos programas;
     Retorne.
  Se a tecla deste evento for a tecla R/r e o modo do programa desta aba atual for "explorador de arquivos",
     coordene a execução dos programas;
     Retorne.
  Se a tecla deste evento for a tecla R/r e o modo do programa desta aba atual for "editor de documentos",
     execute reduza;
     Retorne.
  Se a tecla deste evento for a tecla S/s,
     salve as alterações;
     Retorne.
  Se a tecla deste evento for a tecla T/t,
     selecione todos os itens;
     Retorne.
  Se a tecla deste evento for a tecla U/u e o modo do programa desta aba atual for "editor de texto",
     efetue remoção de comentários;
     Retorne.
  Se a tecla deste evento for a tecla U/u e o modo do programa desta aba atual for "editor de documentos",
     separe as formas agrupadas;
     Retorne.
  Se a tecla deste evento for a tecla V/v,
     cole o item;
     Retorne.
  Se a tecla deste evento for a tecla W/w,
     execute encerramento;
     Retorne.
  Se a tecla deste evento for a tecla X/x,
     execute cut;
     Retorne.
  Se a tecla deste evento for a tecla Y/y,
     refaça a ação;
     Retorne.
  Se a tecla deste evento for a tecla Z/z,
     desfaça a ação;
     Retorne.
  Se a tecla deste evento for a tecla F1,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F2,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F3,
     tente encontrar novamente o texto;
     Retorne.
  Se a tecla deste evento for a tecla F4,
     efetue a saída;
     Retorne.
  Se a tecla deste evento for a tecla F5,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F6,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F7,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F8,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F9,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F10,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F11,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F12,
     exiba a versão do programa no painel de informações;
     Retorne.
  Se a tecla deste evento for a tecla F12,
     exiba a versão do programa no painel de informações;
     Retorne.
  Avise.

Rotina para que se execute um evento (pressionamento de tecla):
  Se o evento for algum atalho de tecla,
     execute o evento (pressionamento de tecla - atalho de tecla);
     Retorne.
  Se o evento for ctrl-tab,
     execute o evento (pressionamento de tecla - ctrl-tab);
     Retorne.
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute o evento usando a listagem desta aba atual (pressionamento de tecla);
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     execute o evento usando o source desta listagem desta aba atual (pressionamento de tecla);
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     execute o evento usando o documento desta listagem desta aba atual (pressionamento de tecla);
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     execute o evento usando a página desta listagem desta aba atual (pressionamento de tecla);
     Retorne.

Rotina para que se execute um evento (clique - menu):
  Encontre um comando usando o menu e a localização deste evento.
  Execute o comando.

Rotina para que se execute um evento (clique - painel de informações):
  Rastreie o ambiente de trabalho usando o evento.

Rotina para que se execute um evento (clique - alterne):
  Encontre uma aba usando a localização deste evento.
  Se a aba for inexistente, retorne.
  Se a aba for a aba atual, ative um detector.
  Alterne para a aba.
  Mostre o ambiente de trabalho com painel de informações.
  Rastreie a aba retornando um segundo detector.
  Se o segundo detector estiver ativo, retorne.
  Se o detector não estiver ativo, retorne.
  Execute encerramento.

Rotina para que se execute um evento (clique):
  Se a localização deste evento estiver no menu,
     execute o evento (clique - menu);
     Retorne.
  Se a localização deste evento estiver no painel de informações,
     execute o evento (clique - painel de informações);
     Retorne.
  Se a localização deste evento estiver no alternador,
     execute o evento (clique - alterne);
     Retorne.
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute o evento usando a listagem desta aba atual (clique);
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     execute o evento usando o source desta listagem desta aba atual (clique);
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     execute o evento usando o documento desta listagem desta aba atual (clique);
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     execute o evento usando a página desta listagem desta aba atual (clique);
     Retorne.

Rotina para que se execute um evento (clique duplo):
  Se a localização deste evento estiver no painel superior direito, retorne.
  Se a localização deste evento estiver no alternador, retorne.
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute o evento usando a listagem desta aba atual (clique duplo);
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     execute o evento usando o source desta listagem desta aba atual (clique duplo);
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     execute o evento usando o documento desta listagem desta aba atual (clique duplo);
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     execute o evento usando a página desta listagem desta aba atual (clique duplo);
     Retorne.

Rotina para que se execute um evento (atualização de tela):
  Mostre o ambiente de trabalho com painel de informações.

Rotina para que se execute um evento (clique direito):
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute o evento usando a listagem desta aba atual (clique direito);
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     execute o evento usando o source desta listagem desta aba atual (clique direito);
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     execute o evento usando o documento desta listagem desta aba atual (clique direito);
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     execute o evento usando a página desta listagem desta aba atual (clique direito);
     Retorne.

Rotina para que se execute um evento (exibir cursor do mouse):
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute o evento usando a listagem desta aba atual (exibir cursor do mouse);
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     execute o evento usando o source desta listagem desta aba atual (exibir cursor do mouse);
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     execute o evento usando o documento desta listagem desta aba atual (exibir cursor do mouse);
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     execute o evento usando a página desta listagem desta aba atual (exibir cursor do mouse);
     Retorne.
  Mostre a seta do mouse.

Rotina para que se execute um evento usando a caixa de diálogo:
  Se a categoria deste evento for "pressionamento de tecla",
     execute o evento usando a caixa de diálogo (pressionamento de tecla);
     Retorne.
  Se a categoria deste evento for "clique do botão esquerdo",
     execute o evento usando a caixa de diálogo (clique);
     Retorne.
  Se a categoria deste evento for "clique duplo",
     execute o evento usando a caixa de diálogo (clique duplo);
     Retorne.
  Se a categoria deste evento for "atualização de tela",
     execute o evento usando a caixa de diálogo (atualização de tela);
     Retorne.
  Se a categoria deste evento for "clique do botão direito",
     execute o evento usando a caixa de diálogo (clique direito);
     Retorne.
  Se a categoria deste evento for "movimentação de cursor",
     execute o evento usando a caixa de diálogo (exibir cursor do mouse);
     Retorne.

Rotina para que se execute um evento usando a caixa de diálogo (copiar):
  Se nada estiver selecionado na caixa de texto desta caixa de diálogo,
     avise o usuário;
     Retorne.
  efetue cópias usando a caixa de texto desta caixa de diálogo.

Rotina para que se execute um evento usando a caixa de diálogo (recortar):
  Se nada estiver selecionado na caixa de texto desta caixa de diálogo,
     avise o usuário;
     Retorne.
  Execute cut usando a caixa de texto desta caixa de diálogo.
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - tecla backspace):
  Execute o evento usando a caixa de texto desta caixa de diálogo (tecla backspace).
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (comando do teclado - confirmação/negação):
  Se a tecla deste evento for a tecla Enter,
     atribua "sim" ao texto do campo;
     renuncie ao controle; Retorne.
  Se a tecla deste evento for a tecla Esc,
     atribua "não" ao texto do campo;
     renuncie ao controle; Retorne.
  Encontre um botão usando a tecla deste evento.
  Se o botão for inexistente, retorne.
  Atribua o nome deste botão ao texto do campo.
  Renuncie ao controle.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - tecla Delete):
  Execute o evento usando a caixa de texto desta caixa de diálogo (tecla Delete).
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - tecla End):
  Execute o evento usando a caixa de texto desta caixa de diálogo (tecla End).
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - tecla Enter):
  Extraia o texto do campo desde a caixa de texto desta caixa de diálogo.
  Renuncie ao controle.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - erro):
  Feche a caixa de diálogo.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - tecla Esc):
  Feche a caixa de diálogo.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - tecla Home):
  Execute o evento usando a caixa de texto desta caixa de diálogo (tecla Home).
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - input):
  Se o evento for algum atalho de tecla,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - atalho de tecla);
     Retorne.
  Se o byte deste evento for imprimível,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - caracteres gráficos);
     Retorne.
  Se a tecla deste evento for a tecla backspace,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - tecla backspace);
     Retorne.
  Se a tecla deste evento for a tecla Delete,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - tecla Delete);
     Retorne.
  Se a tecla deste evento for a tecla End,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - tecla End);
     Retorne.
  Se a tecla deste evento for a tecla Enter,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - tecla Enter);
     Retorne.
  Se a tecla deste evento for a tecla Esc,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - tecla Esc);
     Retorne.
  Se a tecla deste evento for a tecla Home,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - tecla Home);
     Retorne.
  Se a tecla deste evento for a seta pra esquerda,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - seta esquerda);
     Retorne.
  Se a tecla deste evento for a seta pra direita,
     execute o evento usando a caixa de diálogo (pressionamento de tecla - seta direita);
     Retorne.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - seta esquerda):
  Execute o evento usando a caixa de texto desta caixa de diálogo (seta esquerda).
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - caracteres gráficos):
  Execute o evento usando a caixa de texto desta caixa de diálogo (caracteres gráficos).
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - seta direita):
  Execute o evento usando a caixa de texto desta caixa de diálogo (seta direita).
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla - atalho de tecla):
  Se a tecla deste evento for a tecla A/a, execute o evento usando a caixa de diálogo (selecionar tudo).
  Se a tecla deste evento for a tecla C/c, execute o evento usando a caixa de diálogo (copiar).
  Se a tecla deste evento for a tecla V/v, execute o evento usando a caixa de diálogo (colar).
  Se a tecla deste evento for a tecla X/x, execute o evento usando a caixa de diálogo (recortar).
  Se a tecla deste evento for a tecla Y/y, execute o evento usando a caixa de diálogo (refazer).
  Se a tecla deste evento for a tecla Z/z, execute o evento usando a caixa de diálogo (desfazer).
  Se a tecla deste evento for a tecla F4,
     efetue a saída;
     Retorne.

Rotina para que se execute um evento usando a caixa de diálogo (pressionamento de tecla):
  Se a categoria desta caixa de diálogo for "botão",
     execute o evento usando a caixa de diálogo (comando do teclado - confirmação/negação);
     Retorne.
  Se a categoria desta caixa de diálogo for "erro",
     execute o evento usando a caixa de diálogo (pressionamento de tecla - erro);
     Retorne.
  Se a categoria desta caixa de diálogo for "input",
     execute o evento usando a caixa de diálogo (pressionamento de tecla - input);
     Retorne.

Rotina para que se execute um evento usando a caixa de diálogo (clique - confirmação/negação):
  Se a localização deste evento não estiver no painel superior direito, 
    Feche a caixa de diálogo; 
    Retorne.
  Encontre um botão usando a localização deste evento.
  Se o botão for inexistente, 
    Retorne.
  Atribua o nome deste botão ao texto do campo.
  Renuncie ao controle. \Abdique

Rotina para que se execute um evento usando a caixa de diálogo (clique - erro):
  Se a localização deste evento não estiver no painel superior direito, 
    Feche a caixa de diálogo; Retorne.
  Encontre um botão usando a localização deste evento.
  Se o botão for inexistente, retorne.
  Atribua o nome deste botão ao texto do campo.
  Renuncie ao controle.

Rotina para que se execute um evento usando a caixa de diálogo (clique - input):
  Se a localização deste evento não estiver no painel superior direito, 
    Feche a caixa de diálogo; 
    Retorne.
  Se a localização deste evento estiver na caixa de texto desta caixa de diálogo, 
    Rastreie a caixa de diálogo usando o evento; 
    Retorne.
  Encontre um botão usando a localização deste evento.
  Se o botão for inexistente, retorne.
  Se o nome deste botão for "cancelar", 
    Feche a caixa de diálogo; 
    Retorne.
  Extraia o texto do campo desde a caixa de texto desta caixa de diálogo.
  Renuncie ao controle.

Rotina para que se execute um evento usando a caixa de diálogo (clique):
  Se a categoria desta caixa de diálogo for "botão", 
    Execute o evento usando a caixa de diálogo (clique - confirmação/negação); 
    Retorne.
  Se a categoria desta caixa de diálogo for "erro", 
    Execute o evento usando a caixa de diálogo (clique - erro); 
    Retorne.
  Se a categoria desta caixa de diálogo for "input", 
    Execute o evento usando a caixa de diálogo (clique - input); 
    Retorne.

Rotina para que se execute um evento usando a caixa de diálogo (clique duplo):
  Se a caixa de texto desta caixa de diálogo for inexistente, retorne.
  Execute o evento usando a caixa de texto desta caixa de diálogo (clique duplo).
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (colar):
  Se não existir caixa de texto sobre a área de transferência do Windows,
     avise o usuário;
     Retorne.
  Cole o item usando a caixa de texto desta caixa de diálogo.
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (refazer):
  Se a caixa de texto desta caixa de diálogo não pode ser refeito,
     avise o usuário;
     Retorne.
  Refaça a ação usando a caixa de texto desta caixa de diálogo.
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (atualização de tela):
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (clique direito):
  Se a localização deste evento estiver no painel superior direito, retorne.
  Feche a caixa de diálogo.

Rotina para que se execute um evento usando a caixa de diálogo (selecionar tudo):
  Selecione todos os itens usando a caixa de texto desta caixa de diálogo.
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando a caixa de diálogo (exibir cursor do mouse):
  Se a localização deste evento estiver na caixa de texto desta caixa de diálogo,
     mostre o i-beam cursor;
     Retorne.
  Mostre a seta do mouse.

Rotina para que se execute um evento usando a caixa de diálogo (desfazer):
  Se a caixa de texto desta caixa de diálogo não puder ser desfeita,
     avise o usuário;
     Retorne.
  Desfaça a ação usando a caixa de texto desta caixa de diálogo.
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando um submenu:
  Se a categoria deste evento for "pressionamento de tecla",
     execute o evento usando o submenu (pressionamento de tecla);
     Retorne.
  Se a categoria deste evento for "clique do botão esquerdo",
     execute o evento usando o submenu (clique);
     Retorne.
  Se a categoria deste evento for "atualização de tela",
     execute o evento usando o submenu (atualização de tela);
     Retorne.
  Se a categoria deste evento for "movimentação de cursor",
     execute o evento usando o submenu (exibir cursor do mouse);
     Retorne.

Rotina para que se execute um evento usando um submenu (pressionamento de tecla):
  Se a tecla deste evento for a tecla Esc, renuncie ao controle.

Rotina para que se execute um evento usando um submenu (clique - menu):
  Encontre um comando usando o submenu e a localização deste evento.
  Se o comando for inexistente, retorne.
  Atribua o comando ao comando selecionado.
  Renuncie ao controle.

Rotina para que se execute um evento usando um submenu (clique):
  Se a localização deste evento estiver no submenu,
     execute o evento usando o submenu (clique - menu);
     Retorne.
  Renuncie ao controle.
  Se a localização deste evento estiver no menu, reenfileire o evento.

Rotina para que se execute um evento usando um submenu (atualização de tela):
  Mostre o ambiente de trabalho.

Rotina para que se execute um evento usando um submenu (exibir cursor do mouse):
  Mostre a seta do mouse.

Rotina para que se execute alguns eventos:
  Desenfileire um evento.
  Se o evento for inexistente, retorne.
  Execute o evento.
  Reitere. [Execute novamente o código]

Rotina para que se execute alguns eventos usando a caixa de diálogo:
  Desenfileire um evento.
  Se o evento for inexistente, retorne.
  Execute o evento usando a caixa de diálogo.
  Reitere. [Execute novamente o código]

Rotina para que se execute alguns eventos usando um submenu:
  Desenfileire um evento.
  Se o evento for inexistente, retorne.
  Execute o evento usando o submenu.
  Reitere. [Execute novamente o código]

Rotina para que se execute extract globals:
  Se o modo do programa desta aba atual for "editor de texto",
     execute extract globals usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute extract cifra binária literal:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute extract cifra binária literal usando a listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se transcreva cabeçalhos de rotina:
  Se o modo do programa desta aba atual for "editor de texto",
     transcreva cabeçalhos de rotina usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute extract tipos:
  Se o modo do programa desta aba atual for "editor de texto",
     execute extract tipos usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se preencha a forma geométrica usando uma cor:
  Se o modo do programa desta aba atual for "editor de documentos",
     preencha a forma geométrica usando a página desta listagem desta aba atual e a cor;
     Retorne.
  Avise.

Rotina para que se tente encontrar o texto:
  Se o modo do programa desta aba atual for "editor de texto",
     tente encontrar o texto usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     tente encontrar o texto usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se tente encontrar novamente o texto:
  Se o modo do programa desta aba atual for "editor de texto",
     tente encontrar novamente o texto usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     tente encontrar novamente o texto usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute flip:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute flip usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se mude o tamanho usando uma altura da fonte:
  Se o modo do programa desta aba atual for "editor de texto",
     mude o tamanho usando o source desta listagem desta aba atual e a altura da fonte;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     mude o tamanho usando a página desta listagem desta aba atual e a altura da fonte;
     Retorne.
  Avise.

Rotina para que se mude o tamanho de outros itens:
  Se o modo do programa desta aba atual for "editor de texto",
     mude o tamanho segunda usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     mude o tamanho segunda usando a página desta listagem desta aba atual;
     Retorne.

Rotina para que se mude nome da fonte:
  Se o modo do programa desta aba atual for "editor de texto",
     mude nome da fonte usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     mude nome da fonte usando a página desta listagem desta aba atual;
     Retorne.

Rotina para que se execute obtenha tamanho:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute obtenha tamanho usando a listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute grayscale usando um detector:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute grayscale usando a página desta listagem desta aba atual e o detector;
     Retorne.
  Avise.

Rotina para que se execute malha usando um detector:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute malha usando a página desta listagem desta aba atual e o detector;
     Retorne.
  Avise.

Rotina para que se agrupe as formas geométricas:
  Se o modo do programa desta aba atual for "editor de documentos",
     agrupe as formas geométricas usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se ajuste o recuo interno:
  Se o modo do programa desta aba atual for "editor de texto",
     ajuste o recuo interno usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute label usando um label:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute label usando a página desta listagem desta aba atual e o label;
     Retorne.
  Avise.

Rotina para que se crie o arquivo (extensão .lst):
  Se o modo do programa desta aba atual não for "editor de texto",
     avise o usuário;
     Retorne.
  Salve tudo.
  Se o erro do fluxo de entrada/saída não estiver em branco, retorne.
  Encontre um endereço da pasta para ser compilada.
  Se o endereço da pasta estiver em branco,
     mostre erro "Não tem nada para eu compilar e listar";
     Retorne.
  Compile o endereço da pasta.
  Mostre no painel de informações "Listando...".
  Liste tudo.
  Se o detector de erros deste compilador estiver ativo,
     exiba as etapas e erros de compilação;
     Retorne.
  Mostre no painel de informações a string deste temporizador deste compilador seguido de " milissegundos".

Rotina para que se execute lowercase:
  Se o modo do programa desta aba atual for "editor de texto",
     execute lowercase usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     execute lowercase usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute measure:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute measure usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se minimize a janela:
  Minimize a janela principal do programa.

Rotina para que se execute mirror:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute mirror usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se crie a nova pasta:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     crie a nova pasta usando a listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se crie o novo documento:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     crie o novo documento usando a listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se crie a nova página:
  Se o modo do programa desta aba atual for "sumário",
     crie a nova página usando o documento desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se crie a nova forma geométrica usando uma categoria:
  Se o modo do programa desta aba atual for "editor de documentos",
     crie a nova forma geométrica usando a página desta listagem desta aba atual e a categoria;
     Retorne.
  Avise.

Rotina para que se execute new caixa de texto arquivo:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute new caixa de texto arquivo usando a listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se abra o item:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     abra o explorador de arquivos;
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     abra o editor de documentos;
     Retorne.
  Avise.

Rotina para que se abra o editor de documentos:
  Abra o sumário.
  Mostre o ambiente de trabalho com painel de informações.
  Desmanche a fila de eventos.
  Atualize o cursor.

Rotina para que se abra o explorador de arquivos:
  Se o modo do programa desta aba atual não for "explorador de arquivos",
     avise o usuário;
     Retorne.
  Obtenha uns links do endereço completo selecionado usando a listagem desta aba atual.
  Se o links do endereço completo selecionado estiverem vazios,
     mostre erro "Não tem nada aqui pra eu abrir.";
     Retorne.
  Mostre no painel de informações "Abrindo...".
  Atribua a aba atual para uma aba.
  Itere. [o código a seguir]
    Obtenha um endereço completo link desde o links do endereço completo selecionado.
    Se o endereço completo link for inexistente, pare.
    Recolha a listagem desta aba para a raiz.
    Abra a listagem desta aba usando a string deste endereço completo link.
    Alterne a aba para a listagem desta aba.
    Alterne para a aba.
    Se o erro do fluxo de entrada/saída não estiver em branco,
     mostre erro o erro do fluxo de entrada/saída;
     pare.
    Se a listagem desta aba deveria ser somente leitura, defina a listagem desta aba para somente leitura mode. \it
    Encontre uma aba disponível começando com a próxima aba desta aba.
    Se a aba disponível for inexistente, pare.
    Atribua a aba disponível à aba.
  Reitere. [Execute novamente o código]
  Destrua o links do endereço completo selecionado.
  Mostre o ambiente de trabalho com painel de informações.
  Desmanche a fila de eventos.
  Atualize o cursor.

Rotina para que se abra o visualizador hexadecimal:
  Se o modo do programa desta aba atual não for "explorador de arquivos",
     avise o usuário;
     Retorne.
  Atribua o primeiro elemento selecionado desta listagem desta aba atual para um elemento.
  Se o elemento for inexistente,
     mostre erro "Não tem nada pra abrir aqui.";
     Retorne.
  Se a categoria deste elemento não for "arquivo",
     avise o usuário;
     Retorne.
  Mostre no painel de informações "Abrindo...".
  Abra o elemento (arquivo binário).
  Se o erro do fluxo de entrada/saída não estiver em branco,
     mostre erro o erro do fluxo de entrada/saída;
     Retorne.
  Alterne a aba atual para o elemento.
  Mostre o ambiente de trabalho com painel de informações.
  Desmanche a fila de eventos.

Rotina para que se abra o editor de texto:
  Se o modo do programa desta aba atual não for "explorador de arquivos",
     avise o usuário;
     Retorne.
  Atribua o primeiro elemento selecionado desta listagem desta aba atual para um elemento.
  Se o elemento for inexistente,
     mostre erro "Não tem nada pra eu abrir.";
     Retorne.
  Se a categoria deste elemento não for "arquivo",
     avise o usuário;
     Retorne.
  Mostre no painel de informações "Abrindo...".
  Abra o elemento (arquivo de texto).
  Se o erro do fluxo de entrada/saída não estiver em branco,
     mostre erro o erro do fluxo de entrada/saída;
     Retorne.
  Alterne a aba atual para o elemento.
  Mostre o ambiente de trabalho com painel de informações.
  Desmanche a fila de eventos.

Rotina para que se abra o sumário:
  Atribua a primeira página selecionada deste documento desta listagem desta aba atual para uma página.
  Se a página for inexistente,
     mostre erro "Não tem nada pra eu abrir por aqui.";
     Retorne.
  Focalize na página no documento desta listagem desta aba atual.
  Atribua a página à página desta listagem desta aba atual.
  Abra a página desta listagem desta aba atual.
  Alterne a aba atual para a listagem desta aba atual.

Rotina para que se ajuste o recuo externo:
  Se o modo do programa desta aba atual for "editor de texto",
     ajuste o recuo externo usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se cole o item:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     cole o item usando a listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     cole o item usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     cole o item usando o documento desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     cole o item usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se modifique a cor usando uma cor:
  Se o modo do programa desta aba atual for "editor de documentos",
     modifique a cor usando a página desta listagem desta aba atual e a cor;
     Retorne.
  Avise.

Rotina para que se efetue a impressão:
  Se o modo do programa desta aba atual for "editor de texto",
     efetue a impressão usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     efetue a impressão usando o documento desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     efetue a impressão usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se efetue a saída:
  Se o usuário não estiver decidido, retorne.
  Itere. [o código a seguir]
    Mostre no painel de informações "Saindo...".
    Obtenha uma aba desde as abas.
    Se a aba for inexistente,
     renuncie ao controle;
     Retorne.
    Se a listagem desta aba não foi alterada, reitere.
    Alterne para a aba.
    Mostre o ambiente de trabalho.
    Se o usuário não estiver pronto pra fechar a listagem desta aba, retorne.
  Reitere. [Execute novamente o código]

Rotina para que se modifique o valor do raio:
  Se o modo do programa desta aba atual for "editor de documentos",
     modifique o valor do raio usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se refaça a ação:
  Se o modo do programa desta aba atual for "editor de texto",
     refaça a ação usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     refaça a ação usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute reduza:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute reduza usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se renomeie o arquivo:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     renomeie o arquivo usando a listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se renumere as páginas:
  Se o modo do programa desta aba atual for "sumário",
     renumere as páginas usando o documento desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se reverta a ação:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     reverta a ação usando a listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     reverta a ação usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se efetue a rotação da página:
  Se o modo do programa desta aba atual for "editor de documentos",
     efetue a rotação da página usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se efetue a rotação da forma:
  Se o modo do programa desta aba atual for "editor de documentos",
     efetue a rotação da forma usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se coordene a execução dos programas:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     coordene a execução dos programas usando a listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual não for "editor de texto",
     avise o usuário;
     Retorne.
  Salve tudo.
  Se o erro do fluxo de entrada/saída não estiver em branco, retorne.
  Encontre um endereço da pasta para ser compilada.
  Se o endereço da pasta estiver em branco,
     mostre erro "Não tem nada pra eu fazer aqui.";
     Retorne.
  Compile o endereço da pasta.
  Se o detector de erros deste compilador estiver ativo,
     exiba as etapas e erros de compilação;
     Retorne.
  Mostre no painel de informações "Executando...".
  Comece o processo atual usando o endereço EXE completo deste compilador.
  Se o erro do fluxo de entrada/saída não estiver em branco,
     mostre erro o erro do fluxo de entrada/saída;
     Retorne.
  Mostre no painel de informações a string deste temporizador deste compilador seguido de " milissegundos".

Rotina para que se salve as alterações:
  Se a listagem desta aba atual não for savable,
     avise o usuário;
     mostre painel de informações "Não tem como salvar isso aqui"; Retorne.
  Mostre no painel de informações "Salvando...".
  Salve a listagem desta aba atual.
  Se o erro do fluxo de entrada/saída não estiver em branco,
     mostre erro o erro do fluxo de entrada/saída;
     Retorne.
  Mostre no painel de informações "Salvo".

Rotina para que se salve tudo:
  Mostre no painel de informações "Salvando...".
  Atribua 0 para uma contagem.
  Limpe o erro do fluxo de entrada/saída.
  Itere. [o código a seguir]
    Obtenha uma aba desde as abas.
    Se a aba for inexistente,
     mostre painel de informações a contagem e "arquivo salvo" ou "arquivos salvos";
     Retorne.
    Se a listagem desta aba não for savable, reitere.
    Salve a listagem desta aba.
    Se o erro do fluxo de entrada/saída estiver em branco,
     adicione 1 para a contagem;
     Reitere.
  Alterne para a aba.
  Mostre o ambiente de trabalho.
  Mostre erro o erro do fluxo de entrada/saída.

Rotina para que se salve arquivo como pdf:
  Se o modo do programa desta aba atual for "sumário",
     salve arquivo como pdf usando o documento desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se selecione todos os itens:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     selecione todos os itens usando a listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de texto",
     selecione todos os itens usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "sumário",
     selecione todos os itens usando o documento desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     selecione todos os itens usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se envie o item para trás:
  Se o modo do programa desta aba atual for "editor de documentos",
     envie o item para trás usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se suavize os polígonos:
  Se o modo do programa desta aba atual for "editor de documentos",
     suavize os polígonos usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se classifique as definições:
  Se o modo do programa desta aba atual for "editor de texto",
     classifique as definições usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute classifique elementos usando uma string:
  Se o modo do programa desta aba atual for "explorador de arquivos",
     execute classifique elementos usando a listagem desta aba atual e a string;
     Retorne.
  Avise.

Rotina para que se organize as linhas selecionadas no texto:
  Se o modo do programa desta aba atual for "editor de texto",
     organize as linhas selecionadas no texto usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se interrompa a execução do programa:
  Se o processo atual for 0,
     avise o usuário;
     Retorne.
  Mostre no painel de informações "Finalizando execução do programa...".
  Suspenda o processo atual.
  Mostre no painel de informações "Execução finalizada".

Rotina para que se efetue remoção de comentários:
  Se o modo do programa desta aba atual for "editor de texto",
     efetue remoção de comentários usando o source desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se desfaça a ação:
  Se o modo do programa desta aba atual for "editor de texto",
     desfaça a ação usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     desfaça a ação usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se separe as formas agrupadas:
  Se o modo do programa desta aba atual for "editor de documentos",
     separe as formas agrupadas usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se execute desmarque:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute desmarque usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se mude o texto selecionado para maiúsculas:
  Se o modo do programa desta aba atual for "editor de texto",
     mude o texto selecionado para maiúsculas usando o source desta listagem desta aba atual;
     Retorne.
  Se o modo do programa desta aba atual for "editor de documentos",
     mude o texto selecionado para maiúsculas usando a página desta listagem desta aba atual;
     Retorne.
  Avise.

Rotina para que se exiba a versão do programa no painel de informações:
  Mostre no painel de informações o nome deste módulo.

Rotina para que se execute yank usando um número:
  Se o modo do programa desta aba atual for "editor de documentos",
     execute yank usando a página desta listagem desta aba atual e o número;
     Retorne.
  Avise.

Um rótulo é uma string.

Rotina para que se inicialize a IDE:
  Inicialize o ambiente de trabalho.
  \Crie barra de menus.
  Inicialize o explorador de arquivos.
  Inicialize o editor.
  Inicialize o caderno.
  Inicialize o compilador.
  Preencha as abas.
  Alterne para a primeira aba destas abas.

Rotina para que se inicialize o painel superior direito:
  Atribua "normal" ao modo do programa deste painel superior direito.
  Atribua 80 pixels à altura deste painel superior direito. \ 80 pixels -> 53/64 polegadas
  Atribua a coordenada X1 desta tela à coordenada X1 deste painel superior direito.
  Atribua a coordenada Y1 desta tela à coordenada Y1 deste painel superior direito.
  Atribua a coordenada X2 desta tela à coordenada X2 deste painel superior direito.
  Atribua a coordenada Y1 desta tela mais a altura deste painel superior direito à coordenada Y2 deste painel superior direito.
  Atribua a caixa deste painel superior direito à caixa de exibição deste painel superior direito.
  Ajuste a caixa de exibição deste painel superior direito usando 24 pixels e 24 pixels e -24 pixels e -24 pixels.
  Destrua o menu.
  Crie o menu.
  Inicialize o painel de informações.

Rotina para que se inicialize o ambiente de trabalho:
  Atribua 400 e 122 e 810 [era a cor cinza claríssimo] à cor do ambiente de trabalho.  \ Mudei pra ficar igual ao tom do Windows
  \Atribua 400 à saturação desta cor do ambiente de trabalho.
  Atribua a cor azul celeste claro à cor dos comentários. \ Aqui você muda uma cor dos comentários
  Atribua a cor azul celeste claríssimo à malha cor.
  Atribua a cor vermelha ao tracer cor. \ tracer
  Inicialize o painel superior direito.
  Inicialize o alternador.
  Inicialize o pad. \ uses a painel superior direito e o alternador

Rotina para que se inicialize o pad:
  Atribua a coordenada X1 desta tela à coordenada X1 deste pad.
  Atribua a coordenada Y2 deste painel superior direito à coordenada Y1 deste pad.
  Atribua a coordenada X2 desta tela à coordenada X2 deste pad.
  Atribua a coordenada Y1 deste alternador à coordenada Y2 deste pad.

Rotina para que se inicialize o painel de informações:
  Atribua a caixa de exibição deste painel superior direito e 0 pixels [raio da borda] à caixa arredondada deste painel de informações.
  Atribua a coordenada X2 deste último comando destes comandos deste menu à coordenada X1 deste painel de informações.

Rotina para que se inicialize o alternador:
  Inicialize as abas.
  Atribua [24 pixels mais 32 pixels] 56 pixels à altura deste alternador.
  Atribua a coordenada X1 desta tela à coordenada X1 deste alternador.
  Atribua a coordenada Y2 desta tela menos a altura deste alternador à coordenada Y1 deste alternador.
  Atribua a coordenada X2 desta tela à coordenada X2 deste alternador.
  Atribua a coordenada Y2 desta tela à coordenada Y2 deste alternador.
  Ajuste as abas.

Rotina para que se inicialize uns abas:
  Adicione 1 para uma contagem.
  Se a contagem for maior do que 10, pare.
  Crie uma aba.
  Acrescente a aba para as abas.
  Reitere. [Execute novamente o código]

O menu é um menu.

Um menu é uma lista com uns comandos.

Um modo do programa é uma string.

O pad é uma estrutura com uma caixa.

A largura da paleta de cores é uma largura igual a 224 pixels.

Um texto de aviso é uma string.

Uma questão é uma string.

Rotina para que se execute o programa:
  Inicialize os componentes.
  Inicialize a IDE.
  Execute alguns eventos.
  Finalize.
  Feche o programa.

O comando selecionado é um comando.

Um atalho de tecla é uma string.

Rotina para que se mostre o ambiente de trabalho:
  Pinte o ambiente de trabalho.
  Atualize o tela.

Rotina para que se mostre o ambiente de trabalho com painel de informações:
  Limpe a string deste painel de informações.
  Se o modo do programa desta aba atual for "explorador de arquivos", obtenha o painel de informações pela listagem desta aba atual.
  Se o modo do programa desta aba atual for "editor de texto", obtenha o painel de informações pelo source desta listagem desta aba atual.
  Se o modo do programa desta aba atual for "sumário", obtenha o painel de informações pelo documento desta listagem desta aba atual.
  Se o modo do programa desta aba atual for "editor de documentos", obtenha o painel de informações pela página desta listagem desta aba atual.
  Mostre o ambiente de trabalho.

Rotina para que se mostre o ambiente de trabalho com uma string:
  Atribua a string à string deste painel de informações.
  Mostre o ambiente de trabalho.

Rotina para que se mostre erro uma string:
  Crie a caixa de diálogo.
  Atribua "erro" à categoria desta caixa de diálogo.
  Atribua a cor vermelha à cor da canetinha desta caixa de diálogo.
  Atribua a string ao texto de aviso desta caixa de diálogo.
  Adicione um botão para a caixa de diálogo usando "Beleza".
  Avise.
  Execute a caixa de diálogo.
  Destrua a caixa de diálogo.

Rotina para que se mostre o painel de informações:
  Pinte o painel de informações.
  Atualize o tela usando a caixa arredondada deste painel de informações.

Rotina para que se mostre no painel de informações um número e uma string ou uma segunda string;
Rotina para que se mostre painel de informações um número e uma string ou uma segunda string:
  Format o número e a string ou a segunda string para uma terceira string.
  Mostre no painel de informações a terceira string.

Rotina para que se mostre uma string no painel de informações;
Rotina para que se mostre no painel de informações uma string;
Rotina para que se mostre painel de informações uma string:
  Atribua a string à string deste painel de informações.
  Mostre o painel de informações.

O painel de informações é uma estrutura com
  Uma caixa arredondada e
  Uma string.

Um submenu é um menu.

A largura do submenu é uma largura igual a 288 pixels. \ Aqui você muda uma largura do submenu.

A largura do botão da paleta é uma largura igual a 32 pixels.

Rotina para que se alterne o painel superior direito para um modo do programa:
  Atribua o modo do programa ao modo do programa deste painel superior direito.
  Limpe a string deste painel de informações.
  Mostre o ambiente de trabalho.

Rotina para que se alterne o painel superior direito para um modo do programa sem drawing:
  Atribua o modo do programa ao modo do programa deste painel superior direito.
  Limpe a string deste painel de informações.

O alternador é uma estrutura com
  Uma altura e
  Uma caixa.

Rotina para que se alterne para o predecessor de_ uma aba:
  Se a aba for inexistente, retorne.
  Atribua o precedente aba desta aba para uma segunda aba.
  Se a segunda aba for inexistente, atribua o último aba destas abas à segunda aba.
  Alterne para a segunda aba.

Rotina para que se alterne para o successor de_ uma aba:
  Se a aba for inexistente, retorne.
  Atribua a próxima aba desta aba para uma segunda aba.
  Se a segunda aba for inexistente, atribua o primeiro aba destas abas à segunda aba.
  Alterne para a segunda aba.

Rotina para que se alterne para uma aba:
  Se a aba for inexistente, retorne.
  Se a aba for a aba atual, retorne.
  Atribua a aba à aba atual.

Rotina para que se alterne uma aba para uma listagem:
  Se a aba for inexistente, retorne.
  Se a listagem for inexistente, retorne.
  Atribua a listagem à listagem desta aba.
  Atribua o designador desta listagem ao nome desta aba.
  Lowercase o nome desta aba.
  Format o nome desta aba para fit na caixa desta aba usando a fonte padrão.
  Atribua "explorador de arquivos" ao modo do programa desta aba.
  Se o source desta listagem não for inexistente, atribua "editor de texto" ao modo do programa desta aba.
  Se o documento desta listagem não for inexistente, atribua "sumário" ao modo do programa desta aba.
  Se a página desta listagem não for inexistente, atribua "editor de documentos" ao modo do programa desta aba.

Uma aba é uma lista com
  Uma caixa,
  Um nome,
  Um modo do programa,
  Uma listagem.

As abas são umas abas.

Rotina para que se rastreie o ambiente de trabalho usando um evento:
  Se o evento for inexistente, retorne.
  Itere. [o código a seguir]
    Se o botão esquerdo deste mouse não estiver sendo pressionado,
      Pare.
    Atribua a localização deste mouse para uma localização.
    Se a coordenada X desta localização for maior do que a coordenada X2 desta tela,
      Mova a janela principal do programa para a direita;
      Reitere.
    Se a coordenada X desta localização for menor do que a coordenada X1 desta tela,
      Mova a janela principal do programa para a esquerda;
      Reitere.
  Reitere. [Execute novamente o código]

Rotina para que se rastreie a caixa de diálogo usando um evento:
  Se a caixa de diálogo for inexistente, retorne.
  Se a caixa de texto desta caixa de diálogo for inexistente, retorne.
  Se o evento for inexistente, retorne.
  Expanda alguma seleção na caixa de texto desta caixa de diálogo usando a localização deste evento.
  Se o detector de tecla Shift deste evento não estiver ativo, desvencilhe-se de a caixa de texto desta caixa de diálogo.
  Limpe uma seleção.
  Itere. [o código a seguir]
    Se um detector estiver ativo, mostre o ambiente de trabalho com painel de informações.
    Se o botão esquerdo deste mouse não estiver sendo pressionado, retorne.
    Atribua a localização deste mouse para uma localização.
    Role automaticamente a caixa de texto desta caixa de diálogo usando a localização e o detector.
    Expanda alguma seleção na caixa de texto desta caixa de diálogo usando a localização.
    Se a seleção desta caixa de texto desta caixa de diálogo for a seleção, reitere.
    Ative o detector.
    Atribua a seleção desta caixa de texto desta caixa de diálogo à seleção.
  Reitere. [Execute novamente o código]

Rotina para que se rastreie uma aba retornando um detector:
  Desative o detector.
  Se a aba for inexistente, retorne.
  Itere. [o código a seguir]
    Se o botão esquerdo deste mouse não estiver sendo pressionado, retorne.
    Atribua a localização deste mouse para uma localização.
    Encontre uma segunda aba usando a localização.
    Se a segunda aba for inexistente, reitere.
    Se a aba for a segunda aba, reitere.
    Ative o detector.
    Remova a aba desde as abas.
    Se a coordenada X1 desta segunda aba for menor do que a coordenada X1 desta aba, insira a aba para as abas antes de a segunda aba.
    Se a coordenada X1 desta segunda aba for maior do que a coordenada X1 desta aba, insira a aba para as abas após a segunda aba.
    Ajuste as abas.
    Mostre o ambiente de trabalho.
  Reitere. [Execute novamente o código]
