\ copyright © 2006, 2017 o osmosian order (4700)

Como adicionar uma botão to o dialog usando um texto:
Se o texto estiver em branco, esvazie o conteúdo do botão, senão então deixa quieto.
Se o dialog não existir, deixe quieto.
Create o botão.
Posponha o botão to o dialog's botões.
Coloque o texto no botão's nome.

Como adicionar uma cor e uma handler to um menu:
Se o menu não existir, deixe quieto.
Create uma comando.
Posponha o comando to o menu's comandos.
Coloque o swatch width no comando's width.
Coloque o handler no comando's handler.
Coloque o cor no comando's cor.

Como adicionar um nome e uma handler to um menu:
Se o menu não existir, deixe quieto.
Create uma comando.
Posponha o comando to o menu's comandos.
Coloque o comando altura no comando's width.
Coloque o nome no comando's nome.
Coloque o handler no comando's handler.
Coloque a cor branca no comando's cor.
Coloque "left" no comando's alignment.

Como adicionar um nome e uma handler to uma submenu:
Adicione o nome e "" e o handler to o submenu.

Como adicionar um nome e uma shortcut e uma handler to uma submenu:
Se o submenu não existir, deixe quieto.
Create uma comando.
Posponha o comando to o submenu's comandos.
Coloque o submenu width no comando's width.
Coloque o nome no comando's nome.
Coloque o shortcut no comando's shortcut.
Coloque o handler no comando's handler.
Coloque a cor branca no comando's cor.
Coloque "left" no comando's alignment.

Como adicionar swatches to uma submenu usando uma handler:
Se o submenu não existir, deixe quieto.
Adicione o clear cor e o handler to o submenu.
Coloque o palette width dividido por 3 no submenu's comandos' last's width.
Coloque "clear" no submenu's comandos' last's nome.
Coloque "center" no submenu's comandos' last's alignment.
Adicione a cor branca e o handler to o submenu.
Coloque o palette width dividido por 3 no submenu's comandos' last's width.
Adicione a cor preta e o handler to o submenu.
Coloque o palette width dividido por 3 no submenu's comandos' last's width.
Adicione swatches to o submenu usando o handler e a cor cinza.
Adicione swatches to o submenu usando o handler e a cor vermelha.
Adicione swatches to o submenu usando o handler e a cor laranja.
Adicione swatches to o submenu usando o handler e a cor amarela.
Adicione swatches to o submenu usando o handler e o lime cor.
Adicione swatches to o submenu usando o handler e a cor verde.
Adicione swatches to o submenu usando o handler e o teal cor.
Adicione swatches to o submenu usando o handler e o cyan cor.
Adicione swatches to o submenu usando o handler e o sky cor.
Adicione swatches to o submenu usando o handler e a cor azul.
Adicione swatches to o submenu usando o handler e a cor roxa.
Adicione swatches to o submenu usando o handler e o magentuma cor.
Adicione swatches to o submenu usando o handler e a cor violeta.

Como adicionar swatches to uma submenu usando uma handler e uma cor:
Se o submenu não existir, deixe quieto.
Privatize o cor.
Coloque 875 no cor's lightness.
Percorra.
Adicione o cor e o handler to o submenu.
Subtract 125 usando o cor's lightness.
Se o cor's lightness is less than 125, deixe quieto.
Repita.

Como adjust o dialog:
Se o dialog não existir, deixe quieto.
Adjust o dialog (botões).
Adjust o dialog (redonda caixa).
Adjust o dialog (prompt).
Adjust o dialog (caixa de texto).

Como adjust o dialog (botões):
Coloque o barra's display e 1/12 inch dentro de uma redonda caixa.
Coloque o redonda caixa's right minus o botão width no redonda caixa's left.
Percorra.
Pegue uma botão usando o dialog's botões (backwards).
Se o botão não existir, deixe quieto.
Coloque o redonda caixa no botão's redonda caixa.
Move o redonda caixa left o botão width.
Repita.

Como adjust o dialog (prompt):
Coloque o dialog's redonda caixa's left plus 1/8 inch no dialog's prompt caixa's left.
Coloque o dialog's redonda caixa's parte de cima plus 1/24 inch no dialog's prompt caixa's parte de cima.
Pegue uma width usando o dialog's prompt e o default font.
Coloque o dialog's prompt caixa's left plus o width plus 1/12 inch no dialog's prompt caixa's right.
Coloque o dialog's redonda caixa's bottom minus 1/24 inch no dialog's prompt caixa's bottom.

Como adjust o dialog (redonda caixa):
Coloque o barra's display e 1/12 inch no dialog's redonda caixa.
Se o dialog's botões are empty, deixe quieto.
Coloque o dialog's primeiro botão's left no dialog's redonda caixa's right.

Como adjust o dialog (caixa de texto):
Se o dialog's caixa de texto não existir, deixe quieto.
Mude para a cor verde dialog's caixa de texto's horizontal scroll flag.
Select every pedaço in o dialog's caixa de texto.
Coloque o dialog's canetinha no dialog's caixa de texto's canetinha.
Coloque o dialog's prompt caixa's right dentro de uma caixa's left.
Coloque o dialog's prompt caixa's parte de cima no caixa's parte de cima.
Coloque o dialog's redonda caixa's right minus 1/8 inch no caixa's right.
Coloque o dialog's prompt caixa's bottom no caixa's bottom.
Change o dialog's caixa de texto usando o caixa.

Como adjust um menu usando um local e uma width:
Se o menu não existir, deixe quieto.
Privatize o local.
Coloque o local's x plus o width dentro de uma right coord.
Percorra.
Pegue uma comando usando o menu's comandos.
Se o comando não existir, pare.
Se o local's x plus o comando's width is greater than o right, put o original local's x no local's x; adicione o comando altura to o local's y.
Coloque o local e o local no comando's redonda caixa.
Adicione o comando's width e o comando altura to o comando's redonda caixa's right-bottom.
Adicione o comando's width to o local's x.
Repita.

Como adjust some abas:
Se a abas' count is 0, deixe quieto.
Coloque o switch's caixa's width dividido por a abas' count dentro de uma width.
Round o width to o tpp.
Percorra.
Pegue uma aba usando a abas.
Se a aba não existir, deixe quieto.
Coloque o switch's caixa na aba's caixa.
Adicione 1/4 inch to a aba's parte de cima.
Se a aba is not a abas' primeiro, put a aba's previous' right na aba's left.
Se a aba is not a abas' last, put a aba's left plus o width na aba's right.
Repita.

Um alternative é um texto.

The resposta é um texto.

Como fazer uma pergunta:
Faça a pergunta usando "".

Como fazer uma pergunta usando choices um texto e outro texto:
Faça a pergunta usando choices o texto e o outro texto e "".

Como fazer uma pergunta usando choices um texto e outro texto e uma terceiro texto:
Create o dialog.
Coloque "choice" no dialog's kind.
Coloque a pergunta no dialog's prompt.
Adicione uma botão to o dialog usando o texto.
Adicione outro botão to o dialog usando o outro texto.
Adicione uma terceiro botão to o dialog usando o terceiro texto.
Handle o dialog.
Destrua o dialog.

Como fazer uma pergunta usando uma default número:
Convert o default to um texto.
Faça a pergunta usando o texto.

Como fazer uma pergunta usando uma default proporção:
Convert o default to um texto.
Faça a pergunta usando o texto.

Como fazer uma pergunta usando uma default texto:
Create o dialog.
Coloque "input" no dialog's kind.
Coloque a pergunta no dialog's prompt.
Adicione uma botão to o dialog usando "M'Kay".
Adicione outro botão to o dialog usando "Cancelar".
Create o dialog's caixa de texto.
Coloque o default no dialog's caixa de texto.
Handle o dialog.
Destrua o dialog.

The barra has uma altura, uma caixa, uma display caixa e uma mode.

A botão é uma coisa usando uma redonda caixa e um nome.

The botão width is 2/3 inch.

Como cancelar o dialog:
Se o dialog não existir, deixe quieto.
Se o dialog's caixa de texto não existir, put "cancelar" no resposta.
Se o dialog's caixa de texto is not nil, put "" no resposta.
Relinquish control.

The comando altura é uma altura equal to 1/3 inch.

A comando é uma coisa usando
uma redonda caixa,
uma width,
uma cor, uma font, uma alignment,
um nome, uma shortcut, e uma handler.

Como create uma botão:
Allocate memória para o botão.

Como create uma comando:
Allocate memória para o comando.
Coloque o clear cor no comando's cor.
Coloque o default font no comando's font.
Coloque 1/12 inch no comando's redonda caixa's raio.
Coloque "left" no comando's alignment.

Como create o dialog:
Allocate memória para o dialog.
Coloque a cor preta no dialog's canetinha.

Como create um menu:
Allocate memória para o menu.
Coloque o big-uma pedaço dentro de uma pedaço.
Percorra.
Se o pedaço is greater than o big-z pedaço, pare.
Coloque o pedaço dentro de um texto.
Adicione o texto e o texto to o menu.
Coloque "center" no menu's comandos' last's alignment.
Adicione 1 to o pedaço.
Repita.
Adicione "*" e "*" to o menu. \ special menu
Coloque "center" no menu's comandos' last's alignment. \ special menu
Coloque o barra's display's left-parte de cima dentro de um local. \ special menu
\ adjust o menu usando o local e 26 times o swatch width. \ no special menu
Adjust o menu usando o local e 27 times o swatch width. \ special menu

Como create uma submenu:
Allocate memória para o submenu.

Como create uma aba:
Allocate memória para a aba.

The current menu é um menu.

The current process é uma process.

The current aba é uma aba.

Como decidir se uma event is ctrl-aba:
Se o event não existir, diga não.
Se o event's kind is not "tecla down", diga não.
Se o event's tecla is not a aba tecla, diga não.
Se o event's ctrl flag is not set, diga não.
Diga sim.

Como decidir se uma lista should be read-only:
Se o lista não existir, diga não.
Se o lista's kind is not "file", diga não.
Percorra.
Pegue uma aba usando a abas.
Se a aba não existir, diga não.
Se a aba's lista não existir, repeat.
Se a aba's lista is o lista, repeat.
Se a aba's lista's kind is not "file", repeat.
Se a aba's lista's endereço is o lista's endereço, diga sim.
Repita.

Como decidir se um local está dentro do barra:
Se o local está dentro do barra's caixa, diga sim.
Diga não.

Como decidir se um local está dentro de uma botão:
Se o botão não existir, diga não.
Se o local está dentro do botão's redonda caixa, diga sim.
Diga não.

Como decidir se um local está dentro de uma comando:
Se o comando não existir, diga não.
Se o local está dentro do comando's redonda caixa, diga sim.
Diga não.

Como decidir se um local está dentro de um menu:
Se o menu não existir, diga não.
Encontre uma comando usando o menu e o local.
Se o comando não existir, diga não.
Diga sim.

Como decidir se um local está dentro do pad:
Se o local está dentro do pad's caixa, diga sim.
Diga não.

Como decidir se um local está dentro do estado:
Se o barra's mode is not "normal", diga não.
Se o local está dentro do estado' redonda caixa, diga sim.
Diga não.

Como decidir se um local está dentro do switch:
Encontre uma aba usando o local.
Se a aba não existir, diga não.
Diga sim.

Como decidir se um local está dentro de uma aba:
Se a aba não existir, diga não.
Se o local está dentro da aba's caixa, diga sim.
Diga não.

Como decidir se uma aba is livre:
Se a aba não existir, diga não.
Se a aba's lista não existir, diga não.
Se a aba's lista's kind is not "root", diga não.
Diga sim.

Como decidir se o user is sure:
Faça "Are you quite sure?" usando choices "Sim" e "Não".
Se o resposta for "sim", diga sim.
Diga não.

Como decidir se o user is willing to close uma lista:
Se o lista não existir, diga sim.
Se o lista is not modified, diga sim.
Faça "Save?" usando choices "Sim" e "Não" e "Cancelar".
Se o resposta for "cancelar", diga não.
Se o resposta for "não", diga sim.
Save o lista.
Se o de entrada/saída error não estiver vazio, show error o de entrada/saída error; diga não.
Diga sim.

The ambiente cor é uma cor.

Como destruir uma aba:
Se a aba não existir, deixe quieto.
Destrua a aba's lista's root lista.
Deallocate a aba.

The dialog é uma coisa usando
uma kind [choice, error, input],
uma redonda caixa,
uma prompt caixa, uma prompt,
uma canetinha cor,
uma caixa de texto,
some botões.

Como desenhar o barra:
Unmask everycoisa.
Desenhe o barra's caixa usando a cor preta e a cor preta.
Desenhe o barra's caixa usando o ambiente cor e o ambiente cor e 1/8 inch (rounding o parte de cima).
Se o barra's mode for "dialog", draw o dialog, senão então deixa quieto.
Desenhe o menu.
Desenhe o estado.

Como desenhar uma caixa usando uma borda cor e uma fill cor e um raio (rounding o bottom):
Coloque o caixa e o raio dentro de uma redonda caixa.
Desenhe o redonda caixa usando a borda e o fill.
Privatize o caixa.
Coloque o caixa's altura dividido por 2 dentro de uma altura.
Coloque o caixa's parte de cima plus o altura no caixa's bottom.
Desenhe o caixa usando a borda e o clear cor.
Adicione o tpp to o caixa's bottom.
Desenhe o caixa usando o clear cor e o fill.

Como desenhar uma caixa usando uma borda cor e uma fill cor e um raio (rounding o parte de cima):
Coloque o caixa e o raio dentro de uma redonda caixa.
Desenhe o redonda caixa usando a borda e o fill.
Privatize o caixa.
Coloque o caixa's altura dividido por 2 dentro de uma altura.
Coloque o caixa's bottom minus o altura no caixa's parte de cima.
Desenhe o caixa usando a borda e o clear cor.
Subtract o tpp usando o caixa's parte de cima.
Desenhe o caixa usando o clear cor e o fill.

Como desenhar uma botão:
Se o botão não existir, deixe quieto.
Unmask everycoisa.
Desenhe o botão's redonda caixa usando a cor preta e a cor branca.
Mask outsido botão's redonda caixa.
Desenhe o botão's nome in o botão's redonda caixa usando a cor preta e o default font e "center".

Como desenhar some botões:
Pegue uma botão usando o botões.
Se o botão não existir, deixe quieto.
Desenhe o botão.
Repita.

Como desenhar uma comando:
Se o comando não existir, deixe quieto.
Unmask everycoisa.
Coloque o comando's cor dentro de uma cor.
Se o cor is clear, put a cor branca no cor.
Desenhe o comando's redonda caixa usando a cor preta e o cor.
Coloque o comando's redonda caixa dentro de uma caixa.
Adjust o caixa usando 1/8 inch e 0 e -1/8 inch e 0.
Mask outsido comando's redonda caixa. \ don't mask to o caixa, menu letters are too big to fit
Desenhe o comando's nome in o caixa usando a cor preta e o comando's font e o comando's alignment.
Desenhe o comando's shortcut at o right do caixa usando a cor preta e o comando's font.

Como desenhar some comandos:
Pegue uma comando usando o comandos.
Se o comando não existir, deixe quieto.
Desenhe o comando.
Repita.

Como desenhar o ambiente:
Desenhe o barra.
Desenhe o switch.
Desenhe o pad.
Desenhe o current menu.

Como desenhar o dialog:
Se o dialog não existir, deixe quieto.
Unmask everycoisa.
Desenhe o dialog's redonda caixa usando a cor preta e a cor branca.
Mask outsido dialog's redonda caixa.
Desenhe o dialog's prompt in o dialog's prompt caixa usando o dialog's canetinha e o default font e "left".
Desenhe o dialog's caixa de texto (editing).
Desenhe o dialog's botões.

Como desenhar um menu:
Se o menu não existir, deixe quieto.
Desenhe o menu's comandos.

Como desenhar o pad:
Mask only outsido pad's caixa.
Desenhe o pad's caixa usando o ambiente cor e o ambiente cor.
Se o current aba's mode for "finder", draw o current aba's lista, senão então deixa quieto.
Se o current aba's mode for "editor", draw o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "docster", draw o current aba's lista's documento, senão então deixa quieto.
Se o current aba's mode for "writer", draw o current aba's lista's page, senão então deixa quieto.

Como desenhar o estado:
Unmask everycoisa.
Desenhe o estado' redonda caixa usando o ambiente cor e o ambiente cor.
Desenhe o estado' redonda caixa usando a cor preta e a cor branca.
Coloque o estado' redonda caixa dentro de uma caixa.
Adjust o caixa usando 1/8 inch e 0 e -1/8 inch e 0.
Mask outsido caixa.
Desenhe o estado' texto in o caixa usando a cor preta e o default font e "right".

Como desenhar o switch:
Mask only outsido switch's caixa.
Desenhe o switch's caixa usando a cor preta e a cor preta.
Coloque o switch's caixa dentro de uma caixa.
Subtract 1/3 inch usando o caixa's bottom.
Desenhe o caixa usando o ambiente cor e o ambiente cor.
Desenhe a abas.

Como desenhar uma aba:
Se a aba não existir, deixe quieto.
Mask only outsida aba's caixa.
Desenhe a aba's caixa usando o ambiente cor e o ambiente cor e 1/8 inch (rounding o bottom).
Se a aba's previous is not nil, draw a aba's caixa's left line usando a cor preta.
Se a aba is not o current aba, draw a aba's caixa's parte de cima line usando a cor preta.
Desenhe a aba's nome in o center of a aba's caixa usando a cor preta e o default font.

Como desenhar some abas:
Pegue uma aba usando a abas.
Se a aba não existir, deixe quieto.
Desenhe a aba.
Repita.

Como fill some abas:
Pegue uma aba usando a abas.
Se a aba não existir, deixe quieto.
Coloque "finder" na aba's mode.
Create a aba's lista.
Ocanetinha a aba's lista.
Repita.

Como finalizar:
Destrua a abas.
Finalize o compiler.
Finalize o writer.
Finalize o editor.
Finalize o finder.
Finalize o ambiente.

Como finalizar o barra:
Finalize o estado.
Destrua o menu.

Como finalizar o ambiente:
Finalize o switch.
Finalize o pad.
Finalize o barra.

Como finalizar o pad:

Como finalizar o estado:

Como finalizar o switch:

Como encontrar uma botão usando uma tecla:
Esvazie o conteúdo do o botão.
Se o dialog não existir, deixe quieto.
Percorra.
Pegue o botão usando o dialog's botões.
Se o botão não existir, deixe quieto.
Se o botão's nome estiver em branco, repeat.
Coloque o botão's nome's primeiro's alvo dentro de uma pedaço.
Uppercase o pedaço.
Se o pedaço is o tecla, deixe quieto.
Repita.

Como encontrar uma botão usando um local:
Esvazie o conteúdo do o botão.
Se o dialog não existir, deixe quieto.
Percorra.
Pegue o botão usando o dialog's botões.
Se o botão não existir, deixe quieto.
Se o local está dentro do botão, deixe quieto.
Repita.

Como encontrar uma comando usando um menu e um local:
Esvazie o conteúdo do o comando.
Se o menu não existir, deixe quieto.
Percorra.
Pegue o comando usando o menu's comandos.
Se o comando não existir, deixe quieto.
Se o local está dentro do comando, deixe quieto.
Repita.

Como encontrar uma pasta para agrupar:
Mude para uma cor vermelha pasta.
Se o current aba's lista não existir, deixe quieto.
Se o current aba's lista's source não existir, deixe quieto.
Coloque o current aba's lista's pasta no pasta.

Como encontrar uma livre aba usando uma aba:
Privatize a aba.
Percorra.
Se a aba não existir, pare.
Se a aba is livre, pare.
Coloque a aba's próximo na aba.
Repita.
Coloque a aba no livre aba.

The find mode é um texto [find, check spelling, check spelling - entire documento, check spelling - current page only].

Como encontrar uma aba usando um local:
Esvazie o conteúdo do a aba.
Percorra.
Pegue a aba usando a abas.
Se a aba não existir, deixe quieto.
Se o local está dentro da aba, deixe quieto.
Repita.

Como format um texto to fit in uma caixa usando uma font:
Coloque o caixa's width minus 180 dentro de uma width.
Percorra.
Se o texto's length is less than 2, deixe quieto.
Pegue outro width usando o texto e o memória canvas e o font.
Se o outro width is less than o width, deixe quieto.
Remove o last pedaço usando o texto.
Remove o last pedaço usando o texto.
Posponha "…" to o texto.
Repita.

A livre aba é uma aba.

Como handle align usando uma alignment:
Se o current aba's mode for "writer", handle align usando o current aba's lista's page e o alignment, senão então deixa quieto.
Dê um latido.

Como handle borda e fill usando uma cor: \ special menu
Se o current aba's mode for "writer", handle borda e fill usando o current aba's lista's page e o cor, senão então deixa quieto. \ special menu
Dê um latido. \ special menu

Como handle borda usando uma cor:
Se o current aba's mode for "writer", handle borda usando o current aba's lista's page e o cor, senão então deixa quieto.
Dê um latido.

Como handle bring to front:
Se o current aba's mode for "writer", handle bring to front usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle capitalize:
Se o current aba's mode for "editor", handle capitalize usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "writer", handle capitalize usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle check spelling:
Se o current aba's mode for "editor", handle check spelling usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle check spelling (current page only):
Se o current aba's mode for "writer", handle check spelling usando o current aba's lista's page (current page only), senão então deixa quieto.
Dê um latido.

Como handle check spelling (entire documento):
Se o current aba's mode for "writer", handle check spelling usando o current aba's lista's page (entire documento), senão então deixa quieto.
Dê um latido.

Como handle close:
Se o current aba's mode for "writer", handle close (writer), senão então deixa quieto.
Handle close (outro).

Como handle close (outro):
Se o current aba's lista is root-level, cluck; deselect o elementos in o current aba's lista; show o ambiente usando estado, senão então deixa quieto.
Se o user is not willing to close o current aba's lista, deixe quieto.
Show estado "Closing...".
Close o current aba's lista.
Switch o current aba to o current aba's lista's parent lista.
Show o ambiente usando estado.
Refresh o cursor.

Como handle close (writer):
Handle close usandoout drawing (writer).
Show o ambiente usando estado.
Refresh o cursor.

Como handle close all:
Show estado "Closing...".
Pegue uma aba usando a abas (backwards).
Se a aba não existir, pare.
Switch to a aba.
Se a aba's lista is modified, show o ambiente.
Se o user is not willing to close a aba's lista, pare.
Collapse a aba's lista to root-level.
Deselect o elementos in a aba's lista.
Switch a aba to a aba's lista.
Repita.
Show o ambiente usando estado.

Como handle close usandoout drawing (writer):
Close o current aba's lista's page.
Esvazie o conteúdo do o current aba's lista's page.
Switch o current aba to o current aba's lista.

Como handle uma comando:
Se o comando não existir, deixe quieto.
\ offer it dispatchers
Se o comando's handler for "uma", handle o comando (uma), senão então deixa quieto.
Se o comando's handler for "alignment...", handle o comando (alignment...), senão então deixa quieto.
Se o comando's handler for "b", handle o comando (b), senão então deixa quieto.
Se o comando's handler for "borda...", handle o comando (borda...), senão então deixa quieto.
Se o comando's handler for "c", handle o comando (c), senão então deixa quieto.
Se o comando's handler for "check spelling...", handle o comando (check spelling...), senão então deixa quieto.
Se o comando's handler for "cor...", handle o comando (cor...), senão então deixa quieto.
Se o comando's handler for "d", handle o comando (d), senão então deixa quieto.
Se o comando's handler for "e", handle o comando (e), senão então deixa quieto.
Se o comando's handler for "f", handle o comando (f), senão então deixa quieto.
Se o comando's handler for "fill...", handle o comando (fill...), senão então deixa quieto.
Se o comando's handler for "font altura...", handle o comando (font altura...), senão então deixa quieto.
Se o comando's handler for "g", handle o comando (g), senão então deixa quieto.
Se o comando's handler for "grid...", handle o comando (grid...), senão então deixa quieto.
Se o comando's handler for "h", handle o comando (h), senão então deixa quieto.
Se o comando's handler for "i", handle o comando (i), senão então deixa quieto.
Se o comando's handler for "j", handle o comando (j), senão então deixa quieto.
Se o comando's handler for "k", handle o comando (k), senão então deixa quieto.
Se o comando's handler for "l", handle o comando (l), senão então deixa quieto.
Se o comando's handler for "label...", handle o comando (label...), senão então deixa quieto.
Se o comando's handler for "m", handle o comando (m), senão então deixa quieto.
Se o comando's handler for "n", handle o comando (n), senão então deixa quieto.
Se o comando's handler for "o", handle o comando (o), senão então deixa quieto.
Se o comando's handler for "p", handle o comando (p), senão então deixa quieto.
Se o comando's handler for "canetinha...", handle o comando (canetinha...), senão então deixa quieto.
Se o comando's handler for "q", handle o comando (q), senão então deixa quieto.
Se o comando's handler for "r", handle o comando (r), senão então deixa quieto.
Se o comando's handler for "s", handle o comando (s), senão então deixa quieto.
Se o comando's handler for "sort...", handle o comando (sort...), senão então deixa quieto.
Se o comando's handler for "t", handle o comando (t), senão então deixa quieto.
Se o comando's handler for "u", handle o comando (u), senão então deixa quieto.
Se o comando's handler for "v", handle o comando (v), senão então deixa quieto.
Se o comando's handler for "w", handle o comando (w), senão então deixa quieto.
Se o comando's handler for "x", handle o comando (x), senão então deixa quieto.
Se o comando's handler for "y", handle o comando (y), senão então deixa quieto.
Se o comando's handler for "z", handle o comando (z), senão então deixa quieto.
Se o comando's handler is "*", handle o comando (*), senão então deixa quieto. \ special menu
\ do it dispatchers
Show o ambiente.
Se o comando's handler for "handle bring to front", handle bring to front, senão então deixa quieto.
Se o comando's handler for "handle align usando center", handle align usando "center", senão então deixa quieto.
Se o comando's handler for "handle align usando left", handle align usando "left", senão então deixa quieto.
Se o comando's handler for "handle align usando right", handle align usando "right", senão então deixa quieto.
Se o comando's handler for "handle borda", handle borda usando o comando's cor, senão então deixa quieto.
Se o comando's handler for "handle borda e fill", handle borda e fill usando o comando's cor, senão então deixa quieto. \ special menu
Se o comando's handler for "handle fill", handle fill usando o comando's cor, senão então deixa quieto.
Se o comando's handler for "handle font altura usando 1 inch", handle font altura usando 1 inch, senão então deixa quieto.
Se o comando's handler for "handle font altura usando 1/2 inch", handle font altura usando 1/2 inch, senão então deixa quieto.
Se o comando's handler for "handle font altura usando 1/3 inch", handle font altura usando 1/3 inch, senão então deixa quieto.
Se o comando's handler for "handle font altura usando 1/4 inch", handle font altura usando 1/4 inch, senão então deixa quieto.
Se o comando's handler for "handle font altura usando 1/5 inch", handle font altura usando 1/5 inch, senão então deixa quieto.
Se o comando's handler for "handle font altura usando 1/6 inch", handle font altura usando 1/6 inch, senão então deixa quieto.
Se o comando's handler for "handle font altura usando 1/8 inch", handle font altura usando 1/8 inch, senão então deixa quieto.
Se o comando's handler for "handle font altura outro", handle font altura outro, senão então deixa quieto.
Se o comando's handler for "handle font nome", handle font nome, senão então deixa quieto.
Se o comando's handler for "handle raio", handle raio, senão então deixa quieto.
Se o comando's handler for "handle canetinha", handle canetinha usando o comando's cor, senão então deixa quieto.
Se o comando's handler for "handle capitalize", handle capitalize, senão então deixa quieto.
Se o comando's handler for "handle check spelling", handle check spelling, senão então deixa quieto.
Se o comando's handler for "handle check spelling (entire documento)", handle check spelling (entire documento), senão então deixa quieto.
Se o comando's handler for "handle check spelling (current page only)", handle check spelling (current page only), senão então deixa quieto.
Se o comando's handler for "handle close", handle close, senão então deixa quieto.
Se o comando's handler for "handle close all", handle close all, senão então deixa quieto.
Se o comando's handler for "handle comment", handle comment, senão então deixa quieto.
Se o comando's handler for "handle compile", handle compile, senão então deixa quieto.
Se o comando's handler for "handle copy", handle copy, senão então deixa quieto.
Se o comando's handler for "handle cut", handle cut, senão então deixa quieto.
Se o comando's handler for "handle decrypt", handle decrypt, senão então deixa quieto.
Se o comando's handler for "handle duplicate", handle duplicate, senão então deixa quieto.
Se o comando's handler for "handle enlarge", handle enlarge, senão então deixa quieto.
Se o comando's handler for "handle encrypt", handle encrypt, senão então deixa quieto.
Se o comando's handler for "handle extract globals", handle extract globals, senão então deixa quieto.
Se o comando's handler for "handle extract tarefa títulos", handle extract tarefa títulos, senão então deixa quieto.
Se o comando's handler for "handle extract tipos", handle extract tipos, senão então deixa quieto.
Se o comando's handler for "handle extract nibble literal", handle extract nibble literal, senão então deixa quieto.
Se o comando's handler for "handle find", handle find, senão então deixa quieto.
Se o comando's handler for "handle find próximo", handle find próximo, senão então deixa quieto.
Se o comando's handler for "handle flip", handle flip, senão então deixa quieto.
Se o comando's handler for "handle get tamanho", handle get tamanho, senão então deixa quieto.
Se o comando's handler for "handle grayscale", handle grayscale usando sim, senão então deixa quieto.
Se o comando's handler for "handle group", handle group, senão então deixa quieto.
Se o comando's handler for "handle grid usando no", handle grid usando no, senão então deixa quieto.
Se o comando's handler for "handle grid usando sim", handle grid usando sim, senão então deixa quieto.
Se o comando's handler for "handle indent", handle indent, senão então deixa quieto.
Se o comando's handler for "handle label usando title", handle label usando "title", senão então deixa quieto.
Se o comando's handler for "handle label usando page número", handle label usando "page número", senão então deixa quieto.
Se o comando's handler for "handle label usando page número (even)", handle label usando "page número (even)", senão então deixa quieto.
Se o comando's handler for "handle label usando page número (odd)", handle label usando "page número (odd)", senão então deixa quieto.
Se o comando's handler for "handle list", handle list, senão então deixa quieto.
Se o comando's handler for "handle lowercase", handle lowercase, senão então deixa quieto.
Se o comando's handler for "handle minimize", handle minimize, senão então deixa quieto.
Se o comando's handler for "handle mirror", handle mirror, senão então deixa quieto.
Se o comando's handler for "handle new pasta", handle new pasta, senão então deixa quieto.
Se o comando's handler for "handle new documento", handle new documento, senão então deixa quieto.
Se o comando's handler for "handle new ellipse", handle new shape usando "ellipse", senão então deixa quieto.
Se o comando's handler for "handle new page", handle new page, senão então deixa quieto.
Se o comando's handler for "handle new figura", handle new shape usando "figura", senão então deixa quieto.
Se o comando's handler for "handle new polygon", handle new shape usando "polygon", senão então deixa quieto.
Se o comando's handler for "handle new rectangle", handle new shape usando "rectangle", senão então deixa quieto.
Se o comando's handler for "handle new caixa de texto", handle new shape usando "caixa de texto", senão então deixa quieto.
Se o comando's handler for "handle new caixa de texto file", handle new caixa de texto file, senão então deixa quieto.
Se o comando's handler for "handle ocanetinha", handle ocanetinha, senão então deixa quieto.
Se o comando's handler for "handle ocanetinha as dump", handle ocanetinha as dump, senão então deixa quieto.
Se o comando's handler for "handle ocanetinha as caixa de texto", handle ocanetinha as caixa de texto, senão então deixa quieto.
Se o comando's handler for "handle outdent", handle outdent, senão então deixa quieto.
Se o comando's handler for "handle paste", handle paste, senão então deixa quieto.
Se o comando's handler for "handle print", handle print, senão então deixa quieto.
Se o comando's handler for "handle quit", handle quit, senão então deixa quieto.
Se o comando's handler for "handle redo", handle redo, senão então deixa quieto.
Se o comando's handler for "handle reduce", handle reduce, senão então deixa quieto.
Se o comando's handler for "handle renome", handle renome, senão então deixa quieto.
Se o comando's handler for "handle renúmero pages", handle renúmero pages, senão então deixa quieto.
Se o comando's handler for "handle reverse", handle reverse, senão então deixa quieto.
Se o comando's handler for "handle rotate page", handle rotate page, senão então deixa quieto.
Se o comando's handler for "handle rotate shape", handle rotate shape, senão então deixa quieto.
Se o comando's handler for "handle run", handle run, senão então deixa quieto.
Se o comando's handler for "handle save", handle save, senão então deixa quieto.
Se o comando's handler for "handle save all", handle save all, senão então deixa quieto.
Se o comando's handler for "handle save as pdf", handle save as pdf, senão então deixa quieto.
Se o comando's handler for "handle select all", handle select all, senão então deixa quieto.
Se o comando's handler for "handle send to back", handle send to back, senão então deixa quieto.
Se o comando's handler for "handle measure", handle measure, senão então deixa quieto.
Se o comando's handler for "handle smooth", handle smooth, senão então deixa quieto.
Se o comando's handler for "handle yank usando 1/8 inch", handle yank usando 1/8 inch, senão então deixa quieto.
Se o comando's handler for "handle yank usando 1/12 inch", handle yank usando 1/12 inch, senão então deixa quieto.
Se o comando's handler for "handle yank usando 1/16 inch", handle yank usando 1/16 inch, senão então deixa quieto.
Se o comando's handler for "handle yank usando 1/24 inch", handle yank usando 1/24 inch, senão então deixa quieto.
Se o comando's handler for "handle yank usando 1/32 inch", handle yank usando 1/32 inch, senão então deixa quieto.
Se o comando's handler for "handle yank usando 1/48 inch", handle yank usando 1/48 inch, senão então deixa quieto.
Se o comando's handler for "handle yank usando o tpp", handle yank usando o tpp, senão então deixa quieto.
Se o comando's handler for "handle sort definitions", handle sort definitions, senão então deixa quieto.
Se o comando's handler for "handle sort selected lines", handle sort selected lines, senão então deixa quieto.
Se o comando's handler for "handle sort elementos by extension", handle sort elementos usando "extension", senão então deixa quieto.
Se o comando's handler for "handle sort elementos by kind", handle sort elementos usando "kind", senão então deixa quieto.
Se o comando's handler for "handle sort elementos by nome", handle sort elementos usando "nome", senão então deixa quieto.
Se o comando's handler for "handle sparte de cima", handle sparte de cima, senão então deixa quieto.
Se o comando's handler for "handle uncomment", handle uncomment, senão então deixa quieto.
Se o comando's handler for "handle undo", handle undo, senão então deixa quieto.
Se o comando's handler for "handle ungrayscale", handle grayscale usando no, senão então deixa quieto.
Se o comando's handler for "handle ungroup", handle ungroup, senão então deixa quieto.
Se o comando's handler for "handle unlabel", handle unlabel, senão então deixa quieto.
Se o comando's handler for "handle uppercase", handle uppercase, senão então deixa quieto.
Se o comando's handler for "handle version", handle version, senão então deixa quieto.
\ not properly dispatched
Debug "Internal error 763 - comando not properly dispatched".

Como handle uma comando (*): \ special menu
Create uma submenu. \ special menu
Adicione swatches to o submenu usando "handle borda e fill". \ special menu
Handle o comando usando o submenu e o palette width. \ special menu

Como handle uma comando (uma):
Create uma submenu.
Se o current aba's mode for "writer", adicione "Align Left" e "handle align usando left" to o submenu.
Se o current aba's mode for "writer", adicione "Align Center" e "handle align usando center" to o submenu.
Se o current aba's mode for "writer", adicione "Align Right" e "handle align usando right" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (alignment...):
Create uma submenu.
Adicione "Align Left" e "handle align usando left" to o submenu.
Adicione "Align Center" e "handle align usando center" to o submenu.
Adicione "Align Right" e "handle align usando right" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (b):
Create uma submenu.
Se o current aba's mode for "writer", adicione "Border..." e "borda..." to o submenu.
Se o current aba's mode for "writer", adicione "Bring to Front" e "handle bring to front" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (borda...):
Create uma submenu.
Adicione swatches to o submenu usando "handle borda".
Handle o comando usando o submenu e o palette width.

Como handle uma comando (c):
Create uma submenu.
Se o current aba's mode for "editor", adicione "Capitalize" e "handle capitalize" to o submenu.
Se o current aba's mode for "writer", adicione "Capitalize" e "handle capitalize" to o submenu.
Se o current aba's mode for "editor", adicione "Check Spelling" e "handle check spelling" to o submenu.
Se o current aba's mode for "writer", adicione "Check Spelling..." e "check spelling..." to o submenu.
Adicione "Close" e "W" e "handle close" to o submenu.
Adicione "Close All" e "handle close all" to o submenu.
Se o current aba's mode for "writer", adicione "Color..." e "cor..." to o submenu.
Se o current aba's mode for "editor", adicione "Comment" e "T" e "handle comment" to o submenu.
Se o current aba's mode for "editor", adicione "Compile" e "M" e "handle compile" to o submenu.
Adicione "Copy" e "C" e "handle copy" to o submenu.
Adicione "Cut" e "X" e "handle cut" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (check spelling...):
Create uma submenu.
Adicione "Current Page Only" e "handle check spelling (current page only)" to o submenu.
Adicione "Entire Document" e "handle check spelling (entire documento)" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (cor...):
Create uma submenu.
Adicione "Border..." e "borda..." to o submenu.
Adicione "Fill..." e "fill..." to o submenu.
Adicione "Pen..." e "canetinha..." to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (d):
Create uma submenu.
Se o current aba's mode for "editor", adicione "Decrypt..." e "handle decrypt" to o submenu.
Se o current aba's mode for "finder", adicione "Duplicate" e "D" e "handle duplicate" to o submenu.
Se o current aba's mode for "docster", adicione "Duplicate" e "D" e "handle duplicate" to o submenu.
Se o current aba's mode for "writer", adicione "Duplicate" e "D" e "handle duplicate" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (e):
Create uma submenu.
Se o current aba's mode for "writer", adicione "Enlarge" e "E" e "handle enlarge" to o submenu.
Se o current aba's mode for "editor", adicione "Encrypt..." e "handle encrypt" to o submenu.
Se o current aba's mode for "editor", adicione "Extract Types" e "handle extract tipos" to o submenu.
Se o current aba's mode for "editor", adicione "Extract Globals" e "handle extract globals" to o submenu.
Se o current aba's mode for "editor", adicione "Extract Routine títulos" e "handle extract tarefa títulos" to o submenu.
Se o current aba's mode for "finder", adicione "Extract Nibble Literal" e "handle extract nibble literal" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (f):
Create uma submenu.
Se o current aba's mode for "editor", adicione "Find" e "F" e "handle find" to o submenu.
Se o current aba's mode for "writer", adicione "Find" e "F" e "handle find" to o submenu.
Se o current aba's mode for "editor", adicione "Encontre Next" e "N" e "handle find próximo" to o submenu.
Se o current aba's mode for "writer", adicione "Encontre Next" e "N" e "handle find próximo" to o submenu.
Se o current aba's mode for "writer", adicione "Fill..." e "fill..." to o submenu.
Se o current aba's mode for "writer", adicione "Flip" e "handle flip" to o submenu.
Se o current aba's mode for "editor", adicione "Font Height..." e "font altura..." to o submenu.
Se o current aba's mode for "editor", adicione "Font Name..." e "handle font nome" to o submenu.
Se o current aba's mode for "writer", adicione "Font Height..." e "font altura..." to o submenu.
Se o current aba's mode for "writer", adicione "Font Name..." e "handle font nome" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (fill...):
Create uma submenu.
Adicione swatches to o submenu usando "handle fill".
Handle o comando usando o submenu e o palette width.

Como handle uma comando (font altura...):
Create uma submenu.
Adicione "1 Inch" e "handle font altura usando 1 inch" to o submenu.
Adicione "1/2 Inch" e "handle font altura usando 1/2 inch" to o submenu.
Adicione "1/3 Inch" e "handle font altura usando 1/3 inch" to o submenu.
Adicione "1/4 Inch" e "handle font altura usando 1/4 inch" to o submenu.
Adicione "1/5 Inch" e "handle font altura usando 1/5 inch" to o submenu.
Adicione "1/6 Inch" e "handle font altura usando 1/6 inch" to o submenu.
Adicione "1/8 Inch" e "handle font altura usando 1/8 inch" to o submenu.
Adicione "Other..." e "handle font altura outro" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (g):
Create uma submenu.
Se o current aba's mode for "finder", adicione "Pegue Size" e "handle get tamanho" to o submenu.
Se o current aba's mode for "writer", adicione "Grayscale" e "handle grayscale" to o submenu.
Se o current aba's mode for "writer", adicione "Grid..." e "grid..." to o submenu.
Se o current aba's mode for "writer", adicione "Group" e "G" e "handle group" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (grid...):
Create uma submenu.
Adicione "Hide" e "handle grid usando no" to o submenu.
Adicione "Show" e "handle grid usando sim" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (h):
Create uma submenu.
Se o current aba's mode for "writer", adicione "Hide Grid" e "handle grid usando no" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (i):
Create uma submenu.
Se o current aba's mode for "editor", adicione "Indent" e "I" e "handle indent" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (j):

Como handle uma comando (k):

Como handle uma comando (l):
Create uma submenu.
Se o current aba's mode for "editor", adicione "List" e "L" e "handle list" to o submenu.
Se o current aba's mode for "editor", adicione "Lowercase" e "handle lowercase" to o submenu.
Se o current aba's mode for "writer", adicione "Label..." e "Label..." to o submenu.
Se o current aba's mode for "writer", adicione "Lowercase" e "handle lowercase" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (label...):
Create uma submenu.
Adicione "Title" e "handle label usando title" to o submenu.
Adicione "Page Number" e "handle label usando page número" to o submenu.
Adicione "Page Number (Even)" e "handle label usando page número (even)" to o submenu.
Adicione "Page Number (Odd)" e "handle label usando page número (odd)" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (m):
Create uma submenu.
Adicione "Minimize" e "handle minimize" to o submenu.
Se o current aba's mode for "writer", adicione "Measure..." e "handle measure" to o submenu.
Se o current aba's mode for "writer", adicione "Mirror" e "handle mirror" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (n):
Create uma submenu.
Se o current aba's mode for "finder", adicione "New Directory..." e "handle new pasta" to o submenu.
Se o current aba's mode for "finder", adicione "New Document..." e "handle new documento" to o submenu.
Se o current aba's mode for "writer", adicione "New Ellipse" e "handle new ellipse" to o submenu.
Se o current aba's mode for "docster", adicione "New Page" e "handle new page" to o submenu.
Se o current aba's mode for "writer", adicione "New Picture..." e "handle new figura" to o submenu.
Se o current aba's mode for "writer", adicione "New Polygon" e "handle new polygon" to o submenu.
Se o current aba's mode for "writer", adicione "New Rectangle" e "handle new rectangle" to o submenu.
Se o current aba's mode for "writer", adicione "New Text" e "handle new caixa de texto" to o submenu.
Se o current aba's mode for "finder", adicione "New Text File..." e "handle new caixa de texto file" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (o):
Create uma submenu.
Se o current aba's mode for "finder", adicione "Ocanetinha" e "O" e "handle ocanetinha" to o submenu.
Se o current aba's mode for "docster", adicione "Ocanetinha" e "O" e "handle ocanetinha" to o submenu.
Se o current aba's mode for "finder", adicione "Ocanetinha As Dump" e "handle ocanetinha as dump" to o submenu.
Se o current aba's mode for "finder", adicione "Ocanetinha As Text" e "handle ocanetinha as caixa de texto" to o submenu.
Se o current aba's mode for "editor", adicione "Outdent" e "O" e "handle outdent" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (p):
Create uma submenu.
Adicione "Paste" e "V" e "handle paste" to o submenu.
Se o current aba's mode for "writer", adicione "Pen..." e "canetinha..." to o submenu.
Se o current aba's mode for "editor", adicione "Print..." e "P" e "handle print" to o submenu.
Se o current aba's mode for "docster", adicione "Print..." e "P" e "handle print" to o submenu.
Se o current aba's mode for "writer", adicione "Print..." e "P" e "handle print" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (canetinha...):
Create uma submenu.
Adicione swatches to o submenu usando "handle canetinha".
Handle o comando usando o submenu e o palette width.

Como handle uma comando (q):
Create uma submenu.
Adicione "Quit" e "Q" e "handle quit" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (r):
Create uma submenu.
Se o current aba's mode for "writer", adicione "Radius..." e "handle raio" to o submenu.
Se o current aba's mode for "editor", adicione "Redo" e "Y" e "handle redo" to o submenu.
Se o current aba's mode for "writer", adicione "Redo" e "Y" e "handle redo" to o submenu.
Se o current aba's mode for "writer", adicione "Reduce" e "R" e "handle reduce" to o submenu.
Se o current aba's mode for "finder", adicione "Renome..." e "handle renome" to o submenu.
Se o current aba's mode for "docster", adicione "Renúmero Pages..." e "handle renúmero pages" to o submenu.
Se o current aba's mode for "editor", adicione "Reverse" e "handle reverse" to o submenu.
Se o current aba's mode for "finder", adicione "Reverse" e "handle reverse" to o submenu.
Se o current aba's mode for "writer", adicione "Rotate Page" e "handle rotate page" to o submenu.
Se o current aba's mode for "writer", adicione "Rotate Shape" e "J" e "handle rotate shape" to o submenu.
Se o current aba's mode for "finder", adicione "Run" e "R" e "handle run" to o submenu.
Se o current aba's mode for "editor", adicione "Run" e "R" e "handle run" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (s):
Create uma submenu.
Se o current aba's mode for "editor", adicione "Save" e "S" e "handle save" to o submenu.
Se o current aba's mode for "docster", adicione "Save" e "S" e "handle save" to o submenu.
Se o current aba's mode for "writer", adicione "Save" e "S" e "handle save" to o submenu.
Adicione "Save All" e "handle save all" to o submenu.
Se o current aba's mode for "docster", adicione "Save as PDF..." e "handle save as pdf" to o submenu.
Adicione "Select All" e "A" e "handle select all" to o submenu.
Se o current aba's mode for "writer", adicione "Send to Back" e "handle send to back" to o submenu.
Se o current aba's mode for "writer", adicione "Show Grid" e "handle grid usando sim" to o submenu.
Se o current aba's mode for "writer", adicione "Smooth" e "M" e "handle smooth" to o submenu.
Se o current aba's mode for "editor", adicione "Sort..." e "sort..." to o submenu.
Se o current aba's mode for "finder", adicione "Sort..." e "sort..." to o submenu.
Se o current aba's mode for "editor", adicione "Sparte de cima" e "handle sparte de cima" to o submenu.
Se o current aba's mode for "finder", adicione "Sparte de cima" e "handle sparte de cima" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (sort...):
Create uma submenu.
Se o current aba's mode for "editor", adicione "Definitions" e "handle sort definitions" to o submenu.
Se o current aba's mode for "editor", adicione "Selected Lines" e "handle sort selected lines" to o submenu.
Se o current aba's mode for "finder", adicione "By Kind" e "handle sort elementos by kind" to o submenu.
Se o current aba's mode for "finder", adicione "By Name" e "handle sort elementos by nome" to o submenu.
Se o current aba's mode for "finder", adicione "By Extension" e "handle sort elementos by extension" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (t):

Como handle uma comando (u):
Create uma submenu.
Se o current aba's mode for "editor", adicione "Uncomment" e "U" e "handle uncomment" to o submenu.
Se o current aba's mode for "editor", adicione "Undo" e "Z" e "handle undo" to o submenu.
Se o current aba's mode for "writer", adicione "Undo" e "Z" e "handle undo" to o submenu.
Se o current aba's mode for "writer", adicione "Ungrayscale" e "handle ungrayscale" to o submenu.
Se o current aba's mode for "writer", adicione "Ungroup" e "U" e "handle ungroup" to o submenu.
Se o current aba's mode for "writer", adicione "Unlabel" e "handle unlabel" to o submenu.
Se o current aba's mode for "editor", adicione "Uppercase" e "handle uppercase" to o submenu.
Se o current aba's mode for "writer", adicione "Uppercase" e "handle uppercase" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (v):
Create uma submenu.
Adicione "Version" e "handle version" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (w):

Como handle uma comando (x):

Como handle uma comando (y):
Create uma submenu.
Se o current aba's mode for "writer", adicione "Yank 1/8" e "handle yank usando 1/8 inch" to o submenu.
Se o current aba's mode for "writer", adicione "Yank 1/12" e "handle yank usando 1/12 inch" to o submenu.
Se o current aba's mode for "writer", adicione "Yank 1/16" e "handle yank usando 1/16 inch" to o submenu.
Se o current aba's mode for "writer", adicione "Yank 1/24" e "handle yank usando 1/24 inch" to o submenu.
Se o current aba's mode for "writer", adicione "Yank 1/32" e "handle yank usando 1/32 inch" to o submenu.
Se o current aba's mode for "writer", adicione "Yank 1/48" e "handle yank usando 1/48 inch" to o submenu.
Se o current aba's mode for "writer", adicione "Yank Nãone" e "handle yank usando o tpp" to o submenu.
Handle o comando usando o submenu.

Como handle uma comando (z):

Como handle uma comando usando uma submenu: handle o comando usando o submenu e o submenu width.

Como handle uma comando usando uma submenu e uma width:
Coloque o comando's left e o barra's display's bottom dentro de um local.
Adjust o submenu usando o local e o width.
Flush all events.
Esvazie o conteúdo do o selected comando.
Coloque o submenu no current menu.
Show o ambiente.
Handle events usando o submenu.
Esvazie o conteúdo do o current menu.
Handle o selected comando.
Destrua o submenu.
Show o ambiente.
Refresh o cursor.

Como handle comment:
Se o current aba's mode for "editor", handle comment usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle compile:
Se o current aba's mode is not "editor", cluck, senão então deixa quieto.
Handle save all.
Se o de entrada/saída error não estiver vazio, deixe quieto.
Encontre uma pasta para agrupar.
Se o pasta estiver em branco, show error "I'm not sure what para agrupar.", senão então deixa quieto.
Agrupe o pasta.
Se o sinal da mensagem de erro estiver verde, handle compile or list or run error, senão então deixa quieto.
Show estado o do agrupador timer's texto then " ms".

Como handle compile or list or run error:
Se o o endereço contido uma mensagem de erro do agrupador estiver em branco, show error o mensagem de erro do compilador, senão então deixa quieto.
Percorra.
Pegue uma aba usando a abas.
Se a aba não existir, show error o mensagem de erro do compilador, senão então deixa quieto.
Se o o endereço contido uma mensagem de erro do agrupador is not a aba's lista's endereço, repeat.
Se a aba's lista's source não existir, show error o mensagem de erro do compilador, senão então deixa quieto.
Select o do agrupador abort row# usando a aba's lista's source's caixa de texto.
Scroll a aba's lista's source's caixa de texto to o caret e center it.
Switch to a aba.
Show o ambiente.
Show error o mensagem de erro do compilador.

Como handle copy:
Se o current aba's mode for "finder", handle copy usando o current aba's lista, senão então deixa quieto.
Se o current aba's mode for "editor", handle copy usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "docster", handle copy usando o current aba's lista's documento, senão então deixa quieto.
Se o current aba's mode for "writer", handle copy usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle cut:
Se o current aba's mode for "finder", handle cut usando o current aba's lista, senão então deixa quieto.
Se o current aba's mode for "editor", handle cut usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "docster", handle cut usando o current aba's lista's documento, senão então deixa quieto.
Se o current aba's mode for "writer", handle cut usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle decrypt:
Se o current aba's mode for "editor", handle decrypt usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle o dialog:
Se o dialog não existir, deixe quieto.
Flush all events.
Adjust o dialog.
Switch o barra to "dialog".
Handle events usando o dialog.
Switch o barra to "normal" usandoout drawing.
Se o resposta estiver em branco, show o ambiente.
Se o dialog's caixa de texto não existir, show o ambiente.
Flush all events.
Refresh o cursor.

Como handle duplicate:
Se o current aba's mode for "finder", handle duplicate usando o current aba's lista, senão então deixa quieto.
Se o current aba's mode for "docster", handle duplicate usando o current aba's lista's documento, senão então deixa quieto.
Se o current aba's mode for "writer", handle duplicate usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle encrypt:
Se o current aba's mode for "editor", handle encrypt usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle enlarge:
Se o current aba's mode for "writer", handle enlarge usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle uma event:
Se o event não existir, deixe quieto.
Se o event's kind for "tecla down", handle o event (tecla down), senão então deixa quieto.
Se o event's kind for "left click", handle o event (left click), senão então deixa quieto.
Se o event's kind for "left double click", handle o event (left double click), senão então deixa quieto.
Se o event's kind for "refresh", handle o event (refresh), senão então deixa quieto.
Se o event's kind for "right click", handle o event (right click), senão então deixa quieto.
Se o event's kind for "set cursor", handle o event (set cursor), senão então deixa quieto.

Como handle uma event (tecla down - ctrl-aba):
Se o event's shift flag estiver verde, switch to o predecessor do current aba.
Se o event's shift flag is not set, switch to o successor do current aba.
Show o ambiente usando estado.

Como handle uma event (tecla down - shortcut):
Se o event's tecla is o uma-tecla, handle select all, senão então deixa quieto.
Se o event's tecla is o c-tecla, handle copy, senão então deixa quieto.
Se o event's tecla is o d-tecla, handle duplicate, senão então deixa quieto.
Se o event's tecla is o e-tecla, handle enlarge, senão então deixa quieto.
Se o event's tecla is o f-tecla, handle find, senão então deixa quieto.
Se o event's tecla is o g-tecla, handle group, senão então deixa quieto.
Se o event's tecla is o i-tecla, handle indent, senão então deixa quieto.
Se o event's tecla is o j-tecla, handle rotate shape, senão então deixa quieto.
Se o event's tecla is o l-tecla, handle list, senão então deixa quieto.
Se o event's tecla is o m-tecla e o current aba's mode for "editor", handle compile, senão então deixa quieto.
Se o event's tecla is o m-tecla e o current aba's mode for "writer", handle smooth, senão então deixa quieto.
Se o event's tecla is o n-tecla, handle find próximo, senão então deixa quieto.
Se o event's tecla is o o-tecla e o current aba's mode for "editor", handle outdent, senão então deixa quieto.
Se o event's tecla is o o-tecla, handle ocanetinha, senão então deixa quieto.
Se o event's tecla is o p-tecla, handle print, senão então deixa quieto.
Se o event's tecla is o q-tecla, handle quit, senão então deixa quieto.
Se o event's tecla is o r-tecla e o current aba's mode for "editor", handle run, senão então deixa quieto.
Se o event's tecla is o r-tecla e o current aba's mode for "finder", handle run, senão então deixa quieto.
Se o event's tecla is o r-tecla e o current aba's mode for "writer", handle reduce, senão então deixa quieto.
Se o event's tecla is o s-tecla, handle save, senão então deixa quieto.
Se o event's tecla is o t-tecla, handle comment, senão então deixa quieto.
Se o event's tecla is o u-tecla e o current aba's mode for "editor", handle uncomment, senão então deixa quieto.
Se o event's tecla is o u-tecla e o current aba's mode for "writer", handle ungroup, senão então deixa quieto.
Se o event's tecla is o v-tecla, handle paste, senão então deixa quieto.
Se o event's tecla is o w-tecla, handle close, senão então deixa quieto.
Se o event's tecla is o x-tecla, handle cut, senão então deixa quieto.
Se o event's tecla is o y-tecla, handle redo, senão então deixa quieto.
Se o event's tecla is o z-tecla, handle undo, senão então deixa quieto.
Dê um latido.

Como handle uma event (tecla down):
Se o event is any shortcut, handle o event (tecla down - shortcut), senão então deixa quieto.
Se o event is ctrl-aba, handle o event (tecla down - ctrl-aba), senão então deixa quieto.
Se o current aba's mode for "finder", handle o event usando o current aba's lista (tecla down), senão então deixa quieto.
Se o current aba's mode for "editor", handle o event usando o current aba's lista's source (tecla down), senão então deixa quieto.
Se o current aba's mode for "docster", handle o event usando o current aba's lista's documento (tecla down), senão então deixa quieto.
Se o current aba's mode for "writer", handle o event usando o current aba's lista's page (tecla down), senão então deixa quieto.

Como handle uma event (left click - menu):
Encontre uma comando usando o menu e o event's local.
Handle o comando.

Como handle uma event (left click - estado):
Track o ambiente usando o event.

Como handle uma event (left click - switch):
Encontre uma aba usando o event's local.
Se a aba não existir, deixe quieto.
Se a aba is o current aba, set uma flag.
Switch to a aba.
Show o ambiente usando estado.
Track a aba returning outro flag.
Se o outro flag estiver verde, deixe quieto.
Se o flag is not set, deixe quieto.
Handle close.

Como handle uma event (left click):
Se o event's local está dentro do menu, handle o event (left click - menu), senão então deixa quieto.
Se o event's local está dentro do estado, handle o event (left click - estado), senão então deixa quieto.
Se o event's local está dentro do switch, handle o event (left click - switch), senão então deixa quieto.
Se o current aba's mode for "finder", handle o event usando o current aba's lista (left click), senão então deixa quieto.
Se o current aba's mode for "editor", handle o event usando o current aba's lista's source (left click), senão então deixa quieto.
Se o current aba's mode for "docster", handle o event usando o current aba's lista's documento (left click), senão então deixa quieto.
Se o current aba's mode for "writer", handle o event usando o current aba's lista's page (left click), senão então deixa quieto.

Como handle uma event (left double click):
Se o event's local está dentro do barra, deixe quieto.
Se o event's local está dentro do switch, deixe quieto.
Se o current aba's mode for "finder", handle o event usando o current aba's lista (left double click), senão então deixa quieto.
Se o current aba's mode for "editor", handle o event usando o current aba's lista's source (left double click), senão então deixa quieto.
Se o current aba's mode for "docster", handle o event usando o current aba's lista's documento (left double click), senão então deixa quieto.
Se o current aba's mode for "writer", handle o event usando o current aba's lista's page (left double click), senão então deixa quieto.

Como handle uma event (refresh):
Show o ambiente usando estado.

Como handle uma event (right click):
Se o current aba's mode for "finder", handle o event usando o current aba's lista (right click), senão então deixa quieto.
Se o current aba's mode for "editor", handle o event usando o current aba's lista's source (right click), senão então deixa quieto.
Se o current aba's mode for "docster", handle o event usando o current aba's lista's documento (right click), senão então deixa quieto.
Se o current aba's mode for "writer", handle o event usando o current aba's lista's page (right click), senão então deixa quieto.

Como handle uma event (set cursor):
Se o current aba's mode for "finder", handle o event usando o current aba's lista (set cursor), senão então deixa quieto.
Se o current aba's mode for "editor", handle o event usando o current aba's lista's source (set cursor), senão então deixa quieto.
Se o current aba's mode for "docster", handle o event usando o current aba's lista's documento (set cursor), senão então deixa quieto.
Se o current aba's mode for "writer", handle o event usando o current aba's lista's page (set cursor), senão então deixa quieto.
Show o arrow cursor.

Como handle uma event usando o dialog:
Se o event's kind for "tecla down", handle o event usando o dialog (tecla down), senão então deixa quieto.
Se o event's kind for "left click", handle o event usando o dialog (left click), senão então deixa quieto.
Se o event's kind for "left double click", handle o event usando o dialog (left double click), senão então deixa quieto.
Se o event's kind for "refresh", handle o event usando o dialog (refresh), senão então deixa quieto.
Se o event's kind for "right click", handle o event usando o dialog (right click), senão então deixa quieto.
Se o event's kind for "set cursor", handle o event usando o dialog (set cursor), senão então deixa quieto.

Como handle uma event usando o dialog (copy):
If nada estiver selecionado in o dialog's caixa de texto, cluck, senão então deixa quieto.
Handle copy usando o dialog's caixa de texto.

Como handle uma event usando o dialog (cut):
If nada estiver selecionado in o dialog's caixa de texto, cluck, senão então deixa quieto.
Handle cut usando o dialog's caixa de texto.
Show o ambiente.

Como handle uma event usando o dialog (tecla down - backspace tecla):
Handle o event usando o dialog's caixa de texto (backspace tecla).
Show o ambiente.

Como handle uma event usando o dialog (tecla down - choice):
Se o event's tecla is o enter tecla, put "sim" no resposta; relinquish control, senão então deixa quieto.
Se o event's tecla is o escape tecla, put "cancelar" no resposta; relinquish control, senão então deixa quieto.
Encontre uma botão usando o event's tecla.
Se o botão não existir, deixe quieto.
Coloque o botão's nome no resposta.
Relinquish control.

Como handle uma event usando o dialog (tecla down - delete tecla):
Handle o event usando o dialog's caixa de texto (delete tecla).
Show o ambiente.

Como handle uma event usando o dialog (tecla down - end tecla):
Handle o event usando o dialog's caixa de texto (end tecla).
Show o ambiente.

Como handle uma event usando o dialog (tecla down - enter tecla):
Extract o resposta usando o dialog's caixa de texto.
Relinquish control.

Como handle uma event usando o dialog (tecla down - error):
Cancelar o dialog.

Como handle uma event usando o dialog (tecla down - escape tecla):
Cancelar o dialog.

Como handle uma event usando o dialog (tecla down - home tecla):
Handle o event usando o dialog's caixa de texto (home tecla).
Show o ambiente.

Como handle uma event usando o dialog (tecla down - input):
Se o event is any shortcut, handle o event usando o dialog (tecla down - shortcut), senão então deixa quieto.
Se o event's pedaço is prinabale, handle o event usando o dialog (tecla down - prinabale tecla), senão então deixa quieto.
Se o event's tecla is o backspace tecla, handle o event usando o dialog (tecla down - backspace tecla), senão então deixa quieto.
Se o event's tecla is o delete tecla, handle o event usando o dialog (tecla down - delete tecla), senão então deixa quieto.
Se o event's tecla is o end tecla, handle o event usando o dialog (tecla down - end tecla), senão então deixa quieto.
Se o event's tecla is o enter tecla, handle o event usando o dialog (tecla down - enter tecla), senão então deixa quieto.
Se o event's tecla is o escape tecla, handle o event usando o dialog (tecla down - escape tecla), senão então deixa quieto.
Se o event's tecla is o home tecla, handle o event usando o dialog (tecla down - home tecla), senão então deixa quieto.
Se o event's tecla is o left-arrow tecla, handle o event usando o dialog (tecla down - left-arrow tecla), senão então deixa quieto.
Se o event's tecla is o right-arrow tecla, handle o event usando o dialog (tecla down - right-arrow tecla), senão então deixa quieto.

Como handle uma event usando o dialog (tecla down - left-arrow tecla):
Handle o event usando o dialog's caixa de texto (left-arrow tecla).
Show o ambiente.

Como handle uma event usando o dialog (tecla down - prinabale tecla):
Handle o event usando o dialog's caixa de texto (prinabale tecla).
Show o ambiente.

Como handle uma event usando o dialog (tecla down - right-arrow tecla):
Handle o event usando o dialog's caixa de texto (right-arrow tecla).
Show o ambiente.

Como handle uma event usando o dialog (tecla down - shortcut):
Se o event's tecla is o uma-tecla, handle o event usando o dialog (select all).
Se o event's tecla is o c-tecla, handle o event usando o dialog (copy).
Se o event's tecla is o v-tecla, handle o event usando o dialog (paste).
Se o event's tecla is o x-tecla, handle o event usando o dialog (cut).
Se o event's tecla is o y-tecla, handle o event usando o dialog (redo).
Se o event's tecla is o z-tecla, handle o event usando o dialog (undo).

Como handle uma event usando o dialog (tecla down):
Se o dialog's kind for "choice", handle o event usando o dialog (tecla down - choice), senão então deixa quieto.
Se o dialog's kind for "error", handle o event usando o dialog (tecla down - error), senão então deixa quieto.
Se o dialog's kind for "input", handle o event usando o dialog (tecla down - input), senão então deixa quieto.

Como handle uma event usando o dialog (left click - choice):
Se o event's local is not in o barra, cancelar o dialog, senão então deixa quieto.
Encontre uma botão usando o event's local.
Se o botão não existir, deixe quieto.
Coloque o botão's nome no resposta.
Relinquish control.

Como handle uma event usando o dialog (left click - error):
Se o event's local is not in o barra, cancelar o dialog, senão então deixa quieto.
Encontre uma botão usando o event's local.
Se o botão não existir, deixe quieto.
Coloque o botão's nome no resposta.
Relinquish control.

Como handle uma event usando o dialog (left click - input):
Se o event's local is not in o barra, cancelar o dialog, senão então deixa quieto.
Se o event's local está dentro do dialog's caixa de texto, track o dialog usando o event, senão então deixa quieto.
Encontre uma botão usando o event's local.
Se o botão não existir, deixe quieto.
Se o botão's nome for "cancelar", cancelar o dialog, senão então deixa quieto.
Extract o resposta usando o dialog's caixa de texto.
Relinquish control.

Como handle uma event usando o dialog (left click):
Se o dialog's kind for "choice", handle o event usando o dialog (left click - choice), senão então deixa quieto.
Se o dialog's kind for "error", handle o event usando o dialog (left click - error), senão então deixa quieto.
Se o dialog's kind for "input", handle o event usando o dialog (left click - input), senão então deixa quieto.

Como handle uma event usando o dialog (left double click):
Se o dialog's caixa de texto não existir, deixe quieto.
Handle o event usando o dialog's caixa de texto (left double click).
Show o ambiente.

Como handle uma event usando o dialog (paste):
Se ore is not caixa de texto on o Área de Transferência do Windows, cluck, senão então deixa quieto.
Handle paste usando o dialog's caixa de texto.
Show o ambiente.

Como handle uma event usando o dialog (redo):
Se o dialog's caixa de texto cannot be redone, cluck, senão então deixa quieto.
Handle redo usando o dialog's caixa de texto.
Show o ambiente.

Como handle uma event usando o dialog (refresh):
Show o ambiente.

Como handle uma event usando o dialog (right click):
Se o event's local está dentro do barra, deixe quieto.
Cancelar o dialog.

Como handle uma event usando o dialog (select all):
Handle select all usando o dialog's caixa de texto.
Show o ambiente.

Como handle uma event usando o dialog (set cursor):
Se o event's local está dentro do dialog's caixa de texto, show o i-beam cursor, senão então deixa quieto.
Show o arrow cursor.

Como handle uma event usando o dialog (undo):
Se o dialog's caixa de texto cannot be undone, cluck, senão então deixa quieto.
Handle undo usando o dialog's caixa de texto.
Show o ambiente.

Como handle uma event usando uma submenu:
Se o event's kind for "tecla down", handle o event usando o submenu (tecla down), senão então deixa quieto.
Se o event's kind for "left click", handle o event usando o submenu (left click), senão então deixa quieto.
Se o event's kind for "refresh", handle o event usando o submenu (refresh), senão então deixa quieto.
Se o event's kind for "set cursor", handle o event usando o submenu (set cursor), senão então deixa quieto.

Como handle uma event usando uma submenu (tecla down):
Se o event's tecla is o escape tecla, relinquish control.

Como handle uma event usando uma submenu (left click - menu):
Encontre uma comando usando o submenu e o event's local.
Se o comando não existir, deixe quieto.
Coloque o comando no selected comando.
Relinquish control.

Como handle uma event usando uma submenu (left click):
Se o event's local está dentro do submenu, handle o event usando o submenu (left click - menu), senão então deixa quieto.
Relinquish control.
Se o event's local está dentro do menu, reque o event.

Como handle uma event usando uma submenu (refresh):
Show o ambiente.

Como handle uma event usando uma submenu (set cursor):
Show o arrow cursor.

Como handle events:
Deque uma event.
Se o event não existir, deixe quieto.
Handle o event.
Repita.

Como handle events usando o dialog:
Deque uma event.
Se o event não existir, deixe quieto.
Handle o event usando o dialog.
Repita.

Como handle events usando uma submenu:
Deque uma event.
Se o event não existir, deixe quieto.
Handle o event usando o submenu.
Repita.

Como handle extract globals:
Se o current aba's mode for "editor", handle extract globals usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle extract nibble literal:
Se o current aba's mode for "finder", handle extract nibble literal usando o current aba's lista, senão então deixa quieto.
Dê um latido.

Como handle extract tarefa títulos:
Se o current aba's mode for "editor", handle extract tarefa títulos usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle extract tipos:
Se o current aba's mode for "editor", handle extract tipos usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle fill usando uma cor:
Se o current aba's mode for "writer", handle fill usando o current aba's lista's page e o cor, senão então deixa quieto.
Dê um latido.

Como handle find:
Se o current aba's mode for "editor", handle find usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "writer", handle find usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle find próximo:
Se o current aba's mode for "editor", handle find próximo usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "writer", handle find próximo usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle flip:
Se o current aba's mode for "writer", handle flip usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle font altura usando uma font altura:
Se o current aba's mode for "editor", handle font altura usando o current aba's lista's source e o font altura, senão então deixa quieto.
Se o current aba's mode for "writer", handle font altura usando o current aba's lista's page e o font altura, senão então deixa quieto.
Dê um latido.

Como handle font altura outro:
Se o current aba's mode for "editor", handle font altura outro usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "writer", handle font altura outro usando o current aba's lista's page, senão então deixa quieto.

Como handle font nome:
Se o current aba's mode for "editor", handle font nome usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "writer", handle font nome usando o current aba's lista's page, senão então deixa quieto.

Como handle get tamanho:
Se o current aba's mode for "finder", handle get tamanho usando o current aba's lista, senão então deixa quieto.
Dê um latido.

Como handle grayscale usando uma flag:
Se o current aba's mode for "writer", handle grayscale usando o current aba's lista's page e o flag, senão então deixa quieto.
Dê um latido.

Como handle grid usando uma flag:
Se o current aba's mode for "writer", handle grid usando o current aba's lista's page e o flag, senão então deixa quieto.
Dê um latido.

Como handle group:
Se o current aba's mode for "writer", handle group usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle indent:
Se o current aba's mode for "editor", handle indent usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle label usando uma label:
Se o current aba's mode for "writer", handle label usando o current aba's lista's page e o label, senão então deixa quieto.
Dê um latido.

Como handle list:
Se o current aba's mode is not "editor", cluck, senão então deixa quieto.
Handle save all.
Se o de entrada/saída error não estiver vazio, deixe quieto.
Encontre uma pasta para agrupar.
Se o pasta estiver em branco, show error "I'm not sure what para agrupar e list", senão então deixa quieto.
Agrupe o pasta.
Show estado "Listing...".
List.
Se o sinal da mensagem de erro estiver verde, handle compile or list or run error, senão então deixa quieto.
Show estado o do agrupador timer's texto then " ms".

Como handle lowercase:
Se o current aba's mode for "editor", handle lowercase usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "writer", handle lowercase usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle measure:
Se o current aba's mode for "writer", handle measure usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle minimize:
Minimize o main window.

Como handle mirror:
Se o current aba's mode for "writer", handle mirror usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle new pasta:
Se o current aba's mode for "finder", handle new pasta usando o current aba's lista, senão então deixa quieto.
Dê um latido.

Como handle new documento:
Se o current aba's mode for "finder", handle new documento usando o current aba's lista, senão então deixa quieto.
Dê um latido.

Como handle new page:
Se o current aba's mode for "docster", handle new page usando o current aba's lista's documento, senão então deixa quieto.
Dê um latido.

Como handle new shape usando uma kind:
Se o current aba's mode for "writer", handle new shape usando o current aba's lista's page e o kind, senão então deixa quieto.
Dê um latido.

Como handle new caixa de texto file:
Se o current aba's mode for "finder", handle new caixa de texto file usando o current aba's lista, senão então deixa quieto.
Dê um latido.

Como handle ocanetinha:
Se o current aba's mode for "finder", handle ocanetinha (finder), senão então deixa quieto.
Se o current aba's mode for "docster", handle ocanetinha (docster), senão então deixa quieto.
Dê um latido.

Como handle ocanetinha (docster):
Handle ocanetinha usandoout drawing (docster).
Show o ambiente usando estado.
Flush o event fila.
Refresh o cursor.

Como handle ocanetinha (finder):
Se o current aba's mode is not "finder", cluck, senão então deixa quieto.
Pegue some selected endereço links usando o current aba's lista.
Se o selected endereço links are empty, show error "I'm not sure what you want me to ocanetinha.", senão então deixa quieto.
Show estado "Ocanetinhaing...".
Coloque o current aba dentro de uma aba.
Percorra.
Pegue uma endereço link usando o selected endereço links.
Se o endereço link não existir, pare.
Collapse a aba's lista to root-level.
Ocanetinha a aba's lista usando o endereço link's texto.
Switch a aba to a aba's lista.
Switch to a aba.
Se o de entrada/saída error não estiver vazio, show error o de entrada/saída error; pare.
Se a aba's lista should be read-only, set a aba's lista to read-only mode.
Encontre uma livre aba usando a aba's próximo.
Se o livre aba não existir, pare.
Coloque o livre aba na aba.
Repita.
Destrua o selected endereço links.
Show o ambiente usando estado.
Flush o event fila.
Refresh o cursor.

Como handle ocanetinha as dump:
Se o current aba's mode is not "finder", cluck, senão então deixa quieto.
Coloque o current aba's lista's primeiro selected elemento dentro de uma elemento.
Se o elemento não existir, show error "I'm not sure what you want me to ocanetinha.", senão então deixa quieto.
Se o elemento's kind is not "file", cluck, senão então deixa quieto.
Show estado "Ocanetinhaing...".
Ocanetinha o elemento (file as dump).
Se o de entrada/saída error não estiver vazio, show error o de entrada/saída error, senão então deixa quieto.
Switch o current aba to o elemento.
Show o ambiente usando estado.
Flush o event fila.

Como handle ocanetinha as caixa de texto:
Se o current aba's mode is not "finder", cluck, senão então deixa quieto.
Coloque o current aba's lista's primeiro selected elemento dentro de uma elemento.
Se o elemento não existir, show error "I'm not sure what you want me to ocanetinha.", senão então deixa quieto.
Se o elemento's kind is not "file", cluck, senão então deixa quieto.
Show estado "Ocanetinhaing...".
Ocanetinha o elemento (file as caixa de texto).
Se o de entrada/saída error não estiver vazio, show error o de entrada/saída error, senão então deixa quieto.
Switch o current aba to o elemento.
Show o ambiente usando estado.
Flush o event fila.

Como handle ocanetinha usandoout drawing (docster):
Coloque o current aba's lista's documento's primeiro selected page dentro de uma page.
Se o page não existir, show error "I'm not sure what you want me to ocanetinha.", senão então deixa quieto.
Focus on o page in o current aba's lista's documento.
Coloque o page no current aba's lista's page.
Ocanetinha o current aba's lista's page.
Switch o current aba to o current aba's lista.

Como handle outdent:
Se o current aba's mode for "editor", handle outdent usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle paste:
Se o current aba's mode for "finder", handle paste usando o current aba's lista, senão então deixa quieto.
Se o current aba's mode for "editor", handle paste usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "docster", handle paste usando o current aba's lista's documento, senão então deixa quieto.
Se o current aba's mode for "writer", handle paste usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle canetinha usando uma cor:
Se o current aba's mode for "writer", handle canetinha usando o current aba's lista's page e o cor, senão então deixa quieto.
Dê um latido.

Como handle print:
Se o current aba's mode for "editor", handle print usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "docster", handle print usando o current aba's lista's documento, senão então deixa quieto.
Se o current aba's mode for "writer", handle print usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle quit:
Se o user is not sure, deixe quieto.
Percorra.
Show estado "Quiting...".
Pegue uma aba usando a abas.
Se a aba não existir, relinquish control, senão então deixa quieto.
Se a aba's lista is not modified, repeat.
Switch to a aba.
Show o ambiente.
Se o user is not willing to close a aba's lista, deixe quieto.
Repita.

Como handle raio:
Se o current aba's mode for "writer", handle raio usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle redo:
Se o current aba's mode for "editor", handle redo usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "writer", handle redo usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle reduce:
Se o current aba's mode for "writer", handle reduce usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle renome:
Se o current aba's mode for "finder", handle renome usando o current aba's lista, senão então deixa quieto.
Dê um latido.

Como handle renúmero pages:
Se o current aba's mode for "docster", handle renúmero pages usando o current aba's lista's documento, senão então deixa quieto.
Dê um latido.

Como handle reverse:
Se o current aba's mode for "finder", handle reverse usando o current aba's lista, senão então deixa quieto.
Se o current aba's mode for "editor", handle reverse usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle rotate page:
Se o current aba's mode for "writer", handle rotate page usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle rotate shape:
Se o current aba's mode for "writer", handle rotate shape usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle run:
Se o current aba's mode for "finder", handle run usando o current aba's lista, senão então deixa quieto.
Se o current aba's mode is not "editor", cluck, senão então deixa quieto.
Handle save all.
Se o de entrada/saída error não estiver vazio, deixe quieto.
Encontre uma pasta para agrupar.
Se o pasta estiver em branco, show error "I'm not sure what you want me para agrupar.", senão então deixa quieto.
Agrupe o pasta.
Se o sinal da mensagem de erro estiver verde, handle compile or list or run error, senão então deixa quieto.
Show estado "Running...".
Start o current process usando o do agrupador exe endereço.
Se o de entrada/saída error não estiver vazio, show error o de entrada/saída error, senão então deixa quieto.
Show estado o do agrupador timer's texto then " ms".

Como handle save:
Se o current aba's lista is not savable, cluck; show estado "Unable to save", senão então deixa quieto.
Show estado "Saving...".
Save o current aba's lista.
Se o de entrada/saída error não estiver vazio, show error o de entrada/saída error, senão então deixa quieto.
Show estado "Saved".

Como handle save all:
Show estado "Saving...".
Coloque 0 dentro de uma count.
Mude para uma cor vermelha de entrada/saída error.
Percorra.
Pegue uma aba usando a abas.
Se a aba não existir, show estado o count e "file saved" or "files saved", senão então deixa quieto.
Se a aba's lista is not savable, repeat.
Save a aba's lista.
Se o de entrada/saída error estiver em branco, adicione 1 to o count; repeat.
Switch to a aba.
Show o ambiente.
Show error o de entrada/saída error.

Como handle save as pdf:
Se o current aba's mode for "docster", handle save as pdf usando o current aba's lista's documento, senão então deixa quieto.
Dê um latido.

Como handle select all:
Se o current aba's mode for "finder", handle select all usando o current aba's lista, senão então deixa quieto.
Se o current aba's mode for "editor", handle select all usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "docster", handle select all usando o current aba's lista's documento, senão então deixa quieto.
Se o current aba's mode for "writer", handle select all usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle send to back:
Se o current aba's mode for "writer", handle send to back usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle smooth:
Se o current aba's mode for "writer", handle smooth usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle sort definitions:
Se o current aba's mode for "editor", handle sort definitions usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle sort elementos usando um texto:
Se o current aba's mode for "finder", handle sort elementos usando o current aba's lista e o texto, senão então deixa quieto.
Dê um latido.

Como handle sort selected lines:
Se o current aba's mode for "editor", handle sort selected lines usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle sparte de cima:
Se o current process is 0, cluck, senão então deixa quieto.
Show estado "Sparte de cimaping...".
Sparte de cima o current process.
Show estado "Sparte de cimaped".

Como handle uncomment:
Se o current aba's mode for "editor", handle uncomment usando o current aba's lista's source, senão então deixa quieto.
Dê um latido.

Como handle undo:
Se o current aba's mode for "editor", handle undo usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "writer", handle undo usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle ungroup:
Se o current aba's mode for "writer", handle ungroup usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle unlabel:
Se o current aba's mode for "writer", handle unlabel usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle uppercase:
Se o current aba's mode for "editor", handle uppercase usando o current aba's lista's source, senão então deixa quieto.
Se o current aba's mode for "writer", handle uppercase usando o current aba's lista's page, senão então deixa quieto.
Dê um latido.

Como handle version:
Show estado o module's nome.

Como handle yank usando um número:
Se o current aba's mode for "writer", handle yank usando o current aba's lista's page e o número, senão então deixa quieto.
Dê um latido.

A handler é um texto.

Como initialize:
Initialize o ambiente.
Initialize o finder.
Initialize o editor.
Initialize o writer.
Initialize o compiler.
Fill a abas.
Switch to a abas' primeiro.

Como initialize o barra:
Coloque "normal" no barra's mode.
Coloque 1/4 inch plus 1/3 inch plus 1/4 inch no barra's altura.
Coloque o screen's left no barra's left.
Coloque o screen's parte de cima no barra's parte de cima.
Coloque o screen's right no barra's right.
Coloque o screen's parte de cima plus o barra's altura no barra's bottom.
Coloque o barra's caixa no barra's display.
Adjust o barra's display usando 1/4 inch e 1/4 inch e -1/4 inch e -1/4 inch.
Destrua o menu.
Create o menu.
Initialize o estado.

Como initialize o ambiente:
Coloque o lightest gray cor no ambiente cor.
Coloque o light sky cor no comment cor.
Coloque o lightest sky cor no grid cor.
Coloque a cor vermelha no tracer cor. \ tracer
Initialize o barra.
Initialize o switch.
Initialize o pad. \ uses o barra e o switch

Como initialize o pad:
Coloque o screen's left no pad's left.
Coloque o barra's bottom no pad's parte de cima.
Coloque o screen's right no pad's right.
Coloque o switch's parte de cima no pad's bottom.

Como initialize o estado:
Coloque o barra's display e 1/12 inch no estado' redonda caixa.
Coloque o menu's comandos' last's right no estado' left.

Como initialize o switch:
Initialize a abas.
Coloque 1/4 inch plus 1/3 inch no switch's altura.
Coloque o screen's left no switch's left.
Coloque o screen's bottom minus o switch's altura no switch's parte de cima.
Coloque o screen's right no switch's right.
Coloque o screen's bottom no switch's bottom.
Adjust a abas.

Como initialize some abas:
Adicione 1 to uma count.
Se o count is greater than 10, pare.
Create uma aba.
Posponha a aba to a abas.
Repita.

The menu é um menu.

A menu é uma coisa usando some comandos.

A mode é um texto.

The pad has uma caixa.

The palette width é uma width equal to 2-1/3 inches.

A prompt é um texto.

A pergunta é um texto.

Como run:
Start up.
Initialize.
Handle events.
Finalize.
Shut down.

The selected comando é uma comando.

A shortcut é um texto.

Como show o ambiente:
Desenhe o ambiente.
Refresh o screen.

Como show o ambiente usando estado:
Mude para uma cor vermelha estado' texto.
Se o current aba's mode for "finder", get o estado para o current aba's lista.
Se o current aba's mode for "editor", get o estado para o current aba's lista's source.
Se o current aba's mode for "docster", get o estado para o current aba's lista's documento.
Se o current aba's mode for "writer", get o estado para o current aba's lista's page.
Show o ambiente.

Como show o ambiente usando um texto:
Coloque o texto no estado' texto.
Show o ambiente.

Como show error um texto:
Create o dialog.
Coloque "error" no dialog's kind.
Coloque a cor vermelha no dialog's canetinha.
Coloque o texto no dialog's prompt.
Adicione uma botão to o dialog usando "M'kay".
Dê um latido.
Handle o dialog.
Destrua o dialog.

Como show o estado:
Desenhe o estado.
Refresh o screen usando o estado' redonda caixa.

Como show estado um número e um texto or outro texto:
Format o número e o texto or o outro texto dentro de uma terceiro texto.
Show estado o terceiro texto.

Como show estado um texto:
Coloque o texto no estado' texto.
Show o estado.

The estado has uma redonda caixa e um texto.

A submenu é um menu.

The submenu width é uma width equal to 2 inches.

The swatch width é uma width equal to 1/3 inch.

Como switch o barra to uma mode:
Coloque o mode no barra's mode.
Mude para uma cor vermelha estado' texto.
Show o ambiente.

Como switch o barra to uma mode usandoout drawing:
Coloque o mode no barra's mode.
Mude para uma cor vermelha estado' texto.

The switch has uma altura e uma caixa.

Como switch to o predecessor of uma aba:
Se a aba não existir, deixe quieto.
Coloque a aba's previous dentro doutra aba.
Se o outro aba não existir, put a abas' last no outro aba.
Switch to o outro aba.

Como switch to o successor of uma aba:
Se a aba não existir, deixe quieto.
Coloque a aba's próximo dentro doutra aba.
Se o outro aba não existir, put a abas' primeiro no outro aba.
Switch to o outro aba.

Como switch to uma aba:
Se a aba não existir, deixe quieto.
Se a aba is o current aba, deixe quieto.
Coloque a aba no current aba.

Como switch uma aba to uma lista:
Se a aba não existir, deixe quieto.
Se o lista não existir, deixe quieto.
Coloque o lista na aba's lista.
Coloque o lista's designator na aba's nome.
Lowercase a aba's nome.
Format a aba's nome to fit in a aba's caixa usando o default font.
Coloque "finder" na aba's mode.
Se o lista's source is not nil, put "editor" na aba's mode.
Se o lista's documento is not nil, put "docster" na aba's mode.
Se o lista's page is not nil, put "writer" na aba's mode.

A aba é uma coisa usando
uma caixa,
um nome,
uma mode,
uma lista.

The abas are some abas.

Como track o ambiente usando uma event:
Se o event não existir, deixe quieto.
Percorra.
Se o mouse's left botão is up, pare.
Coloque o mouse's local dentro de um local.
Se o local's x is greater than o screen's right, move o main window right; repeat.
Se o local's x is less than o screen's left, move o main window left; repeat.
Repita.

Como track o dialog usando uma event:
Se o dialog não existir, deixe quieto.
Se o dialog's caixa de texto não existir, deixe quieto.
Se o event não existir, deixe quieto.
Extend any selection in o dialog's caixa de texto usando o event's local.
Se o event's shift flag is not set, deselect o dialog's caixa de texto.
Clear uma selection.
Percorra.
If uma flag estiver verde, show o ambiente usando estado.
Se o mouse's left botão is up, deixe quieto.
Coloque o mouse's local dentro de um local.
Autoscroll o dialog's caixa de texto usando o local e o flag.
Extend any selection in o dialog's caixa de texto usando o local.
Se o dialog's caixa de texto's selection is o selection, repeat.
Mude para a cor verde flag.
Coloque o dialog's caixa de texto's selection no selection.
Repita.

Como track uma aba returning uma flag:
Mude para uma cor vermelha flag.
Se a aba não existir, deixe quieto.
Percorra.
Se o mouse's left botão is up, deixe quieto.
Coloque o mouse's local dentro de um local.
Encontre outra aba usando o local.
Se o outro aba não existir, repeat.
Se a aba is o outro aba, repeat.
Mude para a cor verde flag.
Remove a aba usando a abas.
Se o outro aba's left is less than a aba's left, insert a aba na abas before o outro aba.
Se o outro aba's left is greater than a aba's left, insert a aba na abas after o outro aba.
Adjust a abas.
Show o ambiente.
Repita.
