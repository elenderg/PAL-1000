Uma etiqueta é uma tag.  \já existe o termo label, então tag-não-pode ser etiqueta, an-não-ser que label vire marcador.

Uma etiqueta di carregamento eax é uma etiqueta.
Uma etiqueta di desreferência é uma etiqueta.
Uma etiqueta di endereço di carregamento é uma etiqueta.
Uma etiqueta di endereço di rotina é uma etiqueta.
Uma etiqueta di endereço push é uma etiqueta.
Uma etiqueta di epólogo é uma etiqueta.
Uma etiqueta di finalização é uma etiqueta.
Uma etiqueta di finalização condicional é uma etiqueta.
Uma etiqueta di incremento é uma etiqueta.
Uma etiqueta di loop é uma etiqueta.
Uma etiqueta di prólogo é uma etiqueta.
Uma etiqueta di quebra é uma etiqueta.
Uma etiqueta di repetição é uma etiqueta.
Uma etiqueta di saída é uma etiqueta.
Uma etiqueta di salto falso é uma etiqueta.
Uma etiqueta di salvar eax é uma etiqueta.
Uma etiqueta di valor push é uma etiqueta.
Uma etiqueta externa di chamada é uma etiqueta.
Uma etiqueta indireta di chamada é uma etiqueta.
Uma etiqueta intel é uma etiqueta.
Uma etiqueta interna di chamada é uma etiqueta.
Uma etiqueta-não é uma etiqueta.