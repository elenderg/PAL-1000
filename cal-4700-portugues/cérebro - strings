Uma alcunha é um texto.
Uma mensagem di erro é um texto.
Um nome é um texto.
Um texto hexadecimal é um texto.
Um subtexto é um texto.
Um vagão é um texto.
Um endereço di arquivo é um texto. \ endereço completo di arquivo = c:\pasta\subpasta\arquivo.extensão
Um frase é um texto.
Um alternativa é um texto.
A resposta é um texto.
Um acionador é um texto.
Um modo é um texto.
Um solicitação[prompt] é um texto. 
Um questão é um texto.
Um atalho é um texto.
Um texto binário é um texto. 
Um descrição é um texto.
Um tipo di unidade di disco é um texto.
Um nome da unidade di disco é um texto.
O texto localizado é um texto.
O erro di entrada/saída [i/o error] é um texto.
Um categoria[kind] é um texto.
Um mensagem é um texto. 
Um operação é um texto.
Uma frase de efeito [punchline] é um texto.
Um texto pesquisado é um texto.
Um recipiente é um texto.
Uma ojeção [reply] é um texto.
Um remetente é um texto.
Um servidor smtp é um texto.
Um assunto é um texto. 
Um caractere não identificado [token] é um texto.
Uma url é um texto.
Um texto grande é um texto.
Um marcador [label] é um texto.
Um nome di arquivo é um texto. \ after the last slash to end of endereço di arquivo[path] = file.ext
Um nome di arquivo sem extensão é um texto. \ after the last slash to last dot or end of endereço di arquivo[path] = file
Um extensão di arquivo é um texto. \ last dot to end of endereço di arquivo[path] = .ext
Um nome di pasta com barra é um texto. \ rightmost directory with slash = folder2\
Um designador é um texto. \ rightmost directory with slash = folder2\ OR after the last slash to end of endereço di arquivo[path] = file.ext
Um nome di pasta sem barra é um texto. \ rightmost directory without slash = folder2
Uma unidade di disco é um texto. \ começo do endereço do arquivo[path] até a primeira barra = c:\ 
\ Ou o começo do endereço do arquivo[path] até a quarta barra = \\computer\share\
Um texto contido nu pdf é um texto.
\ texto rodeado por ( ) tendo
\  "\(" como parênteses esquerdo
\ "\)" como parênteses direito
\ "\\" como barra invertida
