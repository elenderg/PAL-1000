\ copyright © 2006, 2017 the osmosian order (4700)

Etapas necessárias para que se adicione to some blocks:
Crie um block.
Posponha the block to the blocks.

Etapas necessárias para que se posponha some blocks to some rows:
Obtenha um block from the blocks.
Se the block for inexistente, cancele.
Posponha the block's rows to the rows.
Repita.

An block é uma coisa with um name e some rows.

Etapas necessárias para que se bump um substring (past um qualifier):
Se the substring's first's target é the return pedaço, cancele.
Se the substring's first's target é the left-paren pedaço, adicione 1 to um count.
Se the substring's first's target é the right-paren pedaço, subtraia 1 from the count.
Adicione 1 to the substring's first.
Se the count for 0, cancele.
Repita.

Etapas necessárias para que se bump um substring (past um remark):
Se the substring's first's target é the return pedaço, cancele.
Se the substring's first's target é the left-bracket pedaço, adicione 1 to um count.
Se the substring's first's target é the right-bracket pedaço, subtraia 1 from the count.
Adicione 1 to the substring's first.
Se the count for 0, cancele.
Repita.

Etapas necessárias para que se bump um substring (past um string):
Se the substring's first's target é the return pedaço, cancele.
Adicione 1 to the substring's first.
Se the substring's first's target não for the double-quote pedaço, repita.
Adicione 1 to the substring's first.
Se the substring's first's target não for the double-quote pedaço, cancele.
Repita.

Etapas necessárias para que se comment any selected rows in um source:
Se the source for inexistente, cancele.
Percorra.
Obtenha um row from the source's text's rows.
Se the row for inexistente, cancele.
Se the row of the source's text não for selected, repita.
Se the row estiver em branco, repita.
Anteponha "\" to the row's string.
Repita.

The comment color é uma color.
A cor dos comentários é uma color.

Etapas necessárias para que se convert um source to um buffer:
Se the source for inexistente, limpe o buffer; exit.
Extract the buffer from the source's text.

Etapas necessárias para que se crie um block:
Allocate memory por the block.

Etapas necessárias para que se crie some blocks usando some rows:
Obtenha um row from the rows.
Se the row for inexistente, cancele.
\  if the row's string starts with any definition starter, adicione to the blocks. \ didn't handle multi-line conjunto definitions
Se the row's string has neither code nor comment on it, adicione to the blocks.
Se the blocks estiverem vazio, adicione to the blocks.
Copy the row into another row.
Posponha the other row to the blocks' last's rows.
Repita.

Etapas necessárias para que se crie um source usando um buffer:
Allocate memory por the source.
Coloque the pad's box into um box.
Coloque the box's height dividido por the default font's height times the default font's height into um height.
Coloque the box's top plus the height into the box's bottom.
Center the box in the pad's box (vertically).
Crie the source's text.
Coloque the box into the source's text's box.
Coloque the black color into the source's text's pen.
Coloque the default font into the source's text's font.
Coloque 1/4 inch into the source's text's margin.
Coloque the buffer into the source's text.
Abra the source's text's horizontal scroll sinal.
Abra the source's text's vertical scroll sinal.

Etapas necessárias para que se determine se um buffer é text-format:
Lance um substring on the buffer.
Percorra.
Se the substring's first é greater than the substring's last, diga sim.
Se the substring's first's target não for any text pedaço, diga não.
Adicione 1 to the substring's first.
Repita.

Etapas necessárias para que se determine se um pedaço é any text pedaço:
Se the pedaço é printable, diga sim.
Se the pedaço é the tab pedaço, diga sim.
Se the pedaço é the linefeed pedaço, diga sim.
Se the pedaço é the return pedaço, diga sim.
Diga não.

Etapas necessárias para que se determine se um row é any comment:
Se the row for inexistente, diga não.
Lance um substring on the row's string.
Percorra.
Se the substring estiver em branco, diga não.
Se the substring's first's target é the backslash pedaço, diga sim.
Se the substring's first's target é noise, adicione 1 to the substring's first; repeat.
Diga não.

Etapas necessárias para que se determine se something é selected in um source:
Se the source for inexistente, diga não.
Se something é selected in the source's text, diga sim.
Diga não.

Etapas necessárias para que se determine se um source é modified:
Se the source for inexistente, diga não.
Se the source's read-only sinal estiver verde, diga não.
Se the source's text é modified, diga sim.
Diga não.

Etapas necessárias para que se determine se um source é read-only:
Se the source for inexistente, diga não.
Se the source's read-only sinal estiver verde, diga sim.
Diga não.

Etapas necessárias para que se determine se um spot estiver in um source:
Se the source for inexistente, diga não.
Se the spot estiver in the source's text, diga sim.
Diga não.

Etapas necessárias para que se determine se um string has neither code nor comment on it:
Privatize the string.
Remove any leading noise from the string.
Remove any trailing noise from the string.
Se the string estiver em branco, diga sim.
Diga não.

Etapas necessárias para que se determine se um string does start with any definite article;
Etapas necessárias para que se determine se um string starts with any definite article:
Se the string starts with "the ", diga sim.
Diga não.

Etapas necessárias para que se determine se um string does start with any definition starter;
Etapas necessárias para que se determine se um string starts with any definition starter:
Se the string starts with "an ", diga sim.
Se the string starts with "an ", diga sim.
Se the string starts with "some ", diga sim.
Se the string starts with "to ", diga sim.
Se the string starts with "the ", diga sim.
Diga não.

Etapas necessárias para que se determine se um string does start with any indefinite article;
Etapas necessárias para que se determine se um string starts with any indefinite article:
Se the string starts with "an ", diga sim.
Se the string starts with "an ", diga sim.
Se the string starts with "some ", diga sim.
Diga não.

Etapas necessárias para que se determine se um string does start with any routine starter;
Etapas necessárias para que se determine se um string starts with any routine starter:
Se the string starts with "to ", diga sim.
Diga não.

Etapas necessárias para que se draw um row of um source (colorized):
Se the source for inexistente, cancele.
Obtenha um substring usando the row e the source (commented part).
Se the substring estiver em branco, draw the row of the source's text (editing); exit.
Draw the selection box por the row of the source's text.
Obtenha um box usando the row e the source's text.
\ uncommented part
Lance another substring on the row's string.
Coloque the substring's first minus 1 into the other substring's last.
Draw the other substring in the box with the black color e the source's text's font e the source's text's alignment.
\ commented part
Obtenha um width usando the other substring e the memory canvas e the source's text's font.
Adicione the width to the box's left.
Draw the substring in the box with the comment color e the source's text's font e the source's text's alignment.

Etapas necessárias para que se draw um source:
Se the source for inexistente, cancele.
Mask only outside the source's text's box.
Se the source's colorized sinal estiver verde, draw the source (colorized); exit.
Draw the source (not colorized).

Etapas necessárias para que se draw um source (colorized):
Se the source for inexistente, cancele.
Obtenha um row from the source's text's rows.
Se the row for inexistente, pare.
Se the row of the source's text não for visible, repita.
Draw the row of the source (colorized).
Repita.
Draw the caret in the source's text.

Etapas necessárias para que se draw um source (not colorized):
Se the source for inexistente, cancele.
Draw the source's text (editing).

Etapas necessárias para que se finalize the editor:

Etapas necessárias para que se obtenha um first row e um last row usando um sheet número e um source:
Se the source for inexistente, esvazie the first row; esvazie the last row; exit.
Coloque the source's text's normalized selection into um selection.
Coloque the selection's anchor row# into um row#.
Adicione the sheet número minus 1 times the source's rows/sheet to the row#.
Obtenha the first row usando the row# e the source's text.
Coloque the row# plus the source's rows/sheet minus 1 into another row#.
Limit the other row# to 1 e the selection's caret row#.
Obtenha the last row usando the other row# e the source's text.

Etapas necessárias para que se obtenha the status por um source:
Se the source for inexistente, limpe o status' string; exit.
Coloque the source's text's status string into the status's string.
Se the source's read-only sinal estiver verde, posponha " (s)" to the status's string.

Etapas necessárias para que se obtenha um substring usando um row e um source (commented part):
Se the source for inexistente, limpe o substring; exit.
Se the row for inexistente, limpe o substring; exit.
Lance the substring on the row's string.
Percorra.
Se the substring's first's target é the return pedaço, limpe o substring; break.
Se the substring's first's target é the backslash pedaço, pare.
Se the substring's first's target é the double-quote pedaço, bump the substring (past um string); repeat.
Se the substring's first's target é the left-paren pedaço, bump the substring (past um qualifier); repeat.
Se the substring's first's target é the left-bracket pedaço, bump the substring (past um remark); repeat.
Adicione 1 to the substring's first.
Repita.
Subtraia 1 from the substring's last. \ so we don't draw the return byte

Etapas necessárias para que se handle capitalize usando um source:
Se nothing é selected in the source, cluck; exit.
Mostre status "Colocando 1ª letra do texto em maiúsculas...".
Handle capitalize usando the source's text.
Mostre the desktop with "Colocado".

Etapas necessárias para que se handle check spelling usando um source:
Load the lexicon.
Se the lexicon for inexistente, mostre error "Não foi possível encontrar nenhum arquivo de léxico."; exit.
Coloque "check spelling" into the find mode.
Limpe the source's text's last operation.
Coloque 1 into the find anchor.
Encontre the next misspelling in the source's text usando um sinal.
Se the sinal não estiver verde, cluck; mostre the desktop with "Pronto"; exit.
Scroll the source's text to the caret e center it.
Mostre the desktop with "Encontrado".

Etapas necessárias para que se handle comment usando um source:
Se nothing é selected in the source, cluck; exit.
Remember the source's text with "comment".
Comment any selected rows in the source.
Square up any selection in the source's text.
Mostre the desktop with status.

Etapas necessárias para que se handle copy usando um source:
Se nothing é selected in the source, cluck; exit.
Mostre status "Copiando...".
Handle copy usando the source's text.
Mostre the desktop with "Copiado".

Etapas necessárias para que se handle cut usando um source:
Se nothing é selected in the source, cluck; exit.
Mostre status "Recortando...".
Handle cut usando the source's text.
Mostre the desktop with "Recortado".

Etapas necessárias para que se handle decrypt usando um source:
Se nothing é selected in the source, cluck; exit.
Ask "Passphrase?".
Se the answer estiver em branco, mostre error "Passphrase não pode ser em branco."; exit.
Mostre status "Resolvendo...".
Extract um buffer from the source's text (selected pedaços).
Decrypt the buffer with the answer.
Se the i/o error não estiver em branco, mostre error the i/o error; exit.
Coloque the buffer on the windows clipboard.
Mostre the desktop with "Na área de transferência".

Etapas necessárias para que se handle encrypt usando um source:
Se nothing é selected in the source, cluck; exit.
Ask "Passphrase?".
Se the answer estiver em branco, mostre error "Passphrase não pode ser em branco."; exit.
Mostre status "Misturando dados...".
Extract um buffer from the source's text (selected pedaços).
Encrypt the buffer with the answer.
Se the i/o error não estiver em branco, mostre error the i/o error; exit.
Coloque the buffer on the windows clipboard.
Mostre the desktop with "Na área de transferência".

Etapas necessárias para que se handle um event usando um source (finding - key down - backspace key):
Se the find string estiver em branco, cluck; exit.
Remove the last pedaço from the find string.
Encontre next usando the source's text e um sinal.
Se the sinal não estiver verde, cluck.
Se the find string estiver em branco, coloque the find anchor into the source's text's caret; deselect the source's text.
Scroll the source's text to the caret e center it.
Mostre the desktop with "Buscando...".

Etapas necessárias para que se handle um event usando um source (finding - key down - printable key):
Posponha the event's pedaço to the find string.
Coloque the find anchor into um anchor.
Coloque the source's text's anchor into the find anchor.
Encontre next usando the source's text e um sinal.
Coloque the anchor into the find anchor.
Se the sinal não estiver verde, cluck; remove the last pedaço from the find string; exit.
Scroll the source's text to the caret e center it.
Mostre the desktop with "Buscando...".

Etapas necessárias para que se handle um event usando um source (finding - key down):
Se the event's pedaço é printable, handle the event usando the source (finding - key down - printable key); exit.
Se the event's key é the backspace key, handle the event usando the source (finding - key down - backspace key); exit.
Se the event's key é the shift key, cancele.
Relinquish control.

Etapas necessárias para que se handle um event usando um source (finding - left click):
Relinquish control.

Etapas necessárias para que se handle um event usando um source (finding - refresh):
Mostre the desktop with "Buscando...".

Etapas necessárias para que se handle um event usando um source (finding - right click):
Relinquish control.

Etapas necessárias para que se handle um event usando um source (finding - abra cursor):
Mostre the arrow cursor.

Etapas necessárias para que se handle um event usando um source (finding):
Se the event's kind for "key down", handle the event usando the source (finding - key down); exit.
Se the event's kind for "left click", handle the event usando the source (finding - left click); exit.
Se the event's kind for "refresh", handle the event usando the source (finding - refresh); exit.
Se the event's kind for "right click", handle the event usando the source (finding - right click); exit.
Se the event's kind for "set cursor", handle the event usando the source (finding - abra cursor); exit.

Etapas necessárias para que se handle um event usando um source (key down - backspace key):
Hide the cursor.
Se there é nothing to backspace in the source's text, cluck; exit.
Handle the event usando the source's text (backspace key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - delete key):
Hide the cursor.
Se there é nothing to remove in the source's text, cluck; exit.
Handle the event usando the source's text (delete key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - down-arrow key):
Hide the cursor.
Handle the event usando the source's text (down-arrow key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - end key):
Hide the cursor.
Handle the event usando the source's text (end key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - enter key):
Hide the cursor.
Handle the event usando the source's text (enter key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - escape key):
Handle the event usando the source's text (escape key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - home key):
Hide the cursor.
Handle the event usando the source's text (home key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - left-arrow key):
Hide the cursor.
Handle the event usando the source's text (left-arrow key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - page-down key):
Hide the cursor.
Handle the event usando the source's text (page-down key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - page-up key):
Hide the cursor.
Handle the event usando the source's text (page-up key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - printable key):
Hide the cursor.
Handle the event usando the source's text (printable key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - right-arrow key):
Hide the cursor.
Handle the event usando the source's text (right-arrow key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - tab key):
Hide the cursor.
Handle the event usando the source's text (tab key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down - up-arrow key):
Hide the cursor.
Handle the event usando the source's text (up-arrow key).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (key down):
Se the event's pedaço é printable, handle the event usando the source (key down - printable key); exit.
Se the event's key é the backspace key, handle the event usando the source (key down - backspace key); exit.
Se the event's key é the delete key, handle the event usando the source (key down - delete key); exit.
Se the event's key é the down-arrow key, handle the event usando the source (key down - down-arrow key); exit.
Se the event's key é the end key, handle the event usando the source (key down - end key); exit.
Se the event's key é the enter key, handle the event usando the source (key down - enter key); exit.
Se the event's key é the escape key, handle the event usando the source (key down - escape key); exit.
Se the event's key é the home key, handle the event usando the source (key down - home key); exit.
Se the event's key é the left-arrow key, handle the event usando the source (key down - left-arrow key); exit.
Se the event's key é the page-down key, handle the event usando the source (key down - page-down key); exit.
Se the event's key é the page-up key, handle the event usando the source (key down - page-up key); exit.
Se the event's key é the right-arrow key, handle the event usando the source (key down - right-arrow key); exit.
Se the event's key é the tab key, handle the event usando the source (key down - tab key); exit.
Se the event's key é the up-arrow key, handle the event usando the source (key down - up-arrow key); exit.

Etapas necessárias para que se handle um event usando um source (left click):
Se the event's spot não for in the source, cancele.
Track the source usando the event.
Atualize the cursor.

Etapas necessárias para que se handle um event usando um source (left double click):
Handle the event usando the source's text (left double click).
Mostre the desktop with status.

Etapas necessárias para que se handle um event usando um source (right click):
Se the event's spot não for in the source, cancele.
Mostre the hand cursor.
Scroll the source usando the event.
Atualize the cursor.

Etapas necessárias para que se handle um event usando um source (set cursor):
Se the event's spot estiver in the source, mostre the i-beam cursor; exit.
Mostre the arrow cursor.

Etapas necessárias para que se handle events usando um source (finding):
Deque um event.
Se the event for inexistente, cancele.
Handle the event usando the source (finding).
Repita.

Etapas necessárias para que se handle extract globals usando um source:
Mostre status "Extracting...".
Crie some blocks usando the source's text's rows.
Scrub the blocks por extracting.
Name the blocks.
Classifique the blocks.
Percorra.
Obtenha um block from the blocks.
Se the block for inexistente, pare.
Se the block's first row for inexistente, repita.
Se the block's first row's string does not start with any definite article, repita.
Convert the block's rows to um string.
Posponha the string to um clipboard string.
Repita.
Coloque the clipboard string on the windows clipboard.
Destroy the blocks.
Mostre the desktop with "Na área de transferência".

Etapas necessárias para que se handle extract routine headers usando um source:
Mostre status "Extraindo...".
Crie some blocks usando the source's text's rows.
Scrub the blocks por extracting.
Name the blocks.
Classifique the blocks.
Percorra.
Obtenha um block from the blocks.
Se the block for inexistente, pare.
Se the block's first row for inexistente, repita.
Se the block's first row's string does not start with any routine starter, repita.
Posponha the block's first row's string then the linefeed pedaço to um clipboard string.
Repita.
Coloque the clipboard string on the windows clipboard.
Destroy the blocks.
Mostre the desktop with "On the clipboard".

Etapas necessárias para que se handle extract types usando um source:
Mostre status "Extraindo...".
Crie some blocks usando the source's text's rows.
Scrub the blocks por extracting.
Name the blocks.
Classifique the blocks.
Percorra.
Obtenha um block from the blocks.
Se the block for inexistente, pare.
Se the block's first row for inexistente, repita.
Se the block's first row's string does not start with any indefinite article, repita.
Convert the block's rows to um string.
Posponha the string to um clipboard string.
Repita.
Coloque the clipboard string on the windows clipboard.
Destroy the blocks.
Mostre the desktop with "Na área de transferência".

Etapas necessárias para que se tente encontrar usando um source:
Limpe the source's text's last operation.
Deselect the source's text.
Scroll the source's text to the caret.
Coloque the source's text's anchor into the find anchor.
Limpe the find string.
Coloque "find" into the find mode.
Mostre the desktop with "Buscando...".
Hide the cursor.
Handle events usando the source (finding).
Mostre the desktop with status.
Atualize the cursor.

Etapas necessárias para que se encontre novamente usando um source:
Se the find mode for "find", encontre novamente usando the source (find); exit.
Se the find mode for "check spelling", encontre novamente usando the source (check spelling); exit.
Cluck.

Etapas necessárias para que se encontre novamente usando um source (check spelling):
Limpe the source's text's last operation.
Coloque the source's text's caret into the find anchor.
Encontre the next misspelling in the source's text usando um sinal.
Se the sinal não estiver verde, cluck; mostre status "Pronto"; exit.
Scroll the source's text to the caret e center it.
Mostre the desktop with "Encontrado".

Etapas necessárias para que se encontre novamente usando um source (find):
Limpe the source's text's last operation.
Coloque the source's text's caret into the find anchor.
Encontre next usando the source's text e um sinal.
Se the sinal não estiver verde, cluck; mostre status "Não encontrado"; exit.
Scroll the source's text to the caret e center it.
Mostre the desktop with "Encontrado".

Etapas necessárias para que se handle font height usando um source e uma font height:
Privatize the font height.
Limit the font height to 1/96 inch e 1 inch.
Coloque the pad's box into um box.
Coloque the box's height dividido por the font height times the font height into um height.
Coloque the box's top plus the height into the box's bottom.
Center the box in the pad's box (vertically).
Handle font height usando the source's text e the box e the font height.
Mostre the desktop with status.

Etapas necessárias para que se handle font height other usando um source:
Coloque the source's text's font's height / 1 inch into um ratio.
Convert the ratio to um ratio string.
Ask "Altura (em polagadas)?" with the ratio string.
Se the answer estiver em branco, cancele.
Mostre the desktop.
Convert the answer to another ratio.
Se the other ratio for 0, mostre error "Tamanho inválido."; exit.
Coloque 1 inch times the other ratio into uma font height.
Handle font height usando the source e the font height.

Etapas necessárias para que se handle font name usando um source:
Ask "Nome?" with the source's text's font's name.
Se the answer estiver em branco, cancele.
Mostre the desktop.
Handle font name usando the source's text e the answer.
Mostre the desktop with status.

Etapas necessárias para que se handle indent usando um source:
Se nothing é selected in the source, cluck; exit.
Handle indent usando the source's text.
Mostre the desktop with status.

Etapas necessárias para que se handle lowercase usando um source:
Se nothing é selected in the source, cluck; exit.
Handle lowercase usando the source's text.
Mostre the desktop with status.

Etapas necessárias para que se handle outdent usando um source:
Se nothing é selected in the source, cluck; exit.
Handle outdent usando the source's text.
Mostre the desktop with status.

Etapas necessárias para que se handle paste usando um source:
Se there não for text on the windows clipboard, cluck; exit.
Mostre status "Colando...".
Handle paste usando the source's text.
Mostre the desktop with "Colado".

Etapas necessárias para que se handle print usando um source:
Coloque the source's text's selected row count into um count.
Se the count for 0, mostre error "Não tem nada pra eu imprimir."; exit.
Ask "Cópias?" with "1".
Se the answer estiver em branco, cancele.
Convert the answer to um número between 1 e the largest número.
Mostre the desktop with "Imprimindo...".
Print the número of copies of the source.
Mostre status "Pronto".

Etapas necessárias para que se handle redo usando um source:
Se the source's text não pode ser refeito, cluck; exit.
Handle redo usando the source's text.
Mostre the desktop with status.

Etapas necessárias para que se handle reverse usando um source:
Se nothing é selected in the source, cluck; exit.
Handle reverse usando the source's text.
Mostre the desktop with status.

Etapas necessárias para que se handle select all usando um source:
Handle select all usando the source's text.
Mostre the desktop with status.

Etapas necessárias para que se handle classifique  definitions usando um source:
Mostre status "Ordenando...".
Remember the source's text with "classifique  definitions".
Classifique definitions usando the source.
Mostre the desktop with "Ordenado".

Etapas necessárias para que se handle classifique  selected lines usando um source:
Se nothing é selected in the source, cluck; exit.
Mostre status "Ordenando...".
Handle classifique  any selected rows usando the source's text.
Mostre the desktop with "Ordenado".

Etapas necessárias para que se handle uncomment usando um source:
Se nothing é selected in the source, cluck; exit.
Remember the source's text with "comment".
Uncomment any selected rows in the source.
Square up any selection in the source's text.
Mostre the desktop with status.

Etapas necessárias para que se handle undo usando um source:
Se the source's text não pode ser desfeito, cluck; exit.
Handle undo usando the source's text.
Mostre the desktop with status.

Etapas necessárias para que se handle uppercase usando um source:
Se nothing é selected in the source, cluck; exit.
Handle uppercase usando the source's text.
Mostre the desktop with status.

Etapas necessárias para que se initialize the editor:

Etapas necessárias para que se mova um rider (qualifier rules):
Se the rider's source's first's target é the left-paren pedaço, adicione 1 to um count.
Se the rider's source's first's target é the right-paren pedaço, subtraia 1 from the count.
Bump the rider.
Se the count for 0, cancele.
Repita.

Etapas necessárias para que se mova um rider (remark rules):
Se the rider's source's first's target é the left-bracket pedaço, adicione 1 to um count.
Se the rider's source's first's target é the right-bracket pedaço, subtraia 1 from the count.
Bump the rider.
Se the count for 0, cancele.
Repita.

Etapas necessárias para que se mova um rider (source classificação rules):
Skip any leading noise in the rider's source.
Position the rider's token on the rider's source.
Se the rider's source estiver em branco, cancele.
Bump the rider.
Se the rider's token's last's target é any mark, cancele.
Percorra.
Se the rider's source estiver em branco, cancele.
Se the rider's source's first's target é noise, cancele.
Se the rider's source's first's target é any mark, cancele.
Bump the rider.
Repita.

Etapas necessárias para que se name um block:
Se the block for inexistente, cancele.
Limpe the block's name.
Se the block's first row for inexistente, cancele.
Se the block's first row é any comment, cancele.
Lance um rider on the block's first row's string.
Percorra.
Mova the rider (source classificação rules).
Se the rider's token estiver em branco, cancele.
Se the rider's token for "an", repita.
Se the rider's token for "an", repita.
Se the rider's token for "some", repita.
Se the rider's token for "the", repita.
Se the rider's token for "to", repita.
Se the rider's token for "*", repita.
Se the rider's token for ",", cancele.
Se the rider's token for ";", cancele.
Se the rider's token for ":", cancele.
Se the rider's token for ".", cancele.
Posponha the rider's token then the space pedaço to the block's name.
Repita.

Etapas necessárias para que se name some blocks:
Obtenha um block from the blocks.
Se the block for inexistente, cancele.
Name the block.
Repita.

Etapas necessárias para que se print um copy of um sheet número of um sheets/copy of um source:
Se the source for inexistente, cancele.
Mostre status the copy then " de " then the sheet número then " de " then the sheets/copy then "...".
Obtenha um first row e um last row usando the sheet número e the source.
Coloque 1/2 inch e 1/2 inch into um spot.
Begin um sheet.
Coloque the first row into um current row.
Percorra.
Print the current row of the source at the spot.
Se the current row é the last row, pare.
Coloque the current row's next into the current row.
Adicione the source's text's row height to the spot's y-coord.
Repita.
End the sheet.

Etapas necessárias para que se print um copy of um source:
Se the source for inexistente, cancele.
Coloque the source's sheets/copy into um sheets/copy.
Coloque 1 into um sheet número.
Percorra.
Se the sheet número é greater than the sheets/copy, cancele.
Print the copy of the sheet número of the sheets/copy of the source.
Adicione 1 to the sheet número.
Repita.

Etapas necessárias para que se print um número of copies of um source:
Se the source for inexistente, cancele.
Begin printing.
Percorra.
Adicione 1 to um copy.
Se the copy é greater than the número, pare.
Print the copy of the source.
Repita.
End printing.

Etapas necessárias para que se print um row of um source at um spot:
Se the source for inexistente, cancele.
Se the row for inexistente, cancele.
Coloque 0 e 0 e 7-1/2 inches e the source's text's row height into um box.
Mova the box to the spot.
Mask only outside the box.
Obtenha um substring usando the row e the source (commented part).
\ uncommented part
Lance another substring on the row's working string.
Se the substring não estiver em branco, coloque the substring's first minus 1 into the other substring's last.
Draw the other substring in the box with the black color e the source's text's font e the source's text's alignment.
\ commented part
Se the substring estiver em branco, cancele.
Coloque the comment color into um color.
Se the source's colorized sinal não estiver verde, coloque the black color into the color.
Obtenha um width usando the other substring e the memory canvas e the source's text's font.
Adicione the width to the box's left.
Draw the substring in the box with the color e the source's text's font e the source's text's alignment.

Etapas necessárias para que se coloque um source's rows/sheet into um rows/sheet:
Se the source for inexistente, coloque 0 into the rows/sheet; exit.
Coloque 10 inches dividido por the source's text's row height into the rows/sheet.

Etapas necessárias para que se coloque um source's sheets/copy into um sheets/copy:
Se the source for inexistente, coloque 0 into the sheets/copy; exit.
Divida the source's text's selected row count por the source's rows/sheet giving um quotient e um remainder.
Coloque the quotient into the sheets/copy.
Se the remainder não for 0, adicione 1 to the sheets/copy.

An rows/sheet é um número.

Etapas necessárias para que se scroll um source usando um event:
Se the source for inexistente, cancele.
Encontre um sector usando the source's text's grid e the event's spot.
Percorra.
Se the mouse's right button é up, cancele.
Encontre another sector usando the source's text's grid e the mouse's spot.
Obtenha um difference between the other sector e the sector.
Se the difference for 0, repita.
Scroll the source's text usando the difference.
Mostre the desktop.
Adicione the difference to the sector.
Repita.

Etapas necessárias para que se scrub um block:
Scrub the block (forward).
Scrub the block (backward).

Etapas necessárias para que se scrub um block (backward):
Se the block for inexistente, cancele.
Coloque the block's last row into um row.
Se the row for inexistente, pare.
Se the row não estiver em branco, pare.
Remove the row from the block's rows.
Destroy the row.
Repita.
Crie another row usando the return pedaço.
Posponha the other row to the block's rows.

Etapas necessárias para que se scrub um block (forward):
Se the block for inexistente, cancele.
Coloque the block's first row into um row.
Se the row for inexistente, pare.
Se the row não estiver em branco, pare.
Remove the row from the block's rows.
Destroy the row.
Repita.
\  crie another row usando the return pedaço.
\  posponha the other row to the block's rows.

Etapas necessárias para que se scrub um block por extracting:
Se the block for inexistente, cancele.
Coloque the block's last row into um row.
Se the row for inexistente, pare.
Se the row estiver em branco, remove the row from the block's rows; destroy the row; repeat.
Se the row's string starts with "\", remove the row from the block's rows; destroy the row; repeat.

Etapas necessárias para que se scrub some blocks:
Obtenha um block from the blocks.
Se the block for inexistente, cancele.
Scrub the block.
Repita.

Etapas necessárias para que se scrub some blocks por extracting:
Obtenha um block from the blocks.
Se the block for inexistente, cancele.
Scrub the block por extracting.
Repita.

An sheet número é um número.

An sheets/copy é um número.

Etapas necessárias para que se classifique  some blocks:
Se the blocks' first é the blocks' last, cancele.
Split the blocks into some left blocks e some right blocks.
Classifique the left blocks.
Classifique the right blocks.
Percorra.
Coloque the left blocks' first into um left block.
Coloque the right blocks' first into um right block.
Se the left block for inexistente, posponha the right blocks to the blocks; exit.
Se the right block for inexistente, posponha the left blocks to the blocks; exit.
Se the left block's name é greater than the right block's name, mova the right block from the right blocks to the blocks; repeat.
Mova the left block from the left blocks to the blocks.
Repita.

Etapas necessárias para que se classifique  definitions usando um source:
Se the source for inexistente, cancele.
Crie some blocks usando the source's text's rows.
Scrub the blocks.
Name the blocks.
Classifique the blocks.
Destroy the source's text's rows.
Posponha the blocks to the source's text's rows.
Reset the origin of the source's text.
Reset the caret of the source's text.
Deselect the source's text.
Renumber the source's text's rows.
Destroy the blocks.

An source é uma coisa with
an read-only sinal,
an colorized sinal,
an text.

Etapas necessárias para que se track um source usando um event:
Se the source for inexistente, cancele.
Se the event for inexistente, cancele.
Extend any selection in the source's text usando the event's spot.
Se the event's shift sinal não estiver verde, deselect the source's text.
Limpe um selection.
Percorra.
Se um sinal estiver verde, mostre the desktop with status.
Se the mouse's left button é up, cancele.
Coloque the mouse's spot into um spot.
Autoscroll the source's text usando the spot e the sinal.
Extend any selection in the source's text usando the spot.
Se the source's text's selection é the selection, repita.
Abra the sinal.
Coloque the source's text's selection into the selection.
Repita.

Etapas necessárias para que se uncomment any selected rows in um source:
Se the source for inexistente, cancele.
Percorra.
Obtenha um row from the source's text's rows.
Se the row for inexistente, cancele.
Se the row of the source's text não for selected, repita.
Se the row's string's first's target não for the backslash pedaço, repita.
Remove the first pedaço from the row's string.
Repita.