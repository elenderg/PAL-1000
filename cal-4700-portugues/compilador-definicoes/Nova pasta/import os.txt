import os
import pathlib
from pathlib import Path
  
# muda a pasta para a pasta desejada
os.chdir(r"C:\Users\elenderg\Documents\GitHub\Portugues-Puro\cal-4700-portugues\compilador-definicoes")

PastaAtual = os.getcwd() 
  
print("Pasta Atual = ", PastaAtual)


def main(): 
  
    for count, filename in enumerate(PastaAtual): 

        CaminhoDoArquivo = os.path.realpath(__file__)
        print(CaminhoDoArquivo)                             
        NovoNomedoArquivo = open(CaminhoDoArquivo).readline().rstrip() +  ".txt" #define o nome do arquivo
        print( NovoNomedoArquivo)
        NovoNomedoArquivo.replace('\\', '')
        NovoNomedoArquivo.replace('/', '')
        NovoNomedoArquivo.replace(':', '')                                         
        NovoNomedoArquivo.replace('?', '')
        NovoNomedoArquivo.replace('<', '')
        NovoNomedoArquivo.replace('>', '')
        NovoNomedoArquivo.replace('|', '')
        NovoNomedoArquivo.replace('*', '')
        print(NovoNomedoArquivo)
        NovoNomedoArquivo =  PastaAtual +  NovoNomedoArquivo # junta o nome anterior a pasta atual
          
        #rename() function will 
        #rename all the files 
        os.rename(CaminhoDoArquivo, NovoNomedoArquivo) 
  
# Driver Code 
if __name__ == '__main__': 
      
# Calling main() function 
    main() 
