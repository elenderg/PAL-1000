An aba is um tab.

An abrangência retrita is um local.

An alcunha is um moniker.

An alcunha parcial is um partial moniker.

An altura da fonte is um font height.

An altura-de-uma letra is um letter height.

An altura is um height.

An ângulo is um angle.

An área-de-transferência-de-formas is um shape clipboard.

An área-de-transferência-de-página is um page clipboard.

An arquivo is um file.

An arranhão is um scratch .

An aumento is um rise.

An balde is um bucket.

An brilho is um brightness .

An caminho is um path.

An caractere is um character.

An claridade is um lightness.

An coisa is um thing.

An coluna-com-# is um column#.

An comando is um command.

An comprimento is um length.

An contador is um counter.

An contagem-de-baldes is um bucket count.

An contagem-de-pedaços is um byte count.

An contagem is um count.

An contexto is um context.

An coordenada is um coord.

An cópia is um copy.

An cor-de-arco íris is um rainbow color.

An corrida is um run.

An deslocamento is um offset.

An diploma is um degree.

An diretório is um directory .

An distância is um distance.

An elemento is um nodette.

An entrada-de-pilha is um stack entry.

An entrada is um input.

An esquerda is um left.

An evento is um event.

An exibidor-de-texto is um console.

An figura geométrica is um geometric figure.

An figura is um figure.

An fila-de-eventos is um event queue.

An forma is um shape.

An grão is um grain.

An grau preciso is um precise degree.

An grupo-de-componentes is um gptoken.

An grupo-de-imagens is um gpimage.

An Identificador-de-Classe is um clsid.

An Identificador-de-Interface MAPI is um iid.

An Identificador Global Exclusivo is um guid.

An intermediário is um intermediate.

An janela is um window.

An largura is um width.

An letra is um letter. \-não-usar o termo carta aqui

An linha-de-base is um baseline.

An lista is um node.

An locutor is um talker.

An marca is um tick.

An margem is um margin.

An máscara is um mask.

An matiz is um hue.

An máximo divisor comum is um gcd.

An mdc is um gcd.

An milisegundo is um ms.

An milissegundo is um millisecond.

An minialcunha is um monikette.

An mínimo múltiplo comum is um lcm.

An mmc is um lcm.

An múltiplo is um multiple.

An nome is um name.

An número aleatório is um  random number.

An número-de-linha is um row# .

An número-de-página is um page number.

An número-de-texto is um string#.

An número-de-vezes is um times.

An onda is um wave.

An página is um page.

An painel is um canvas.

An parâmetro longo is um l-param.

An parâmetro word is um w-param.

An pedacinho is um bit.

An número-de-pedaço is um byte#.

An polígono is um polygon.

An ponto is um point.

Some pontos extras are some extra points.

An porcentagem is um percent.

An posição absoluta is um absolute position.

An processo is um process.

An profundidade is um depth.

An próxima letra is um letra.

An quantia is um amount.

An quociente is um quotient.

An raio is um radius.

An raiz quadrada is um square root.

An recuo direito is um indent.

An recuo-para-esquerda is um outdent .

An recurso-de-fonte is um font resource.

An recurso-de-fonte osmosiana is um recurso-de-fonte.

An referência-de-tarefa is um routine reference.

An registro is um record.

An resposta is um reply.

An resto is um remainder.

An retângulo is um rectangle.

An saturação is um saturation.

An segundo is um second.

An seta chique is um fancy arrow.

An setinha is um cursor.

An subtexto is um substring.

An tamanho is um size.

An tarefa is um routine.

An texto-de-consulta is um query string.

An texto hexadecimal is um hex string.

An texto is um string.

An tipo integrado is um built-in type.

An tipo is um type.

An título is um heading.

An topo is um top.

An vagão is um buffer.

An variável is um variable.

An aba atual is um aba.

An aba gratuita is um aba.

An altura da letra is um letter height.

An altura da letra grande is um altura da letra.

An altura da letra pequena is um altura da letra.

\An cabeçalho-de-importação is um registro with

\An endereço called original first thunk,

\An número called timestamp,

\An endereço called forwarder chain,

\An endereço called name memory address,

\An endereço called first thunk memory address.

An contagem-de-heap is um número.

An contagem-de-pilhas-e-montes is um número.

The corte-de-texto is um número equal to 500.

An cursor-de-mão is um cursor.

An cursor-de-seta is um cursor.

An cursor-de-texto is um cursor.

An endereço is um number.

\An Identificador Único Universal is um registro with
\An número called d1,
\An metade-de-uma barra [wyrd = 2 bytes] called d2,
\An metade-de-uma barra [wyrd = 2 bytes] called d3,
\8 bytes [8 bytes = 2 barras] called d4.

\An indicador is um pointer.

An janela principal is um window.

An quantidade-de-linhas numa folha [rows/sheet] is um number.

The margem-de-texto is um número equal to 1/24 inch.

The máximo-de-desfazimentos numa página is um número equal to 32.

The máximo-de-vezes que se-pode desfazer num texto is um contagem equal to 32.

An menu atual is um menu.

\An número is um number.

An número-de-cor-de-arco-íris atual is um número.
[que vai-de-1 até 6 indicando, respectivamente as cores, vermelho, laranja, amarelo, verde, azul-e-roxo]

The número-de-página memorizado is um número equal to 1.

An painel da tela is um painel[canvas].

An painel-de-impressão is um painel.

An painel-de-memória is um painel.

An arquivo pdf is um pdf.

An entrada da pilha is um coisa with um texto.

Some pilhas-de-entrada are some stack entries.

\An contexto is um coisa with um spot[local], um caixa, um título, um tamanho da letra, um cor, um número, \ pen width? ***

The contexto da pilha is some contexts.

\The pilha-de-contexto is some context stack.

An processo atual is um process.

The recuo-de-documento is um número equal to 2.

The semente aleatória is um número. \ https://pt.wikipedia.org/wiki/Semente_aleatória 

An latido is um wave. \ originalmente é um cacarejo, mas pretendo mudar.